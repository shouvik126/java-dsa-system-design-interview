---
layout: default
title: Quiz - Designing Instagram
permalink: /System-Design/4-system-design-problems/4.5-quiz-designing-instagram/
prev_title: "Designing Instagram"
prev_url: "/System-Design/4-system-design-problems/4.5-designing-instagram/"
---

# Quiz: Designing Instagram

Test your architectural knowledge of Instagram system design with **9 scenario-driven interview questions** covering CAP availability trade-offs, read/write connection decoupling, PhotoID sharding, 64-bit epoch ID layouts, KGS offset sequences, pre-generated news feeds, and hybrid Push/Pull fan-out models.

---

<!-- Quiz Dashboard -->
<div class="quiz-dashboard">
  <div class="dash-stat">
    <span class="dash-label">Score</span>
    <span class="dash-value" id="quiz-score">0 / 9</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Progress</span>
    <span class="dash-value" id="quiz-progress">0 / 9 Answered</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Accuracy</span>
    <span class="dash-value" id="quiz-accuracy">0%</span>
  </div>
  <button class="quiz-reset-btn" onclick="resetQuiz()">
    <i class="fas fa-redo-alt"></i> Reset Quiz
  </button>
</div>

<div class="quiz-progress-bar-container">
  <div class="quiz-progress-bar" id="quiz-progress-bar" style="width: 0%;"></div>
</div>

<div id="quiz-container"></div>

<style>
/* Quiz Styling - Compatible with Light & Dark Themes */
.quiz-dashboard {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-secondary, #f8fafc);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 16px 24px;
  margin-bottom: 16px;
}

html.dark-theme .quiz-dashboard {
  background: #1e293b;
  border-color: #334155;
}

.dash-stat {
  display: flex;
  flex-direction: column;
}

.dash-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted, #64748b);
  font-weight: 600;
}

.dash-value {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-color, #0f172a);
}

html.dark-theme .dash-value {
  color: #f8fafc;
}

.quiz-reset-btn {
  background: #3b82f6;
  color: #ffffff;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, transform 0.1s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.quiz-reset-btn:hover {
  background: #2563eb;
  transform: translateY(-1px);
}

.quiz-progress-bar-container {
  width: 100%;
  height: 8px;
  background: var(--border-color, #e2e8f0);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 32px;
}

html.dark-theme .quiz-progress-bar-container {
  background: #334155;
}

.quiz-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #3b82f6, #10b981);
  transition: width 0.3s ease;
}

.quiz-card {
  background: var(--bg-color, #ffffff);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 28px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  transition: border-color 0.2s ease;
}

html.dark-theme .quiz-card {
  background: #0f172a;
  border-color: #1e293b;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

.quiz-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.quiz-topic {
  font-size: 0.82rem;
  font-weight: 600;
  color: #2563eb;
  background: rgba(37, 99, 235, 0.1);
  padding: 4px 10px;
  border-radius: 20px;
}

html.dark-theme .quiz-topic {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.15);
}

.quiz-qnum {
  font-size: 0.85rem;
  color: var(--text-muted, #64748b);
  font-weight: 500;
}

.quiz-question {
  font-size: 1.1rem;
  font-weight: 600;
  margin-top: 6px;
  margin-bottom: 20px;
  color: var(--text-color, #0f172a);
  line-height: 1.5;
}

html.dark-theme .quiz-question {
  color: #f1f5f9;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.quiz-opt {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  width: 100%;
  text-align: left;
  background: var(--bg-secondary, #f8fafc);
  border: 1.5px solid var(--border-color, #e2e8f0);
  border-radius: 10px;
  padding: 14px 16px;
  font-size: 0.95rem;
  color: var(--text-color, #334155);
  cursor: pointer;
  transition: all 0.2s ease;
}

html.dark-theme .quiz-opt {
  background: #1e293b;
  border-color: #334155;
  color: #cbd5e1;
}

.quiz-opt:hover:not(:disabled) {
  border-color: #3b82f6;
  background: rgba(59, 130, 246, 0.05);
}

html.dark-theme .quiz-opt:hover:not(:disabled) {
  border-color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
}

.opt-letter {
  font-weight: 700;
  background: var(--border-color, #cbd5e1);
  color: #0f172a;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  flex-shrink: 0;
  font-size: 0.85rem;
}

html.dark-theme .opt-letter {
  background: #475569;
  color: #f8fafc;
}

.opt-text {
  line-height: 1.45;
  flex-grow: 1;
}

/* Correct Option State */
.quiz-opt.opt-correct {
  background: #dcfce7 !important;
  border-color: #16a34a !important;
  color: #14532d !important;
}

.quiz-opt.opt-correct .opt-letter {
  background: #16a34a !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-correct {
  background: rgba(22, 163, 74, 0.2) !important;
  border-color: #22c55e !important;
  color: #bbf7d0 !important;
}

/* Incorrect Option State */
.quiz-opt.opt-incorrect {
  background: #fef2f2 !important;
  border-color: #dc2626 !important;
  color: #7f1d1d !important;
}

.quiz-opt.opt-incorrect .opt-letter {
  background: #dc2626 !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-incorrect {
  background: rgba(220, 38, 38, 0.2) !important;
  border-color: #ef4444 !important;
  color: #fecaca !important;
}

/* Explanation Box */
.quiz-explanation {
  margin-top: 18px;
  padding: 16px;
  border-radius: 10px;
  font-size: 0.93rem;
  line-height: 1.55;
  display: none;
}

.quiz-explanation.exp-right {
  background: #f0fdf4;
  border: 1px solid #86efac;
  color: #166534;
}

html.dark-theme .quiz-explanation.exp-right {
  background: rgba(22, 163, 74, 0.15);
  border-color: #22c55e;
  color: #bbf7d0;
}

.quiz-explanation.exp-wrong {
  background: #fef2f2;
  border: 1px solid #fca5a5;
  color: #991b1b;
}

html.dark-theme .quiz-explanation.exp-wrong {
  background: rgba(220, 38, 38, 0.15);
  border-color: #ef4444;
  color: #fecaca;
}

.exp-header {
  font-weight: 700;
  font-size: 1rem;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.exp-body {
  margin-top: 6px;
}
</style>

<script>
const quizQuestions = [
  {
    id: 1,
    topic: "Consistency vs. Availability Trade-off",
    question: "Why does Instagram's News Feed architecture choose High Availability over Strong Consistency in its non-functional requirements?",
    options: {
      A: "Because strong consistency requires hardware GPU acceleration for image compression.",
      B: "Because seeing a friend's new photo a few seconds late is an acceptable user trade-off, whereas feed downtime or sub-200ms latency violations degrade user experience severely.",
      C: "Because NoSQL databases cannot store image metadata.",
      D: "Because HTTP 302 redirects fail if consistency is enabled."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Consistency models define read/write visibility bounds, not GPU image compression.",
      B: "Option B is correct! In social networks, eventual consistency is acceptable: a user receiving a new post 5 seconds late causes no harm. However, feed downtime or high rendering latency (>200ms) causes immediate user drop-off, making High Availability (AP) the right CAP trade-off.",
      C: "Option C is incorrect. NoSQL stores excel at storing metadata records.",
      D: "Option D is incorrect. HTTP status codes operate independently of database consistency levels."
    }
  },
  {
    id: 2,
    topic: "Connection Decoupling Architecture",
    question: "Uploading photos is a slow, disk-bound write process, whereas fetching news feeds is a fast, in-memory read process. Why must application web servers isolate read requests onto separate dedicated server pools from write uploads?",
    options: {
      A: "To prevent slow photo uploads from exhausting web server HTTP connection limits (e.g. 500 connections/server) and starving incoming read/feed requests.",
      B: "Because SQL databases block read queries whenever a write connection opens.",
      C: "To convert PNG files into MP4 video formats.",
      D: "Because Amazon S3 rejects write connections from web servers."
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! Web servers have fixed concurrent connection caps (e.g. 500 sockets). Large media uploads hold HTTP connections open for seconds. If reads and writes share the same server pool, heavy upload spikes consume all 500 sockets, starving fast read requests. Decoupling read/write server pools isolates traffic.",
      B: "Option B is incorrect. Modern databases process concurrent reads and writes using MVCC (Multi-Version Concurrency Control).",
      C: "Option C is incorrect. Video transcoding is handled by asynchronous background worker fleets.",
      D: "Option D is incorrect. Amazon S3 natively accepts concurrent HTTP PUT/POST writes."
    }
  },
  {
    id: 3,
    topic: "Database Partitioning Strategy",
    question: "When sharding the 3.7 TB metadata database, why is partitioning by `PhotoID` (`PhotoID % N`) preferred over partitioning by `UserID` (`UserID % N`)?",
    options: {
      A: "Partitioning by `UserID` makes SQL joins impossible across all database tables.",
      B: "Partitioning by `UserID` creates severe hotspots on celebrity accounts and non-uniform storage distribution, whereas `PhotoID` sharding distributes rows and QPS uniformly.",
      C: "Because `PhotoID` strings are 50% smaller than `UserID` strings.",
      D: "Because `UserID` sharding requires hardware load balancers."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Multi-shard joins are complex under both schemes, but sharding strategy focuses on load distribution.",
      B: "Option B is correct! `UserID` partitioning places all photos of a user on a single shard. High-volume posters fill their shard faster, and celebrity accounts (millions of views) create massive traffic hotspots on a single database shard. `PhotoID` sharding spreads photos evenly across all database partitions.",
      C: "Option C is incorrect. Integer primary keys (`INT` / `BIGINT`) occupy identical 4 or 8 byte allocations.",
      D: "Option D is incorrect. Hardware load balancers operate at the network layer, independent of database sharding keys."
    }
  },
  {
    id: 4,
    topic: "64-Bit PhotoID & Epoch Time Sorting",
    question: "Instagram embeds Epoch Time (31 bits) and an Auto-Increment Sequence (9 bits) into its 64-bit `PhotoID`. What is the key architectural advantage of this design?",
    options: {
      A: "It compresses the raw 200 KB image file directly into the primary key.",
      B: "Primary key index scans on `PhotoID` automatically return photos in chronological order without needing secondary index lookups or cross-shard sorting.",
      C: "It eliminates the need for Amazon S3 object storage.",
      D: "It encrypts user locations for privacy compliance."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. `PhotoID` is a 64-bit primary key identifier, not image file compression.",
      B: "Option B is correct! By placing epoch timestamp bits at the higher-order positions of the 64-bit `PhotoID`, newer photos naturally receive larger `PhotoID` values. Sorting by `PhotoID DESC` sorts chronologically across database shards instantly without secondary index maintenance.",
      C: "Option C is incorrect. Object storage (S3) is still required to store raw image media files.",
      D: "Option D is incorrect. Encryption is handled at the database field level or KMS."
    }
  },
  {
    id: 5,
    topic: "Key Generation Service (KGS) Sequences",
    question: "To eliminate single points of failure without distributed lock contention, two Key Generation Service (KGS) database instances run with sequence offsets: Server 1 uses `increment=2, offset=1` (odd IDs) and Server 2 uses `increment=2, offset=2` (even IDs). What is guaranteed by this setup?",
    options: {
      A: "Both servers generate identical IDs simultaneously for backup verification.",
      B: "Both servers generate 100% unique primary keys concurrently with zero collisions, even if one server dies or operates out of sync.",
      C: "It forces all photo uploads to be processed in strict sequential order globally.",
      D: "It automatically backs up image files to secondary cloud regions."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Generating identical IDs would cause primary key collision failures.",
      B: "Option B is correct! Interleaved sequence offsets (Odd: 1, 3, 5... Even: 2, 4, 6...) guarantee that Server 1 and Server 2 can never generate the same ID. This provides fault-tolerant key generation with zero inter-server coordination overhead.",
      C: "Option C is incorrect. Strict global ordering is not required; key uniqueness and speed are the primary goals.",
      D: "Option D is incorrect. KGS manages primary key IDs, not binary image replication."
    }
  },
  {
    id: 6,
    topic: "Pre-Generated News Feed Architecture",
    question: "Why does Instagram pre-generate personalized News Feeds asynchronously in a `UserNewsFeed` cache table instead of querying followed users' photos on demand during client requests?",
    options: {
      A: "Because relational SQL queries cannot execute `WHERE` clauses on user tables.",
      B: "On-demand querying requires multi-table joins, recency sorting, and ranking across dozens of database shards for every request, violating the sub-200ms feed latency target.",
      C: "Because client mobile apps reject JSON responses larger than 1 KB.",
      D: "Because background pre-generation reduces Redis RAM usage by 90%."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. SQL databases execute `WHERE` clauses natively.",
      B: "Option B is correct! On-demand feed generation requires fetching the follow list, querying recent photos across multiple database shards, and running a ranking algorithm—taking 500ms+ under heavy load. Pre-generating feeds into `UserNewsFeed` memory tables allows feed reads to complete in under 50ms.",
      C: "Option C is incorrect. Mobile apps routinely consume multi-megabyte JSON payloads.",
      D: "Option D is incorrect. Pre-generating feeds utilizes in-memory caches to favor speed over RAM conservation."
    }
  },
  {
    id: 7,
    topic: "Hybrid Feed Delivery & Celebrity Fan-Out",
    question: "When a celebrity user with 50 million followers posts a new photo, pushing the photo ID to 50 million followers' pre-generated feed caches causes a massive 'Fan-Out' bottleneck. How does a Hybrid Feed Delivery model solve this?",
    options: {
      A: "By deleting the celebrity's account to protect database health.",
      B: "By using Push delivery for standard accounts (<1,000 followers) and switching celebrity accounts (>100,000 followers) to a Pull model where followers fetch the post on-demand.",
      C: "By storing celebrity photos on DVD optical discs.",
      D: "By disabling news feed generation for all users during peak hours."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Celebrity accounts drive core engagement and business value.",
      B: "Option B is correct! Pushing a new post to 50M feed caches creates a burst of 50M cache write operations (Fan-out on write). The Hybrid model uses Push for normal users, but for celebrity accounts, followers fetch (Pull) the celebrity's latest post on-demand when opening their feed, eliminating 50M write operations.",
      C: "Option C is incorrect. Optical media is obsolete for cloud feed architectures.",
      D: "Option D is incorrect. Disabling feed generation violates service availability."
    }
  },
  {
    topic: "Capacity Sizing for Metadata Storage",
    id: 8,
    question: "Instagram stores 500M users (68B/row), 2M daily photos (284B/row), and 250B follow edges (8B/row). What is the total estimated metadata database storage required over 10 years?",
    options: {
      A: "32 GB",
      B: "3.7 TB",
      C: "370 TB",
      D: "1,425 TB"
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. 32 GB covers only the User table.",
      B: "Option B is correct! User Table = 32 GB. 10-Year Photo Metadata = 2M/day × 284B × 3650 days ≈ 1.88 TB. UserFollow Table = 250B edges × 8B ≈ 1.82 TB. Total = 32 GB + 1.88 TB + 1.82 TB = 3.73 TB ≈ 3.7 TB.",
      C: "Option C is incorrect. 370 TB overestimates metadata size by a factor of 100.",
      D: "Option D is incorrect. 1,425 TB is the storage required for raw 200 KB image media files in Object Storage (S3), not database metadata."
    }
  },
  {
    id: 9,
    topic: "Multi-Tier Caching & CDN Distribution",
    question: "How does Instagram achieve sub-50ms photo rendering for globally distributed users while maintaining low database CPU utilization?",
    options: {
      A: "By storing all 1,425 TB of raw image files directly inside main database RAM.",
      B: "By distributing raw image files across edge CDNs geographically close to users and caching top 20% hot metadata rows in Redis under an LRU eviction policy.",
      C: "By requiring users to download photos via BitTorrent peer-to-peer networks.",
      D: "By disabling image compression on mobile clients."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Storing 1,425 TB of media in RAM is cost-prohibitive and impractical.",
      B: "Option B is correct! Edge CDNs cache static photo files close to users geographically, delivering sub-50ms image loads. Redis caches the top 20% hot metadata rows (80/20 Pareto rule), offloading 80% of read traffic from backend database clusters.",
      C: "Option C is incorrect. P2P networks introduce unpredictable latency and poor mobile browser support.",
      D: "Option D is incorrect. Image compression reduces bandwidth requirements; disabling it slows rendering."
    }
  }
];

let userAnswers = {};

function renderQuiz() {
  const container = document.getElementById('quiz-container');
  if (!container) return;

  container.innerHTML = quizQuestions.map((q, idx) => {
    return `
      <div class="quiz-card" id="quiz-card-${q.id}">
        <div class="quiz-card-header">
          <span class="quiz-topic">${q.topic}</span>
          <span class="quiz-qnum">Question ${idx + 1} of ${quizQuestions.length}</span>
        </div>
        <div class="quiz-question">${q.question}</div>
        <div class="quiz-options">
          ${Object.keys(q.options).map(optKey => `
            <button class="quiz-opt" id="opt-${q.id}-${optKey}" onclick="handleOptionClick(${q.id}, '${optKey}')">
              <span class="opt-letter">${optKey}</span>
              <span class="opt-text">${q.options[optKey]}</span>
            </button>
          `).join('')}
        </div>
        <div class="quiz-explanation" id="exp-${q.id}"></div>
      </div>
    `;
  }).join('');

  updateDashboard();
}

function handleOptionClick(questionId, selectedOpt) {
  const q = quizQuestions.find(item => item.id === questionId);
  if (!q) return;

  userAnswers[questionId] = selectedOpt;
  const isRight = (selectedOpt === q.correct);

  Object.keys(q.options).forEach(optKey => {
    const btn = document.getElementById(`opt-${questionId}-${optKey}`);
    if (!btn) return;

    btn.classList.remove('opt-correct', 'opt-incorrect');

    if (optKey === q.correct) {
      btn.classList.add('opt-correct');
    } else if (optKey === selectedOpt && !isRight) {
      btn.classList.add('opt-incorrect');
    }
  });

  const expDiv = document.getElementById(`exp-${questionId}`);
  if (expDiv) {
    expDiv.style.display = 'block';
    expDiv.className = `quiz-explanation ${isRight ? 'exp-right' : 'exp-wrong'}`;

    if (isRight) {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-check-circle" style="color: #16a34a;"></i> Correct Answer!
        </div>
        <div class="exp-body">
          ${q.explanations[selectedOpt]}
        </div>
      `;
    } else {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-times-circle" style="color: #dc2626;"></i> Incorrect Choice (Selected: ${selectedOpt})
        </div>
        <div class="exp-body">
          <p style="margin-bottom: 8px;"><strong>Why Option ${selectedOpt} is incorrect:</strong> ${q.explanations[selectedOpt]}</p>
          <p style="margin-top: 8px; margin-bottom: 0;"><strong>Correct Answer (${q.correct}):</strong> ${q.explanations[q.correct]}</p>
        </div>
      `;
    }
  }

  updateDashboard();
}

function updateDashboard() {
  const answeredCount = Object.keys(userAnswers).length;
  let correctCount = 0;

  Object.keys(userAnswers).forEach(qId => {
    const q = quizQuestions.find(item => item.id == qId);
    if (q && userAnswers[qId] === q.correct) {
      correctCount++;
    }
  });

  const total = quizQuestions.length;
  const accuracy = answeredCount > 0 ? Math.round((correctCount / answeredCount) * 100) : 0;
  const progressPercent = Math.round((answeredCount / total) * 100);

  document.getElementById('quiz-score').innerText = `${correctCount} / ${total}`;
  document.getElementById('quiz-progress').innerText = `${answeredCount} / ${total} Answered`;
  document.getElementById('quiz-accuracy').innerText = `${accuracy}%`;
  document.getElementById('quiz-progress-bar').style.width = `${progressPercent}%`;
}

function resetQuiz() {
  userAnswers = {};
  renderQuiz();
}

document.addEventListener('DOMContentLoaded', renderQuiz);
</script>
