---
layout: default
title: Quiz - Designing YouTube (or Netflix)
permalink: /System-Design/4-system-design-problems/4.9-quiz-designing-youtube-or-netflix/
prev_title: "Designing YouTube (or Netflix)"
prev_url: "/System-Design/4-system-design-problems/4.9-designing-youtube-or-netflix/"
next_title: "Designing Typeahead Suggestion"
next_url: "/System-Design/4-system-design-problems/4.10-designing-typeahead-suggestion/"
---

# Quiz: Designing YouTube (or Netflix)

Test your architectural knowledge of YouTube and Netflix system design with **9 scenario-driven interview questions** covering 200:1 read-heavy asymmetry, Object Storage vs relational metadata, Bigtable thumbnail engines, VideoID database sharding, HLS/MPEG-DASH adaptive bitrate transcoding, inline video deduplication, consistent hashing with HTTP 302 redirections, 80/20 Pareto caching, and asynchronous AI pipeline execution policies.

---

<!-- Quiz Dashboard -->
<div class="quiz-dashboard">
  <div class="dash-stat">
    <span class="dash-label">Score</span>
    <span class="dash-value" id="quiz-score">0 / 9</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Progress</span>
    <span class="dash-value" id="quiz-progress">0 / 9 Answered</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Accuracy</span>
    <span class="dash-value" id="quiz-accuracy">0%</span>
  </div>
  <button class="quiz-reset-btn" onclick="resetQuiz()">
    <i class="fas fa-redo-alt"></i> Reset Quiz
  </button>
</div>

<div class="quiz-progress-bar-container">
  <div class="quiz-progress-bar" id="quiz-progress-bar" style="width: 0%;"></div>
</div>

<div id="quiz-container"></div>

<style>
/* Quiz Styling - Compatible with Light & Dark Themes */
.quiz-dashboard {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-secondary, #f8fafc);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 16px 24px;
  margin-bottom: 16px;
}

html.dark-theme .quiz-dashboard {
  background: #1e293b;
  border-color: #334155;
}

.dash-stat {
  display: flex;
  flex-direction: column;
}

.dash-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted, #64748b);
  font-weight: 600;
}

.dash-value {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-color, #0f172a);
}

html.dark-theme .dash-value {
  color: #f8fafc;
}

.quiz-reset-btn {
  background: #3b82f6;
  color: #ffffff;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, transform 0.1s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.quiz-reset-btn:hover {
  background: #2563eb;
  transform: translateY(-1px);
}

.quiz-progress-bar-container {
  width: 100%;
  height: 8px;
  background: var(--border-color, #e2e8f0);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 32px;
}

html.dark-theme .quiz-progress-bar-container {
  background: #334155;
}

.quiz-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #3b82f6, #10b981);
  transition: width 0.3s ease;
}

.quiz-card {
  background: var(--bg-color, #ffffff);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 28px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  transition: border-color 0.2s ease;
}

html.dark-theme .quiz-card {
  background: #0f172a;
  border-color: #1e293b;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

.quiz-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.quiz-topic {
  font-size: 0.82rem;
  font-weight: 600;
  color: #2563eb;
  background: rgba(37, 99, 235, 0.1);
  padding: 4px 10px;
  border-radius: 20px;
}

html.dark-theme .quiz-topic {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.15);
}

.quiz-qnum {
  font-size: 0.85rem;
  color: var(--text-muted, #64748b);
  font-weight: 500;
}

.quiz-question {
  font-size: 1.1rem;
  font-weight: 600;
  margin-top: 6px;
  margin-bottom: 20px;
  color: var(--text-color, #0f172a);
  line-height: 1.5;
}

html.dark-theme .quiz-question {
  color: #f1f5f9;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.quiz-opt {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  width: 100%;
  text-align: left;
  background: var(--bg-secondary, #f8fafc);
  border: 1.5px solid var(--border-color, #e2e8f0);
  border-radius: 10px;
  padding: 14px 16px;
  font-size: 0.95rem;
  color: var(--text-color, #334155);
  cursor: pointer;
  transition: all 0.2s ease;
}

html.dark-theme .quiz-opt {
  background: #1e293b;
  border-color: #334155;
  color: #cbd5e1;
}

.quiz-opt:hover:not(:disabled) {
  border-color: #3b82f6;
  background: rgba(59, 130, 246, 0.05);
}

html.dark-theme .quiz-opt:hover:not(:disabled) {
  border-color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
}

.opt-letter {
  font-weight: 700;
  background: var(--border-color, #cbd5e1);
  color: #0f172a;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  flex-shrink: 0;
  font-size: 0.85rem;
}

html.dark-theme .opt-letter {
  background: #475569;
  color: #f8fafc;
}

.opt-text {
  line-height: 1.45;
  flex-grow: 1;
}

/* Correct Option State */
.quiz-opt.opt-correct {
  background: #dcfce7 !important;
  border-color: #16a34a !important;
  color: #14532d !important;
}

.quiz-opt.opt-correct .opt-letter {
  background: #16a34a !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-correct {
  background: rgba(22, 163, 74, 0.2) !important;
  border-color: #22c55e !important;
  color: #bbf7d0 !important;
}

/* Incorrect Option State */
.quiz-opt.opt-incorrect {
  background: #fef2f2 !important;
  border-color: #dc2626 !important;
  color: #7f1d1d !important;
}

.quiz-opt.opt-incorrect .opt-letter {
  background: #dc2626 !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-incorrect {
  background: rgba(220, 38, 38, 0.2) !important;
  border-color: #ef4444 !important;
  color: #fecaca !important;
}

/* Explanation Box */
.quiz-explanation {
  margin-top: 18px;
  padding: 16px;
  border-radius: 10px;
  font-size: 0.93rem;
  line-height: 1.55;
  display: none;
}

.quiz-explanation.exp-right {
  background: #f0fdf4;
  border: 1px solid #86efac;
  color: #166534;
}

html.dark-theme .quiz-explanation.exp-right {
  background: rgba(22, 163, 74, 0.15);
  border-color: #22c55e;
  color: #bbf7d0;
}

.quiz-explanation.exp-wrong {
  background: #fef2f2;
  border: 1px solid #fca5a5;
  color: #991b1b;
}

html.dark-theme .quiz-explanation.exp-wrong {
  background: rgba(220, 38, 38, 0.15);
  border-color: #ef4444;
  color: #fecaca;
}

.exp-header {
  font-weight: 700;
  font-size: 1rem;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.exp-body {
  margin-top: 6px;
}
</style>

<script>
const quizQuestions = [
  {
    id: 1,
    topic: "Read-Heavy Asymmetry & Storage Decoupling",
    question: "YouTube ingests 25 GB of new video content per second (2.16 PB/day) and serves outgoing video streams at 1 TB/second (a 200:1 read-to-write ratio). Why does the architecture strictly decouple unstructured video binary files (Amazon S3) from structured application metadata (MySQL)?",
    options: {
      A: "Because MySQL databases cannot store primary key strings longer than 64 bytes.",
      B: "Because storing multi-gigabyte binary files directly inside relational database tables would destroy B-Tree index performance, cause massive table bloat, and prevent efficient horizontal scaling, whereas Object Storage is specifically optimized for cheap, immutable HTTP blob delivery.",
      C: "Because Object Storage automatically executes SQL queries faster than MySQL replicas.",
      D: "Because raw video files must be converted into JSON strings before being written to disk."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. MySQL supports large key sizes, but binary BLOBs in tables ruin caching and index traversal.",
      B: "Option B is correct! Relational databases are built for low-latency structured indexing and row transactions. Storing massive binary objects in database tables causes severe buffer pool pollution, bloats index pages, and degrades query throughput. Object storage (S3/GCS) provides 99.999999999% durability, flat HTTP access, and cost-effective petabyte-scale storage.",
      C: "Option C is incorrect. Object storage provides key-value blob operations, not relational SQL query processing.",
      D: "Option D is incorrect. Binary video files are stored as raw chunked streams (e.g. MP4/TS segments), not JSON strings."
    }
  },
  {
    id: 2,
    topic: "Asynchronous Upload Pipeline & Resumable API Design",
    question: "When a user uploads a 4K video file via `POST /v1/videos/upload`, the API server returns an `HTTP 202 Accepted` response within milliseconds instead of waiting for video processing to finish. Why is this asynchronous pattern necessary, and how are network disruptions handled during upload?",
    options: {
      A: "HTTP 202 forces the user's browser to transcode the video locally using WebAssembly before uploading.",
      B: "Video transcoding into multiple codecs and resolutions takes minutes or hours; returning HTTP 202 immediately releases web server connection worker threads, while chunked multipart protocols (like TUS) allow clients to resume uploads from the last byte offset if the connection drops.",
      C: "HTTP 202 acts as a security firewall that deletes duplicate video files before they touch the load balancer.",
      D: "Returning HTTP 202 compresses incoming raw video files by 99% before they enter the processing queue."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Transcoding runs on server-side worker clusters, not client WebAssembly.",
      B: "Option B is correct! Transcoding is a heavy compute-bound background task. Holding open HTTP connections during transcoding would quickly exhaust web server thread pools and cause timeouts. HTTP 202 acknowledges receipt and queues background processing, while chunked multipart upload protocols ensure users never lose upload progress if their internet connection drops.",
      C: "Option C is incorrect. HTTP 202 is a standard HTTP status code indicating request acceptance for asynchronous processing, not a firewall or deduplicator.",
      D: "Option D is incorrect. Status codes carry HTTP metadata headers, not compression algorithms."
    }
  },
  {
    id: 3,
    topic: "Small-File Storage Bottleneck & Key-Value Engines",
    question: "YouTube hosts billions of video thumbnail images (~5 KB each). Browsing a single homepage generates 30 to 50 thumbnail read requests simultaneously. Why does storing billions of thumbnails as individual files on a standard Linux disk filesystem (ext4) fail, and why is a wide-column store like Bigtable preferred?",
    options: {
      A: "Linux filesystems cannot store files smaller than 1 MB without corrupting disk sector headers.",
      B: "Storing billions of tiny files on standard disk filesystems causes severe disk head seek latency and inode exhaustion, whereas Bigtable packs small thumbnail binary blobs into consolidated, continuous SSTable blocks on disk for high-throughput sequential reads.",
      C: "Bigtable automatically translates 5 KB images into text documents that require zero disk storage.",
      D: "Standard filesystems restrict image resolutions to a maximum of 360p."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Linux filesystems support arbitrary file sizes, but small files incur high metadata block overhead.",
      B: "Option B is correct! Operating systems struggle with billions of tiny files because reading each file requires a separate disk head seek to locate directory inodes. This produces extreme IOPS bottlenecks. Bigtable merges small binary key-value pairs into sequential SSTable file blocks on disk, allowing fast sequential batch reads and efficient RAM caching.",
      C: "Option C is incorrect. Bigtable stores raw binary byte arrays (BLOBs), not compressed text abstractions.",
      D: "Option D is incorrect. Filesystem storage is format-agnostic and completely independent of image resolution constraints."
    }
  },
  {
    id: 4,
    topic: "Database Sharding & Hot Uploader Mitigation",
    question: "When sharding the relational `Video` metadata table across database instances, why is `VideoID` sharding (`Hash(VideoID) % Total_Shards`) preferred over `UserID` sharding, despite user-level queries requiring scatter-gather calls across shards?",
    options: {
      A: "Sharding by UserID prevents databases from generating primary key indexes.",
      B: "Sharding by UserID creates the 'Celebrity / Hot Uploader' problem where famous content creators concentrate massive read/write traffic and uneven storage onto a single database shard, whereas VideoID sharding distributes data uniformly across all nodes.",
      C: "Sharding by VideoID forces all video comments to be stored inside browser memory.",
      D: "Sharding by UserID requires converting MySQL databases into NoSQL document stores."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Primary key indexes function normally regardless of shard key choice.",
      B: "Option B is correct! If metadata is sharded by UserID, all videos and traffic for a major creator (e.g. MrBeast) land on one shard, creating an extreme bottleneck and uneven disk utilization. Sharding by VideoID ensures uniform row distribution across all shards. The resulting scatter-gather queries for channel listings are easily mitigated using secondary search indexes (Elasticsearch) and hot row caching.",
      C: "Option C is incorrect. Comment data is stored in partitioned database tables, not browser memory.",
      D: "Option D is incorrect. Sharding is a database architecture pattern applicable to both SQL and NoSQL engines."
    }
  },
  {
    id: 5,
    topic: "Video Transcoding & Dynamic Bitrate Adaptation",
    question: "How does the video encoding pipeline support uninterrupted playback when a viewer's mobile device transitions from a high-speed 5G network to a congested 3G connection while streaming?",
    options: {
      A: "The server pauses streaming for 30 seconds to re-encode the entire movie into a lower quality file on the fly.",
      B: "The encoder splits videos into 2-10 second segment chunks encoded at multiple resolutions (1080p, 720p, 360p); the client media player uses HLS/MPEG-DASH manifest files to dynamically request smaller resolution chunks without stalling playback.",
      C: "The client device drops audio streams so the video payload stays small enough for 3G.",
      D: "The CDN automatically doubles the client's network bandwidth using HTTP/3 multiplexing."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Real-time re-encoding of entire files on the fly for millions of users would cause catastrophic server compute overload.",
      B: "Option B is correct! Adaptive Bitrate Streaming (HLS/MPEG-DASH) pre-encodes video into short 2-10 second segment chunks across multiple discrete resolution levels (e.g. 1080p, 720p, 480p, 360p). As client network conditions change, the client video player inspects its buffer health and switches quality levels seamlessly on the next chunk boundary without buffering.",
      C: "Option C is incorrect. Adaptive streaming adjusts video resolution/bitrate while preserving synchronized audio streams.",
      D: "Option D is incorrect. CDNs deliver data over available network bandwidth; they cannot physically alter cellular carrier speeds."
    }
  },
  {
    id: 6,
    topic: "Media Deduplication Mechanics",
    question: "Why is video deduplication executed inline during the initial upload stream rather than solely as a background post-processing script?",
    options: {
      A: "Because background post-processing scripts are incapable of reading video files stored in Amazon S3.",
      B: "Inline deduplication extracts perceptual fingerprints (e.g. Block Matching) during the upload stream; if a duplicate match is found in the global index, the upload can be halted immediately, saving petabytes of storage, transcoding CPU cycles, cache memory, and network bandwidth.",
      C: "Inline deduplication forces uploaders to pay a licensing fee before uploading media files.",
      D: "Post-processing deduplication automatically deletes the original video uploader's channel account."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Background jobs read object storage without issue; post-processing deduplication is widely used for batch audits.",
      B: "Option B is correct! Running deduplication inline catches duplicate uploads as early as possible. Fingerprinting the incoming stream allows the system to abort redundant uploads or point metadata directly to existing encoded blobs, avoiding expensive transcoding computations, duplicate object storage costs, and wasted edge bandwidth.",
      C: "Option C is incorrect. Deduplication is an engineering optimization for resource management, not a billing mechanism.",
      D: "Option D is incorrect. Deduplication links asset pointers; it does not terminate user accounts."
    }
  },
  {
    id: 7,
    topic: "Load Balancing & Dynamic Redirection",
    question: "If a breaking news video suddenly goes viral, consistent hashing maps that VideoID to a single cache hostname, causing extreme traffic spikes on that physical server. How does the architecture prevent this cache server from crashing under load?",
    options: {
      A: "By rebooting the cache server every 60 seconds to clear temporary memory logs.",
      B: "By issuing dynamic HTTP 302/307 redirections from overloaded cache servers to direct client players to underutilized mirror cache nodes within the same POP, alongside multi-tier CDN caching.",
      C: "By converting all video requests for that viral video into static PNG images.",
      D: "By blocking all incoming read requests until the viral video drops out of top search trends."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Frequently rebooting busy servers degrades availability and worsens cache miss rates.",
      B: "Option B is correct! When a cache node experiences CPU or bandwidth saturation (>80%), it returns HTTP 302 Found or 307 Temporary Redirect status codes to incoming clients, forwarding them to less busy replica cache servers in the same data center location. Simultaneously, CDN edge nodes absorb the vast majority of viral read traffic.",
      C: "Option C is incorrect. Video streaming players require valid video chunk streams, not static image files.",
      D: "Option D is incorrect. Blocking viral video requests violates the fundamental high availability non-functional requirement."
    }
  },
  {
    id: 8,
    topic: "In-Memory Caching & Eviction Mechanics",
    question: "With 4 Billion video views per day generating petabytes of read traffic, how does applying the 80/20 Pareto Principle allow YouTube to achieve high cache hit ratios while maintaining cost-effective memory usage?",
    options: {
      A: "By caching 80% of all uploaded videos permanently and deleting 20% of user accounts every month.",
      B: "By caching the top 20% of popular videos and active metadata that generate 80% of total daily read traffic using an LRU (Least Recently Used) eviction policy, allowing stale content to drop out naturally while active media stays in RAM.",
      C: "By restricting caching strictly to text comments while forcing all video streams to fetch directly from primary database disks.",
      D: "By requiring users to manually clear server cache memory after watching 5 videos."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. User accounts are persistent entity records; they are never deleted for cache management.",
      B: "Option B is correct! Video access follows power-law distributions: 20% of videos account for 80% of total views. Caching 20% of daily read volume in an in-memory tier (Redis/Memcached) backed by an LRU eviction policy keeps hot content in RAM while letting aging, unviewed content fall back to cost-effective origin storage.",
      C: "Option C is incorrect. Video media streaming dominates bandwidth usage; caching media chunks is far more critical than caching text alone.",
      D: "Option D is incorrect. Server-side caching tiers are managed automatically by backend infrastructure, transparent to end users."
    }
  },
  {
    id: 9,
    topic: "Machine Learning Integration & Execution Policies",
    question: "When integrating AI models for Automated Closed Captioning (Speech-to-Text) and Content Moderation into YouTube's processing pipeline, how do their architectural execution policies differ?",
    options: {
      A: "Speech-to-text runs synchronously on client phones, whereas content moderation runs inside primary database triggers.",
      B: "Speech-to-text operates under a non-blocking policy (the video goes live even if captions are pending), whereas safety moderation often operates under a policy-dependent blocking gatekeeper policy (flagged content is held for human review before publishing).",
      C: "Both AI models must complete execution before the video upload HTTP POST request receives a 200 OK response.",
      D: "Speech-to-text models delete video files if the audio track contains background music."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Heavy ML models run on dedicated GPU worker clusters in the backend pipeline, not client phones or SQL database triggers.",
      B: "Option B is correct! Closed caption generation is non-critical to video publishing, so it runs asynchronously under a non-blocking policy. If captioning lags, the video publishes anyway and captions are attached later. Conversely, safety moderation acts as a product policy gatekeeper: if a classifier detects severe rule violations or copyright infringement, publishing is blocked until human review completes.",
      C: "Option C is incorrect. Running GPU-heavy AI models synchronously inside the upload HTTP request would cause server timeouts and block upload completion.",
      D: "Option D is incorrect. Speech-to-text transcribes spoken language into text captions; it does not arbitrarily delete videos containing background music."
    }
  }
];

let userAnswers = {};

function renderQuiz() {
  const container = document.getElementById('quiz-container');
  if (!container) return;

  container.innerHTML = quizQuestions.map((q, idx) => {
    return `
      <div class="quiz-card" id="quiz-card-${q.id}">
        <div class="quiz-card-header">
          <span class="quiz-topic">${q.topic}</span>
          <span class="quiz-qnum">Question ${idx + 1} of ${quizQuestions.length}</span>
        </div>
        <div class="quiz-question">${q.question}</div>
        <div class="quiz-options">
          ${Object.keys(q.options).map(optKey => `
            <button class="quiz-opt" id="opt-${q.id}-${optKey}" onclick="handleOptionClick(${q.id}, '${optKey}')">
              <span class="opt-letter">${optKey}</span>
              <span class="opt-text">${q.options[optKey]}</span>
            </button>
          `).join('')}
        </div>
        <div class="quiz-explanation" id="exp-${q.id}"></div>
      </div>
    `;
  }).join('');

  updateDashboard();
}

function handleOptionClick(questionId, selectedOpt) {
  const q = quizQuestions.find(item => item.id === questionId);
  if (!q) return;

  userAnswers[questionId] = selectedOpt;
  const isRight = (selectedOpt === q.correct);

  Object.keys(q.options).forEach(optKey => {
    const btn = document.getElementById(`opt-${questionId}-${optKey}`);
    if (!btn) return;

    btn.classList.remove('opt-correct', 'opt-incorrect');

    if (optKey === q.correct) {
      btn.classList.add('opt-correct');
    } else if (optKey === selectedOpt && !isRight) {
      btn.classList.add('opt-incorrect');
    }
  });

  const expDiv = document.getElementById(`exp-${questionId}`);
  if (expDiv) {
    expDiv.style.display = 'block';
    expDiv.className = `quiz-explanation ${isRight ? 'exp-right' : 'exp-wrong'}`;

    if (isRight) {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-check-circle" style="color: #16a34a;"></i> Correct Answer!
        </div>
        <div class="exp-body">
          ${q.explanations[selectedOpt]}
        </div>
      `;
    } else {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-times-circle" style="color: #dc2626;"></i> Incorrect Choice (Selected: ${selectedOpt})
        </div>
        <div class="exp-body">
          <p style="margin-bottom: 8px;"><strong>Why Option ${selectedOpt} is incorrect:</strong> ${q.explanations[selectedOpt]}</p>
          <p style="margin-top: 8px; margin-bottom: 0;"><strong>Correct Answer (${q.correct}):</strong> ${q.explanations[q.correct]}</p>
        </div>
      `;
    }
  }

  updateDashboard();
}

function updateDashboard() {
  const answeredCount = Object.keys(userAnswers).length;
  let correctCount = 0;

  Object.keys(userAnswers).forEach(qId => {
    const q = quizQuestions.find(item => item.id == qId);
    if (q && userAnswers[qId] === q.correct) {
      correctCount++;
    }
  });

  const total = quizQuestions.length;
  const accuracy = answeredCount > 0 ? Math.round((correctCount / answeredCount) * 100) : 0;
  const progressPercent = Math.round((answeredCount / total) * 100);

  document.getElementById('quiz-score').innerText = `${correctCount} / ${total}`;
  document.getElementById('quiz-progress').innerText = `${answeredCount} / ${total} Answered`;
  document.getElementById('quiz-accuracy').innerText = `${accuracy}%`;
  document.getElementById('quiz-progress-bar').style.width = `${progressPercent}%`;
}

function resetQuiz() {
  userAnswers = {};
  renderQuiz();
}

document.addEventListener('DOMContentLoaded', renderQuiz);
</script>
