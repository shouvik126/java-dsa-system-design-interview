---
layout: default
title: Quiz - Designing Youtube or Netflix
permalink: /System-Design/4-system-design-problems/4.9-quiz-designing-youtube-or-netflix/
prev_title: "Designing Youtube or Netflix"
prev_url: "/System-Design/4-system-design-problems/4.9-designing-youtube-or-netflix/"
---

# Quiz: Designing YouTube or Netflix

Test your architectural knowledge of YouTube and Netflix system design with **9 scenario-driven interview questions** covering 200:1 read/write ratios, asynchronous HTTP 202 processing queues, Object Storage vs HDFS trade-offs, Bigtable thumbnail block storage, VideoID database sharding, inline deduplication, dynamic HTTP redirections, 80-20 LRU caching, and pipeline AI integration.

---

<!-- Quiz Dashboard -->
<div class="quiz-dashboard">
  <div class="dash-stat">
    <span class="dash-label">Score</span>
    <span class="dash-value" id="quiz-score">0 / 9</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Progress</span>
    <span class="dash-value" id="quiz-progress">0 / 9 Answered</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Accuracy</span>
    <span class="dash-value" id="quiz-accuracy">0%</span>
  </div>
  <button class="quiz-reset-btn" onclick="resetQuiz()">
    <i class="fas fa-redo-alt"></i> Reset Quiz
  </button>
</div>

<div class="quiz-progress-bar-container">
  <div class="quiz-progress-bar" id="quiz-progress-bar" style="width: 0%;"></div>
</div>

<div id="quiz-container"></div>

<style>
/* Quiz Styling - Compatible with Light & Dark Themes */
.quiz-dashboard {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-secondary, #f8fafc);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 16px 24px;
  margin-bottom: 16px;
}

html.dark-theme .quiz-dashboard {
  background: #1e293b;
  border-color: #334155;
}

.dash-stat {
  display: flex;
  flex-direction: column;
}

.dash-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted, #64748b);
  font-weight: 600;
}

.dash-value {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-color, #0f172a);
}

html.dark-theme .dash-value {
  color: #f8fafc;
}

.quiz-reset-btn {
  background: #3b82f6;
  color: #ffffff;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, transform 0.1s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.quiz-reset-btn:hover {
  background: #2563eb;
  transform: translateY(-1px);
}

.quiz-progress-bar-container {
  width: 100%;
  height: 8px;
  background: var(--border-color, #e2e8f0);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 32px;
}

html.dark-theme .quiz-progress-bar-container {
  background: #334155;
}

.quiz-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #3b82f6, #10b981);
  transition: width 0.3s ease;
}

.quiz-card {
  background: var(--bg-color, #ffffff);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 28px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  transition: border-color 0.2s ease;
}

html.dark-theme .quiz-card {
  background: #0f172a;
  border-color: #1e293b;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

.quiz-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.quiz-topic {
  font-size: 0.82rem;
  font-weight: 600;
  color: #2563eb;
  background: rgba(37, 99, 235, 0.1);
  padding: 4px 10px;
  border-radius: 20px;
}

html.dark-theme .quiz-topic {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.15);
}

.quiz-qnum {
  font-size: 0.85rem;
  color: var(--text-muted, #64748b);
  font-weight: 500;
}

.quiz-question {
  font-size: 1.1rem;
  font-weight: 600;
  margin-top: 6px;
  margin-bottom: 20px;
  color: var(--text-color, #0f172a);
  line-height: 1.5;
}

html.dark-theme .quiz-question {
  color: #f1f5f9;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.quiz-opt {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  width: 100%;
  text-align: left;
  background: var(--bg-secondary, #f8fafc);
  border: 1.5px solid var(--border-color, #e2e8f0);
  border-radius: 10px;
  padding: 14px 16px;
  font-size: 0.95rem;
  color: var(--text-color, #334155);
  cursor: pointer;
  transition: all 0.2s ease;
}

html.dark-theme .quiz-opt {
  background: #1e293b;
  border-color: #334155;
  color: #cbd5e1;
}

.quiz-opt:hover:not(:disabled) {
  border-color: #3b82f6;
  background: rgba(59, 130, 246, 0.05);
}

html.dark-theme .quiz-opt:hover:not(:disabled) {
  border-color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
}

.opt-letter {
  font-weight: 700;
  background: var(--border-color, #cbd5e1);
  color: #0f172a;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  flex-shrink: 0;
  font-size: 0.85rem;
}

html.dark-theme .opt-letter {
  background: #475569;
  color: #f8fafc;
}

.opt-text {
  line-height: 1.45;
  flex-grow: 1;
}

/* Correct Option State */
.quiz-opt.opt-correct {
  background: #dcfce7 !important;
  border-color: #16a34a !important;
  color: #14532d !important;
}

.quiz-opt.opt-correct .opt-letter {
  background: #16a34a !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-correct {
  background: rgba(22, 163, 74, 0.2) !important;
  border-color: #22c55e !important;
  color: #bbf7d0 !important;
}

/* Incorrect Option State */
.quiz-opt.opt-incorrect {
  background: #fef2f2 !important;
  border-color: #dc2626 !important;
  color: #7f1d1d !important;
}

.quiz-opt.opt-incorrect .opt-letter {
  background: #dc2626 !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-incorrect {
  background: rgba(220, 38, 38, 0.2) !important;
  border-color: #ef4444 !important;
  color: #fecaca !important;
}

/* Explanation Box */
.quiz-explanation {
  margin-top: 18px;
  padding: 16px;
  border-radius: 10px;
  font-size: 0.93rem;
  line-height: 1.55;
  display: none;
}

.quiz-explanation.exp-right {
  background: #f0fdf4;
  border: 1px solid #86efac;
  color: #166534;
}

html.dark-theme .quiz-explanation.exp-right {
  background: rgba(22, 163, 74, 0.15);
  border-color: #22c55e;
  color: #bbf7d0;
}

.quiz-explanation.exp-wrong {
  background: #fef2f2;
  border: 1px solid #fca5a5;
  color: #991b1b;
}

html.dark-theme .quiz-explanation.exp-wrong {
  background: rgba(220, 38, 38, 0.15);
  border-color: #ef4444;
  color: #fecaca;
}

.exp-header {
  font-weight: 700;
  font-size: 1rem;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.exp-body {
  margin-top: 6px;
}
</style>

<script>
const quizQuestions = [
  {
    id: 1,
    topic: "Read/Write Ratio & Egress Bandwidth Scaling",
    question: "YouTube processes roughly 46,000 video views per second against 230 video uploads per second (a 200:1 read-to-write ratio), resulting in 1 TB/sec of egress network bandwidth. What architectural decision is directly mandated by this 1 TB/sec outgoing bandwidth requirement?",
    options: {
      A: "Offloading video playback streaming to a Content Delivery Network (CDN) to serve popular video content from edge locations close to viewers.",
      B: "Replacing MySQL metadata database servers with memory-mapped SSD hardware.",
      C: "Enforcing strict synchronous REST API upload calls on clients.",
      D: "Storing all video files directly inside PostgreSQL database tables as BYTEA fields."
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! Outgoing traffic of 1 TB/sec cannot be served efficiently by a single central data center without incurring massive transit latency and bandwidth bottlenecks. CDNs replicate video content across geographically distributed edge nodes, serving playback from edge cache memory/SSD.",
      B: "Option B is incorrect. Memory-mapped SSDs optimize metadata database query performance, not network egress bandwidth for multi-megabyte video streams.",
      C: "Option C is incorrect. Synchronous API calls handle upload initiation, not network egress delivery.",
      D: "Option D is incorrect. Storing binary video files in database tables degrades database performance severely and fails to scale egress bandwidth."
    }
  },
  {
    id: 2,
    topic: "Asynchronous Upload Processing & HTTP 202",
    question: "When a user uploads a video via `uploadVideo(...)`, why does the API return HTTP 202 (Accepted) immediately rather than waiting for encoding and thumbnail generation to finish?",
    options: {
      A: "Because HTTP 200 OK responses are restricted to text-only JSON endpoints.",
      B: "Video encoding across multiple codecs and resolutions takes minutes to complete; decoupling upload initiation via a background processing queue prevents client connection timeouts and frees HTTP web workers.",
      C: "Because HTTP 202 automatically compresses video files on the client device.",
      D: "To notify the user's mobile device that the database transaction failed."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. HTTP 200 OK can carry any payload type.",
      B: "Option B is correct! Transcoding raw video into multiple bitrates/resolutions and extracting thumbnails is a computationally expensive background job. Holding a synchronous HTTP request open for minutes causes client timeouts and exhausts server thread pools. Returning HTTP 202 pushes the task into an asynchronous queue.",
      C: "Option C is incorrect. HTTP status codes represent server response status, not client-side video compression algorithms.",
      D: "Option D is incorrect. HTTP 202 indicates successful acceptance of an asynchronous request."
    }
  },
  {
    id: 3,
    topic: "Video Storage Strategy: Object Storage vs. HDFS",
    question: "Why is Cloud Object Storage (e.g., Amazon S3) chosen over Hadoop Distributed File System (HDFS) for hosting streaming video files in a video-on-demand platform?",
    options: {
      A: "HDFS cannot store binary files larger than 10 MB.",
      B: "HDFS is built for sequential batch-processing analytics jobs and favors total throughput over request latency, whereas Object Storage natively serves named blobs over HTTP with built-in replication and seamless CDN integration.",
      C: "Object storage automatically transcode videos into MP4 formats without encoding servers.",
      D: "HDFS requires hardware GPU cards on every storage node."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. HDFS is explicitly designed to handle multi-gigabyte and multi-terabyte files.",
      B: "Option B is correct! Encoded video playback requires low-latency byte-range HTTP streaming to viewers worldwide. Object Storage (like S3) serves HTTP GET requests efficiently, manages replication automatically, and interfaces natively with CDNs. HDFS is optimized for batch MapReduce/Spark analytics scanning huge files sequentially.",
      C: "Option C is incorrect. Object storage acts as a blob store; encoding requires separate worker nodes.",
      D: "Option D is incorrect. HDFS run on commodity CPU hardware without GPU requirements."
    }
  },
  {
    id: 4,
    topic: "Thumbnail Storage & Small File IOPS",
    question: "Assuming 5 small thumbnails (~5 KB each) per video across billions of videos, storing them as individual files on standard disk storage produces severe IOPS bottlenecks. Why is Google Bigtable an effective storage engine for video thumbnails?",
    options: {
      A: "Bigtable stores files exclusively in relational SQL tables using ACID transactions.",
      B: "Bigtable consolidates billions of small file blobs into contiguous sequential blocks on disk, reducing random physical disk head seeks while providing high-throughput read operations.",
      C: "Bigtable automatically converts JPEG thumbnails into WebP images.",
      D: "Bigtable bypasses network hardware by utilizing direct memory access."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Bigtable is a NoSQL wide-column store, not a relational ACID database.",
      B: "Option B is correct! Billion of small 5 KB files stored individually on a traditional file system cause excessive random disk seeks, drastically slowing read throughput. Bigtable packs small key-value records sequentially into larger SSTable disk blocks, enabling high-performance reads for small payloads.",
      C: "Option C is incorrect. Format conversion is handled by the image processing pipeline, not the storage engine.",
      D: "Option D is incorrect. Bigtable is a distributed network storage system."
    }
  },
  {
    id: 5,
    topic: "Metadata Sharding: VideoID vs. UserID",
    question: "When partitioning the MySQL video metadata database, what is the primary downside of sharding by `UserID` (`hash(UserID)`), and why is sharding by `VideoID` (`hash(VideoID)`) preferred?",
    options: {
      A: "Sharding by `UserID` makes it impossible to query videos by title.",
      B: "Sharding by `UserID` creates severe read/write hotspots on popular creators' shards, whereas `VideoID` sharding distributes video metadata uniformly across all database nodes.",
      C: "Sharding by `VideoID` eliminates the need for database replicas.",
      D: "Sharding by `UserID` prevents caching metadata in Redis."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Searching by title requires cross-shard scatter-gather queries under both sharding schemes.",
      B: "Option B is correct! If metadata is sharded by `UserID`, all videos belonging to a viral creator land on a single database node. Traffic spikes for that creator overload that single shard. Sharding by `VideoID` randomly distributes videos across shards, spreading read/write loads evenly. Scatter-gather reads for user channel lists are handled by aggregator servers and front-end caches.",
      C: "Option C is incorrect. Replication is still mandatory for high availability and fault tolerance.",
      D: "Option D is incorrect. Caching operates independently of database sharding keys."
    }
  },
  {
    id: 6,
    topic: "Inline Video Deduplication",
    question: "Why is inline video deduplication (detecting duplicates during video upload) superior to post-processing deduplication (detecting duplicates after upload completes)?",
    options: {
      A: "Inline deduplication stops duplicate uploads early, saving network bandwidth, encoding compute, and storage capacity before resources are consumed.",
      B: "Post-processing deduplication corrupts original video metadata.",
      C: "Inline deduplication encrypts video streams using AES-256.",
      D: "Post-processing deduplication is impossible on cloud infrastructure."
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! Running fingerprint matching algorithms (e.g. Block Matching or Phase Correlation) inline as an upload begins identifies matching existing videos early. If a duplicate is detected, the upload terminates immediately, preventing wasted incoming bandwidth, encoding CPU cycles, and storage capacity.",
      B: "Option B is incorrect. Post-processing deduplication does not corrupt metadata; it simply wastes compute resources prior to detection.",
      C: "Option C is incorrect. Deduplication identifies identical media content; encryption is a separate security layer.",
      D: "Option D is incorrect. Post-processing deduplication is widely implemented, but less efficient for large media files."
    }
  },
  {
    id: 7,
    topic: "Dynamic HTTP Redirections for Viral Content",
    question: "When a video goes viral, consistent hashing routes heavy traffic for that `VideoID` to a specific cache server instance. How do dynamic HTTP 302 redirections alleviate physical server overload?",
    options: {
      A: "By permanently deleting the viral video from storage to force users to refresh.",
      B: "By allowing an overloaded cache server to redirect clients to less busy cache servers in the same edge location.",
      C: "By converting incoming video stream requests into static HTML pages.",
      D: "By disabling client-side video playback controls."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Deleting content destroys core user experience and availability.",
      B: "Option B is correct! A single physical cache server assigned to a hot key can experience CPU/bandwidth saturation. The overloaded server responds with an HTTP 302 redirect pointing client playback devices to alternate, less busy cache instances within the cluster. (Trade-off: adds one extra HTTP round-trip latency penalty).",
      C: "Option C is incorrect. Video playback requires binary media streams, not static HTML.",
      D: "Option D is incorrect. HTTP redirection operates transparently at the networking tier."
    }
  },
  {
    id: 8,
    topic: "80-20 Rule & Metadata Cache Sizing",
    question: "In video streaming platforms, traffic follows Pareto's 80-20 rule (20% of popular videos generate 80% of daily views). How is this principle leveraged in cache capacity design?",
    options: {
      A: "By caching 100% of all uploaded historical video files directly in RAM.",
      B: "By caching 20% of the daily read volume of videos and metadata in an LRU memory cache, successfully absorbing ~80% of incoming read traffic.",
      C: "By deleting 80% of unpopular videos from the database every 24 hours.",
      D: "By restricting daily video views per user to 20 requests."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Caching 100% of petabyte-scale video archives in RAM is cost-prohibitive.",
      B: "Option B is correct! Because 80% of views target 20% of popular videos, caching just 20% of daily read volume in RAM (using LRU eviction to drop aging content) satisfies ~80% of read requests from memory without querying backend databases or origin storage.",
      C: "Option C is incorrect. Unpopular 'long-tail' content is preserved in cost-effective object storage, not deleted.",
      D: "Option D is incorrect. View throttling degrades user retention and product utility."
    }
  },
  {
    id: 9,
    topic: "Asynchronous AI Pipeline Integration",
    question: "Where do AI components (such as Speech-to-Text automatic captioning and content moderation classifiers) integrate into the architecture, and how should caption failure be handled?",
    options: {
      A: "Inside the synchronous `streamVideo` API call; if captions fail, streaming playback is aborted.",
      B: "Inside the asynchronous upload processing queue; captioning runs asynchronously so that if captioning fails, video publishing proceeds unblocked.",
      C: "Directly within client mobile web browsers before video upload begins.",
      D: "Inside database trigger procedures during SQL `INSERT` statements."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Running AI models during stream playback adds massive latency and failure risks to video playback.",
      B: "Option B is correct! AI captioning and moderation workers consume tasks from the asynchronous processing queue alongside encoders. Captions must be non-blocking: if speech recognition fails or delays, the video is published live immediately, and subtitle tracks append asynchronously once ready.",
      C: "Option C is incorrect. Mobile devices lack the memory and processing power to execute heavy deep-learning speech and moderation models.",
      D: "Option D is incorrect. SQL triggers should remain fast and transactionally lightweight."
    }
  }
];

let userAnswers = {};

function renderQuiz() {
  const container = document.getElementById('quiz-container');
  if (!container) return;

  container.innerHTML = quizQuestions.map((q, idx) => {
    return `
      <div class="quiz-card" id="quiz-card-${q.id}">
        <div class="quiz-card-header">
          <span class="quiz-topic">${q.topic}</span>
          <span class="quiz-qnum">Question ${idx + 1} of ${quizQuestions.length}</span>
        </div>
        <div class="quiz-question">${q.question}</div>
        <div class="quiz-options">
          ${Object.keys(q.options).map(optKey => `
            <button class="quiz-opt" id="opt-${q.id}-${optKey}" onclick="handleOptionClick(${q.id}, '${optKey}')">
              <span class="opt-letter">${optKey}</span>
              <span class="opt-text">${q.options[optKey]}</span>
            </button>
          `).join('')}
        </div>
        <div class="quiz-explanation" id="exp-${q.id}"></div>
      </div>
    `;
  }).join('');

  updateDashboard();
}

function handleOptionClick(questionId, selectedOpt) {
  const q = quizQuestions.find(item => item.id === questionId);
  if (!q) return;

  userAnswers[questionId] = selectedOpt;
  const isRight = (selectedOpt === q.correct);

  Object.keys(q.options).forEach(optKey => {
    const btn = document.getElementById(`opt-${questionId}-${optKey}`);
    if (!btn) return;

    btn.classList.remove('opt-correct', 'opt-incorrect');

    if (optKey === q.correct) {
      btn.classList.add('opt-correct');
    } else if (optKey === selectedOpt && !isRight) {
      btn.classList.add('opt-incorrect');
    }
  });

  const expDiv = document.getElementById(`exp-${questionId}`);
  if (expDiv) {
    expDiv.style.display = 'block';
    expDiv.className = `quiz-explanation ${isRight ? 'exp-right' : 'exp-wrong'}`;

    if (isRight) {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-check-circle" style="color: #16a34a;"></i> Correct Answer!
        </div>
        <div class="exp-body">
          ${q.explanations[selectedOpt]}
        </div>
      `;
    } else {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-times-circle" style="color: #dc2626;"></i> Incorrect Choice (Selected: ${selectedOpt})
        </div>
        <div class="exp-body">
          <p style="margin-bottom: 8px;"><strong>Why Option ${selectedOpt} is incorrect:</strong> ${q.explanations[selectedOpt]}</p>
          <p style="margin-top: 8px; margin-bottom: 0;"><strong>Correct Answer (${q.correct}):</strong> ${q.explanations[q.correct]}</p>
        </div>
      `;
    }
  }

  updateDashboard();
}

function updateDashboard() {
  const answeredCount = Object.keys(userAnswers).length;
  let correctCount = 0;

  Object.keys(userAnswers).forEach(qId => {
    const q = quizQuestions.find(item => item.id == qId);
    if (q && userAnswers[qId] === q.correct) {
      correctCount++;
    }
  });

  const total = quizQuestions.length;
  const accuracy = answeredCount > 0 ? Math.round((correctCount / answeredCount) * 100) : 0;
  const progressPercent = Math.round((answeredCount / total) * 100);

  document.getElementById('quiz-score').innerText = `${correctCount} / ${total}`;
  document.getElementById('quiz-progress').innerText = `${answeredCount} / ${total} Answered`;
  document.getElementById('quiz-accuracy').innerText = `${accuracy}%`;
  document.getElementById('quiz-progress-bar').style.width = `${progressPercent}%`;
}

function resetQuiz() {
  userAnswers = {};
  renderQuiz();
}

document.addEventListener('DOMContentLoaded', renderQuiz);
</script>
