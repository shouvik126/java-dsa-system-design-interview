---
layout: default
title: Quiz - Designing Typeahead Suggestion
permalink: /System-Design/4-system-design-problems/4.10-quiz-designing-typeahead-suggestion/
prev_title: "Designing Typeahead Suggestion"
prev_url: "/System-Design/4-system-design-problems/4.10-designing-typeahead-suggestion/"
next_title: "Designing an API Rate Limiter"
next_url: "/System-Design/4-system-design-problems/4.11-designing-an-api-rate-limiter/"
---

# Quiz: Designing Typeahead Suggestion

Test your architectural knowledge of Typeahead Suggestion system design with **9 scenario-driven interview questions** covering 200 ms real-time SLAs, In-Memory Trie vs B-Tree trade-offs, pre-computed Top 10 node caching, memory optimization via terminal references, offline MapReduce sampling, non-blocking primary-secondary deployment, Exponential Moving Average (EMA) frequency decay, Hash vs Capacity partitioning fan-out, and client-side debouncing with offline AI integration.

---

<!-- Quiz Dashboard -->
<div class="quiz-dashboard">
  <div class="dash-stat">
    <span class="dash-label">Score</span>
    <span class="dash-value" id="quiz-score">0 / 9</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Progress</span>
    <span class="dash-value" id="quiz-progress">0 / 9 Answered</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Accuracy</span>
    <span class="dash-value" id="quiz-accuracy">0%</span>
  </div>
  <button class="quiz-reset-btn" onclick="resetQuiz()">
    <i class="fas fa-redo-alt"></i> Reset Quiz
  </button>
</div>

<div class="quiz-progress-bar-container">
  <div class="quiz-progress-bar" id="quiz-progress-bar" style="width: 0%;"></div>
</div>

<div id="quiz-container"></div>

<style>
/* Quiz Styling - Compatible with Light & Dark Themes */
.quiz-dashboard {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-secondary, #f8fafc);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 16px 24px;
  margin-bottom: 16px;
}

html.dark-theme .quiz-dashboard {
  background: #1e293b;
  border-color: #334155;
}

.dash-stat {
  display: flex;
  flex-direction: column;
}

.dash-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted, #64748b);
  font-weight: 600;
}

.dash-value {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-color, #0f172a);
}

html.dark-theme .dash-value {
  color: #f8fafc;
}

.quiz-reset-btn {
  background: #3b82f6;
  color: #ffffff;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, transform 0.1s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.quiz-reset-btn:hover {
  background: #2563eb;
  transform: translateY(-1px);
}

.quiz-progress-bar-container {
  width: 100%;
  height: 8px;
  background: var(--border-color, #e2e8f0);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 32px;
}

html.dark-theme .quiz-progress-bar-container {
  background: #334155;
}

.quiz-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #3b82f6, #10b981);
  transition: width 0.3s ease;
}

.quiz-card {
  background: var(--bg-color, #ffffff);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 28px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  transition: border-color 0.2s ease;
}

html.dark-theme .quiz-card {
  background: #0f172a;
  border-color: #1e293b;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

.quiz-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.quiz-topic {
  font-size: 0.82rem;
  font-weight: 600;
  color: #2563eb;
  background: rgba(37, 99, 235, 0.1);
  padding: 4px 10px;
  border-radius: 20px;
}

html.dark-theme .quiz-topic {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.15);
}

.quiz-qnum {
  font-size: 0.85rem;
  color: var(--text-muted, #64748b);
  font-weight: 500;
}

.quiz-question {
  font-size: 1.1rem;
  font-weight: 600;
  margin-top: 6px;
  margin-bottom: 20px;
  color: var(--text-color, #0f172a);
  line-height: 1.5;
}

html.dark-theme .quiz-question {
  color: #f1f5f9;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.quiz-opt {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  width: 100%;
  text-align: left;
  background: var(--bg-secondary, #f8fafc);
  border: 1.5px solid var(--border-color, #e2e8f0);
  border-radius: 10px;
  padding: 14px 16px;
  font-size: 0.95rem;
  color: var(--text-color, #334155);
  cursor: pointer;
  transition: all 0.2s ease;
}

html.dark-theme .quiz-opt {
  background: #1e293b;
  border-color: #334155;
  color: #cbd5e1;
}

.quiz-opt:hover:not(:disabled) {
  border-color: #3b82f6;
  background: rgba(59, 130, 246, 0.05);
}

html.dark-theme .quiz-opt:hover:not(:disabled) {
  border-color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
}

.opt-letter {
  font-weight: 700;
  background: var(--border-color, #cbd5e1);
  color: #0f172a;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  flex-shrink: 0;
  font-size: 0.85rem;
}

html.dark-theme .opt-letter {
  background: #475569;
  color: #f8fafc;
}

.opt-text {
  line-height: 1.45;
  flex-grow: 1;
}

/* Correct Option State */
.quiz-opt.opt-correct {
  background: #dcfce7 !important;
  border-color: #16a34a !important;
  color: #14532d !important;
}

.quiz-opt.opt-correct .opt-letter {
  background: #16a34a !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-correct {
  background: rgba(22, 163, 74, 0.2) !important;
  border-color: #22c55e !important;
  color: #bbf7d0 !important;
}

/* Incorrect Option State */
.quiz-opt.opt-incorrect {
  background: #fef2f2 !important;
  border-color: #dc2626 !important;
  color: #7f1d1d !important;
}

.quiz-opt.opt-incorrect .opt-letter {
  background: #dc2626 !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-incorrect {
  background: rgba(220, 38, 38, 0.2) !important;
  border-color: #ef4444 !important;
  color: #fecaca !important;
}

/* Explanation Box */
.quiz-explanation {
  margin-top: 18px;
  padding: 16px;
  border-radius: 10px;
  font-size: 0.93rem;
  line-height: 1.55;
  display: none;
}

.quiz-explanation.exp-right {
  background: #f0fdf4;
  border: 1px solid #86efac;
  color: #166534;
}

html.dark-theme .quiz-explanation.exp-right {
  background: rgba(22, 163, 74, 0.15);
  border-color: #22c55e;
  color: #bbf7d0;
}

.quiz-explanation.exp-wrong {
  background: #fef2f2;
  border: 1px solid #fca5a5;
  color: #991b1b;
}

html.dark-theme .quiz-explanation.exp-wrong {
  background: rgba(220, 38, 38, 0.15);
  border-color: #ef4444;
  color: #fecaca;
}

.exp-header {
  font-weight: 700;
  font-size: 1rem;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.exp-body {
  margin-top: 6px;
}
</style>

<script>
const quizQuestions = [
  {
    id: 1,
    topic: "Latency SLAs & Real-Time Read Constraints",
    question: "In a global typeahead suggestion system processing 60,000 queries per second, responses must be delivered to users within a strict 200 ms budget. What primary architectural constraint does this 200 ms budget enforce on the read path?",
    options: {
      A: "The read path must serve suggestions entirely from in-memory data structures (Tries), completely avoiding disk-based database lookups during keystroke processing.",
      B: "The read path must run deep learning NLP transformers synchronously on every keystroke to predict user intent.",
      C: "The read path must issue synchronous SQL queries with ACID transactions to ensure real-time consistency.",
      D: "The read path must force clients to wait until 10 characters are typed before returning suggestions."
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! Disk access latency (millisecond range per seek) is far too slow to evaluate prefix queries on every keystroke under a 200 ms SLA. Hosting the entire 25 GB index in RAM inside Trie servers guarantees sub-millisecond data lookup times.",
      B: "Option B is incorrect. Synchronous NLP transformer inference takes tens or hundreds of milliseconds per request, violating the strict 200 ms latency budget.",
      C: "Option C is incorrect. Synchronous SQL queries and ACID transactions introduce massive lock contention and disk I/O bottlenecks.",
      D: "Option D is incorrect. Restricting inputs degrades user experience; the service must return suggestions starting from the very first character."
    }
  },
  {
    id: 2,
    topic: "Data Structure Selection: In-Memory Trie vs. Relational B-Tree",
    question: "Why is a Trie (or Radix Tree) data structure fundamentally superior to relational database B-Tree indexes for auto-complete prefix lookups?",
    options: {
      A: "Tries automatically encrypt stored search strings using AES-256.",
      B: "A Trie represents string paths character-by-character, allowing prefix lookups in O(L) time (where L is prefix length) regardless of the total number of indexed terms, whereas B-Tree range scans incur logarithmic disk seek overhead.",
      C: "B-Trees cannot store multi-byte Unicode strings.",
      D: "Tries eliminate the need for primary-secondary replication."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Data structure choice dictates lookup complexity and memory arrangement, not encryption algorithms.",
      B: "Option B is correct! A Trie organizes strings hierarchically by character. Navigating to a prefix node takes O(L) steps matching the typed letters. B-Trees on relational disk engines must perform index range scans across millions of rows, causing high latency.",
      C: "Option C is incorrect. Relational B-Trees handle Unicode strings natively via UTF-8/UTF-16 encodings.",
      D: "Option D is incorrect. Replication is still mandatory for high availability and fault tolerance."
    }
  },
  {
    id: 3,
    topic: "Pre-computed Top 10 Suggestions (Memory vs. Latency)",
    question: "Why do Trie nodes explicitly store a pre-computed list of their top 10 completions instead of traversing child sub-trees at query time?",
    options: {
      A: "Sub-trees in a global search index can be tens of levels deep; traversing thousands of sub-tree nodes on every keystroke to rank terms would break the 200 ms latency budget.",
      B: "Trie child pointers are automatically deleted after tree construction.",
      C: "To prevent clients from seeing non-personalized global search suggestions.",
      D: "Because memory is cheaper than CPU cache lines on modern servers."
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! Walking a deep sub-tree (e.g., 30 levels deep for long phrases) and aggregating frequency counts at query time requires scanning thousands of nodes per keystroke. Caching the top 10 results directly inside the prefix node trades memory storage for instant O(1) retrieval.",
      B: "Option B is incorrect. Child pointers are retained to allow deeper prefix traversals as users keep typing.",
      C: "Option C is incorrect. Pre-computed top lists contain global popular terms; personalization is layered on top.",
      D: "Option D is incorrect. The trade-off is specifically spending RAM capacity to eliminate runtime sub-tree traversal latency."
    }
  },
  {
    id: 4,
    topic: "Memory Footprint Optimization via Terminal Node References",
    question: "Storing full string phrases inside the top 10 lists of millions of Trie nodes creates massive memory redundancy. How is this storage cost minimized without losing lookup speed?",
    options: {
      A: "By compressing all string phrases using gzip inside each node.",
      B: "By storing references (pointers) to terminal nodes along with frequency counts inside top 10 lists, and reconstructing phrases by walking parent pointers upward.",
      C: "By deleting terminal nodes once the top 10 lists are computed.",
      D: "By storing only the first letter of each suggested search phrase."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Decompressing gzip strings on every keystroke adds CPU decompression overhead to read paths.",
      B: "Option B is correct! Replacing duplicated full text strings across prefix nodes with pointers to terminal nodes reduces memory consumption drastically. When returning suggestions, the full string is reconstructed on-the-fly by traversing parent pointers.",
      C: "Option C is incorrect. Terminal nodes store essential search count data and parent references needed to reconstruct phrases.",
      D: "Option D is incorrect. Returning only the first letter provides an incomplete, broken suggestion to the user."
    }
  },
  {
    id: 5,
    topic: "Decoupled Update Path & Query Log Sampling",
    question: "At 60,000 QPS, updating in-memory Trie counts synchronously on every search query is infeasible. How does the update pipeline maintain fresh term frequencies without degrading read performance?",
    options: {
      A: "By writing search queries directly to live Trie server RAM using atomic CPU instructions.",
      B: "By sampling incoming search queries (e.g., 1 in 1,000), logging them to persistent storage, and aggregating frequencies offline via periodic MapReduce/Spark batch jobs.",
      C: "By dropping 99% of user search requests completely without returning suggestions.",
      D: "By locking the Trie data structure for 5 seconds every minute to apply updates."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Direct synchronous RAM writes across multi-node clusters at 60K QPS create severe lock contention and CPU bottlenecks.",
      B: "Option B is correct! Sampling 1 out of 1,000 queries accurately captures statistically significant search trends while filtering out rare noise (<1000 searches). Offline MapReduce jobs process logs asynchronously, leaving live read traffic completely untouched.",
      C: "Option C is incorrect. Sampling occurs on the logging side; 100% of user read requests still receive suggestions.",
      D: "Option D is incorrect. Locking the Trie blocks all incoming read queries, violating availability and latency SLAs."
    }
  },
  {
    id: 6,
    topic: "Non-Blocking Trie Updates & Replica Promotion",
    question: "How do Trie servers apply updated offline-generated term frequencies to live memory without blocking incoming read requests?",
    options: {
      A: "By using primary-secondary server pairs or shadow Trie copies, updating the secondary/shadow offline and atomically swapping active traffic pointers.",
      B: "By rebooting all Trie servers simultaneously every hour.",
      C: "By converting incoming HTTP requests to UDP packets during updates.",
      D: "By executing SQL UPDATE statements directly against in-memory tables."
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! Updating a secondary node (or building a shadow Trie in memory) offline ensures zero disruption to live reads. Once the new structure is built, traffic is atomically swapped to the updated Trie copy, and the old version is reclaimed.",
      B: "Option B is incorrect. Rebooting all servers at once causes complete system downtime.",
      C: "Option C is incorrect. Changing transport protocols does not prevent write lock blocking.",
      D: "Option D is incorrect. Tries are custom in-memory tree structures, not relational SQL tables."
    }
  },
  {
    id: 7,
    topic: "Frequency Decay & Exponential Moving Average (EMA)",
    question: "Why is Exponential Moving Average (EMA) used instead of simple cumulative historical counts when updating term frequencies?",
    options: {
      A: "EMA eliminates the need to store data in main memory.",
      B: "EMA gives higher mathematical weight to recent search activity, allowing trending terms to rise quickly to the top 10 while stale historical terms naturally decay.",
      C: "EMA compresses ASCII strings into binary bitsets.",
      D: "EMA guarantees zero network latency between load balancers."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. EMA is a mathematical scoring function; it does not alter memory storage requirements.",
      B: "Option B is correct! Search trends evolve constantly (e.g., breaking news or seasonal events). Cumulative raw counts favor old historical terms forever. EMA applies exponential decay weights, favoring recent activity so real-time trends break into top 10 suggestion lists quickly.",
      C: "Option C is incorrect. EMA is an averaging calculation, not a string compression algorithm.",
      D: "Option D is incorrect. EMA operates at the scoring tier and has no effect on network latency."
    }
  },
  {
    id: 8,
    topic: "Partitioning Trade-offs: Hash Partitioning vs. Range Partitioning",
    question: "When distributing a 25 GB Trie across multiple servers, what is the primary operational trade-off of Hash-Based Partitioning compared to Range-Based Partitioning?",
    options: {
      A: "Hash partitioning causes severe data hotspots on popular starting letters.",
      B: "Hash partitioning eliminates server hotspots by spreading terms uniformly, but forces every prefix request to fan out to ALL Trie servers and pass through aggregator nodes.",
      C: "Hash partitioning prevents the system from storing multi-word phrases.",
      D: "Range partitioning allows instant lookup without knowing starting letters."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Range partitioning causes hotspots (e.g., letters 'S' or 'C' carry far more words than 'Z'); Hash partitioning eliminates hotspots.",
      B: "Option B is correct! Hashing terms spreads data randomly across servers, preventing traffic hotspots. However, because terms starting with 'cap' can reside on any node, every prefix query must fan out to all Trie partition servers, requiring an aggregator layer to merge partial top 10 lists.",
      C: "Option C is incorrect. Hashing handles multi-word strings seamlessly.",
      D: "Option D is incorrect. Range partitioning relies strictly on initial characters to determine server destinations."
    }
  },
  {
    id: 9,
    topic: "Client-Side Optimizations & Asynchronous AI Integration",
    question: "Where does AI/ML model scoring fit into a Typeahead Suggestion service with a 200 ms SLA, and how do client-side optimizations reduce backend server load?",
    options: {
      A: "AI models run inline on client devices; client-side optimizations send HTTP calls on every keystroke without delay.",
      B: "AI models run offline during MapReduce update pipelines to compute pre-ranked top 10 lists; clients use 50 ms debouncing and request cancellation to minimize unnecessary QPS.",
      C: "AI models evaluate search trends in real time on the load balancer; client debouncing increases total server QPS.",
      D: "AI models run synchronously inside database triggers; clients store full Trie snapshot copies in local storage."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Heavy ML models cannot run inline on client hardware per keystroke without incurring severe latency and battery drain.",
      B: "Option B is correct! Heavy ML ranking models (factoring in location, trends, and user demographics) execute offline during batch builds, populating top 10 lists inside snapshot files. On the client side, 50 ms debouncing (waiting for typing pauses) and cancelling outdated in-flight requests reduce backend QPS significantly.",
      C: "Option C is incorrect. Debouncing reduces server QPS by skipping intermediate keystrokes; AI models do not run on load balancers.",
      D: "Option D is incorrect. Synchronous ML execution inside database triggers degrades system throughput and violates SLAs."
    }
  }
];

let userAnswers = {};

function renderQuiz() {
  const container = document.getElementById('quiz-container');
  if (!container) return;

  container.innerHTML = quizQuestions.map((q, idx) => {
    return `
      <div class="quiz-card" id="quiz-card-${q.id}">
        <div class="quiz-card-header">
          <span class="quiz-topic">${q.topic}</span>
          <span class="quiz-qnum">Question ${idx + 1} of ${quizQuestions.length}</span>
        </div>
        <div class="quiz-question">${q.question}</div>
        <div class="quiz-options">
          ${Object.keys(q.options).map(optKey => `
            <button class="quiz-opt" id="opt-${q.id}-${optKey}" onclick="handleOptionClick(${q.id}, '${optKey}')">
              <span class="opt-letter">${optKey}</span>
              <span class="opt-text">${q.options[optKey]}</span>
            </button>
          `).join('')}
        </div>
        <div class="quiz-explanation" id="exp-${q.id}"></div>
      </div>
    `;
  }).join('');

  updateDashboard();
}

function handleOptionClick(questionId, selectedOpt) {
  const q = quizQuestions.find(item => item.id === questionId);
  if (!q) return;

  userAnswers[questionId] = selectedOpt;
  const isRight = (selectedOpt === q.correct);

  Object.keys(q.options).forEach(optKey => {
    const btn = document.getElementById(`opt-${questionId}-${optKey}`);
    if (!btn) return;

    btn.classList.remove('opt-correct', 'opt-incorrect');

    if (optKey === q.correct) {
      btn.classList.add('opt-correct');
    } else if (optKey === selectedOpt && !isRight) {
      btn.classList.add('opt-incorrect');
    }
  });

  const expDiv = document.getElementById(`exp-${questionId}`);
  if (expDiv) {
    expDiv.style.display = 'block';
    expDiv.className = `quiz-explanation ${isRight ? 'exp-right' : 'exp-wrong'}`;

    if (isRight) {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-check-circle" style="color: #16a34a;"></i> Correct Answer!
        </div>
        <div class="exp-body">
          ${q.explanations[selectedOpt]}
        </div>
      `;
    } else {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-times-circle" style="color: #dc2626;"></i> Incorrect Choice (Selected: ${selectedOpt})
        </div>
        <div class="exp-body">
          <p style="margin-bottom: 8px;"><strong>Why Option ${selectedOpt} is incorrect:</strong> ${q.explanations[selectedOpt]}</p>
          <p style="margin-top: 8px; margin-bottom: 0;"><strong>Correct Answer (${q.correct}):</strong> ${q.explanations[q.correct]}</p>
        </div>
      `;
    }
  }

  updateDashboard();
}

function updateDashboard() {
  const answeredCount = Object.keys(userAnswers).length;
  let correctCount = 0;

  Object.keys(userAnswers).forEach(qId => {
    const q = quizQuestions.find(item => item.id == qId);
    if (q && userAnswers[qId] === q.correct) {
      correctCount++;
    }
  });

  const total = quizQuestions.length;
  const accuracy = answeredCount > 0 ? Math.round((correctCount / answeredCount) * 100) : 0;
  const progressPercent = Math.round((answeredCount / total) * 100);

  document.getElementById('quiz-score').innerText = `${correctCount} / ${total}`;
  document.getElementById('quiz-progress').innerText = `${answeredCount} / ${total} Answered`;
  document.getElementById('quiz-accuracy').innerText = `${accuracy}%`;
  document.getElementById('quiz-progress-bar').style.width = `${progressPercent}%`;
}

function resetQuiz() {
  userAnswers = {};
  renderQuiz();
}

document.addEventListener('DOMContentLoaded', renderQuiz);
</script>
