---
layout: default
title: Quiz - Designing Typeahead Suggestion
permalink: /System-Design/4-system-design-problems/4.10-quiz-designing-typeahead-suggestion/
prev_title: "Designing Typeahead Suggestion"
prev_url: "/System-Design/4-system-design-problems/4.10-designing-typeahead-suggestion/"
next_title: "Designing an API Rate Limiter"
next_url: "/System-Design/4-system-design-problems/4.11-designing-an-api-rate-limiter/"
---

# Quiz: Designing Typeahead Suggestion

Test your architectural knowledge of Typeahead Suggestion system design with **9 scenario-driven interview questions** covering in-memory Trie storage vs disk DB bottlenecks, pre-computed Top 10 node caching, log sampling & MapReduce update pipelines, Exponential Moving Average (EMA) recency scoring, real-time blacklist filtering layers, Hash vs Range partitioning fan-out trade-offs, level-by-level BFS snapshot serialization, client-side debouncing and request cancellation, and offline AI ranking engine execution.

---

<!-- Quiz Dashboard -->
<div class="quiz-dashboard">
  <div class="dash-stat">
    <span class="dash-label">Score</span>
    <span class="dash-value" id="quiz-score">0 / 9</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Progress</span>
    <span class="dash-value" id="quiz-progress">0 / 9 Answered</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Accuracy</span>
    <span class="dash-value" id="quiz-accuracy">0%</span>
  </div>
  <button class="quiz-reset-btn" onclick="resetQuiz()">
    <i class="fas fa-redo-alt"></i> Reset Quiz
  </button>
</div>

<div class="quiz-progress-bar-container">
  <div class="quiz-progress-bar" id="quiz-progress-bar" style="width: 0%;"></div>
</div>

<div id="quiz-container"></div>

<style>
/* Quiz Styling - Compatible with Light & Dark Themes */
.quiz-dashboard {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-secondary, #f8fafc);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 16px 24px;
  margin-bottom: 16px;
}

html.dark-theme .quiz-dashboard {
  background: #1e293b;
  border-color: #334155;
}

.dash-stat {
  display: flex;
  flex-direction: column;
}

.dash-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted, #64748b);
  font-weight: 600;
}

.dash-value {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-color, #0f172a);
}

html.dark-theme .dash-value {
  color: #f8fafc;
}

.quiz-reset-btn {
  background: #3b82f6;
  color: #ffffff;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, transform 0.1s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.quiz-reset-btn:hover {
  background: #2563eb;
  transform: translateY(-1px);
}

.quiz-progress-bar-container {
  width: 100%;
  height: 8px;
  background: var(--border-color, #e2e8f0);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 32px;
}

html.dark-theme .quiz-progress-bar-container {
  background: #334155;
}

.quiz-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #3b82f6, #10b981);
  transition: width 0.3s ease;
}

.quiz-card {
  background: var(--bg-color, #ffffff);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 28px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  transition: border-color 0.2s ease;
}

html.dark-theme .quiz-card {
  background: #0f172a;
  border-color: #1e293b;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

.quiz-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.quiz-topic {
  font-size: 0.82rem;
  font-weight: 600;
  color: #2563eb;
  background: rgba(37, 99, 235, 0.1);
  padding: 4px 10px;
  border-radius: 20px;
}

html.dark-theme .quiz-topic {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.15);
}

.quiz-qnum {
  font-size: 0.85rem;
  color: var(--text-muted, #64748b);
  font-weight: 500;
}

.quiz-question {
  font-size: 1.1rem;
  font-weight: 600;
  margin-top: 6px;
  margin-bottom: 20px;
  color: var(--text-color, #0f172a);
  line-height: 1.5;
}

html.dark-theme .quiz-question {
  color: #f1f5f9;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.quiz-opt {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  width: 100%;
  text-align: left;
  background: var(--bg-secondary, #f8fafc);
  border: 1.5px solid var(--border-color, #e2e8f0);
  border-radius: 10px;
  padding: 14px 16px;
  font-size: 0.95rem;
  color: var(--text-color, #334155);
  cursor: pointer;
  transition: all 0.2s ease;
}

html.dark-theme .quiz-opt {
  background: #1e293b;
  border-color: #334155;
  color: #cbd5e1;
}

.quiz-opt:hover:not(:disabled) {
  border-color: #3b82f6;
  background: rgba(59, 130, 246, 0.05);
}

html.dark-theme .quiz-opt:hover:not(:disabled) {
  border-color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
}

.opt-letter {
  font-weight: 700;
  background: var(--border-color, #cbd5e1);
  color: #0f172a;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  flex-shrink: 0;
  font-size: 0.85rem;
}

html.dark-theme .opt-letter {
  background: #475569;
  color: #f8fafc;
}

.opt-text {
  line-height: 1.45;
  flex-grow: 1;
}

/* Correct Option State */
.quiz-opt.opt-correct {
  background: #dcfce7 !important;
  border-color: #16a34a !important;
  color: #14532d !important;
}

.quiz-opt.opt-correct .opt-letter {
  background: #16a34a !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-correct {
  background: rgba(22, 163, 74, 0.2) !important;
  border-color: #22c55e !important;
  color: #bbf7d0 !important;
}

/* Incorrect Option State */
.quiz-opt.opt-incorrect {
  background: #fef2f2 !important;
  border-color: #dc2626 !important;
  color: #7f1d1d !important;
}

.quiz-opt.opt-incorrect .opt-letter {
  background: #dc2626 !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-incorrect {
  background: rgba(220, 38, 38, 0.2) !important;
  border-color: #ef4444 !important;
  color: #fecaca !important;
}

/* Explanation Box */
.quiz-explanation {
  margin-top: 18px;
  padding: 16px;
  border-radius: 10px;
  font-size: 0.93rem;
  line-height: 1.55;
  display: none;
}

.quiz-explanation.exp-right {
  background: #f0fdf4;
  border: 1px solid #86efac;
  color: #166534;
}

html.dark-theme .quiz-explanation.exp-right {
  background: rgba(22, 163, 74, 0.15);
  border-color: #22c55e;
  color: #bbf7d0;
}

.quiz-explanation.exp-wrong {
  background: #fef2f2;
  border: 1px solid #fca5a5;
  color: #991b1b;
}

html.dark-theme .quiz-explanation.exp-wrong {
  background: rgba(220, 38, 38, 0.15);
  border-color: #ef4444;
  color: #fecaca;
}

.exp-header {
  font-weight: 700;
  font-size: 1rem;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.exp-body {
  margin-top: 6px;
}
</style>

<script>
const quizQuestions = [
  {
    id: 1,
    topic: "In-Memory Data Structures vs Relational Disk Bottlenecks",
    question: "Typeahead autocompletion requires serving 60,000 QPS under a strict 200 ms total response budget (< 20 ms server processing latency). Why is executing SQL 'LIKE prefix%' queries against a disk-backed relational database (e.g. MySQL) architecturally impossible at this scale, and why does an in-memory Trie solve this problem?",
    options: {
      A: "Because relational databases cannot store multi-byte UTF-8 string data.",
      B: "Because disk I/O seek times (~5-10 ms per operation) cause extreme queueing bottlenecks under 60,000 QPS, whereas an in-memory Trie eliminates disk seek latency entirely and enables O(K) prefix lookups in RAM.",
      C: "Because relational databases do not support primary key indexes on text columns.",
      D: "Because in-memory Tries automatically compress network payloads into JSON format without CPU overhead."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Relational databases natively support UTF-8 multi-byte encoding.",
      B: "Option B is correct! Disk I/O seek operations take 5–10 ms each. Under a read load of 60,000 QPS, disk spindles and I/O channels stall, driving response latencies far past the 200 ms budget. An in-memory Trie holds the dataset (~25 GB) in RAM, delivering prefix lookups in sub-millisecond time.",
      C: "Option C is incorrect. SQL databases support B-Tree indexes on text columns, but disk I/O access remains the bottleneck.",
      D: "Option D is incorrect. Tries organize string prefixes in memory; payload serialization is handled separately by web application servers."
    }
  },
  {
    id: 2,
    topic: "Memory vs Latency Trade-Offs in Trie Lookups",
    question: "Standard sub-tree traversal (Depth-First Search) from a prefix node to gather and sort completion candidates violates the server processing budget when sub-trees are deep or wide. How does the typeahead engine achieve sub-millisecond prefix resolution, and what is the trade-off?",
    options: {
      A: "By delegating sub-tree sorting to client browsers using WebAssembly.",
      B: "By pre-computing and caching the top 10 completion references directly inside every single Trie Node, trading node memory storage for sub-millisecond O(K) read latencies.",
      C: "By restricting query prefixes to a maximum length of 2 characters.",
      D: "By clearing Trie memory after every 1,000 user requests to prevent tree growth."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Sub-tree traversal is performed on backend server nodes, not client-side WebAssembly.",
      B: "Option B is correct! Traversing deep sub-trees on every keystroke breaks latency bounds. By storing pre-computed top 10 lists directly within each node, lookups become O(K) (where K is prefix length). The trade-off is increased memory usage due to duplicated terminal node references across ancestor nodes.",
      C: "Option C is incorrect. The engine handles arbitrary prefix lengths typed by users.",
      D: "Option D is incorrect. Clearing memory periodically destroys system accuracy and cache utility."
    }
  },
  {
    id: 3,
    topic: "Decoupled Write Pipelines & Log Sampling",
    question: "At a read throughput of 60,000 QPS, executing real-time in-memory Trie mutations for every incoming user query causes fatal lock contention and stalls read threads. How does the update pipeline maintain data freshness without impacting read performance?",
    options: {
      A: "By locking the entire Trie data structure for 1 second every minute to execute in-place updates.",
      B: "By sampling incoming queries (e.g. 1 in 1,000), aggregating search frequencies offline using MapReduce, and deploying fresh Trie snapshots asynchronously via atomic pointer swaps or primary-secondary failovers.",
      C: "By requiring users to submit search query frequency counts manually via a dedicated REST endpoint.",
      D: "By forcing client browsers to store and update global query frequencies in local storage."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Locking the Trie causes massive request queueing and breaks low-latency guarantees for active reads.",
      B: "Option B is correct! Logging every query is wasteful. Sampling 1 out of 1,000 queries provides accurate statistical distribution while reducing log volume by 99.9%. Offline MapReduce jobs aggregate frequencies out-of-band, and new trie instances are swapped into service atomically without taking read locks.",
      C: "Option C is incorrect. Search analytics are captured transparently by API gateways, not manual user submissions.",
      D: "Option D is incorrect. Global search popularity requires centralized aggregation across millions of users."
    }
  },
  {
    id: 4,
    topic: "Recency Weighting & Frequency Decay",
    question: "Cumulative historical search counts fail to capture sudden breaking news or rapidly emerging search trends. How does the offline frequency aggregation pipeline score terms to ensure recent trends override outdated historical popularity?",
    options: {
      A: "By resetting all term search counters to zero at midnight every day.",
      B: "By applying an Exponential Moving Average (EMA) over a rolling time window (e.g., 10 days), giving higher mathematical weight to recent search volume while decaying older counts.",
      C: "By manually hardcoding breaking news keywords into database configuration files.",
      D: "By sorting suggestions alphabetically whenever search counts tie."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Daily zero resets destroy historical context for consistently popular search queries.",
      B: "Option B is correct! Exponential Moving Average (EMA) applies a decay factor that heavily weights search volume in recent time windows while gradually reducing the impact of older searches. This allows viral breaking news terms to quickly climb top 10 lists.",
      C: "Option C is incorrect. High-scale autocompletion requires automated algorithmic scoring rather than manual file edits.",
      D: "Option D is incorrect. Alphabetical sorting addresses tie-breaking, not temporal recency weighting."
    }
  },
  {
    id: 5,
    topic: "Immediate Content Moderation & Policy Enforcement",
    question: "A court order or policy violation requires the immediate removal of a specific autocompletion phrase from user dropdowns. Why is relying solely on the next offline MapReduce build cycle inadequate, and how is instant removal implemented?",
    options: {
      A: "Offline MapReduce jobs cannot delete terms once they enter the Term Frequency DB.",
      B: "Waiting for offline batch builds (which take hours) leaves policy-violating terms live; an in-memory Filtering Layer (Bloom Filter / Blacklist Set) on Application Servers intercepts and strips banned phrases instantly before returning results.",
      C: "Banned terms are deleted by restarting all Trie Servers simultaneously.",
      D: "The system forces all clients to clear their browser cache via remote HTTP payload triggers."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Offline pipelines purge banned entries easily, but batch jobs run on hourly/daily schedules.",
      B: "Option B is correct! Legal and policy compliance demands sub-second removal. Because updating the offline trie takes time, an in-memory filtering layer (such as a Redis Bloom Filter or Sync Set) sits on Application Servers, dropping banned suggestions in real-time before responses reach end-users.",
      C: "Option C is incorrect. Restarting servers causes unnecessary downtime without modifying the underlying dataset.",
      D: "Option D is incorrect. Client-side cache invalidation does not prevent backend servers from continuing to serve banned terms."
    }
  },
  {
    id: 6,
    topic: "Distributed Trie Partitioning Strategies",
    question: "When scaling a 25 GB Trie across multiple physical servers, an engineering team compares Hash-based partitioning against Alphabetical Range-based partitioning. What is the fundamental operational trade-off of Hash-based partitioning?",
    options: {
      A: "Hash-based partitioning causes data corruptions because strings cannot be hashed cleanly.",
      B: "Hash-based partitioning distributes memory and traffic uniformly across nodes to eliminate hotspots, but scatters matching prefixes, forcing every prefix query to broadcast (fan-out) to all partition servers and merge results via aggregators.",
      C: "Hash-based partitioning requires 100x more RAM than Range-based partitioning.",
      D: "Range-based partitioning eliminates query hotspots by dynamically resizing server memory on the fly."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Standard cryptographic and non-cryptographic hash functions (e.g. MurmurHash) hash strings deterministically.",
      B: "Option B is correct! Range-based partitioning causes severe data imbalance because letters like 'S' have far more terms than 'Z'. Hashing terms spreads data and memory load uniformly across servers, eliminating hotspots. However, because hashes break prefix locality, searching for prefix 'cap' requires broadcasting the query to all N nodes and aggregating partial results.",
      C: "Option C is incorrect. Data storage requirements remain roughly identical regardless of partitioning scheme.",
      D: "Option D is incorrect. Range-based partitioning suffers heavily from static data imbalance and hotspots."
    }
  },
  {
    id: 7,
    topic: "Durable Snapshotting & Rebuild Mechanics",
    question: "A Trie Server experiences a hardware failure and reboots. Re-ingesting raw text logs to reconstruct the in-memory tree takes hours. How does serialized level-by-level (BFS) snapshotting enable fast node recovery, and why are top 10 cached lists excluded from the snapshot file?",
    options: {
      A: "Excluding top 10 lists reduces file size and avoids complex pointer serialization; on startup, the server reads the lean node structure and recalculates top 10 lists using a fast bottom-up pass.",
      B: "Top 10 lists cannot be written to disk because binary files do not support array structures.",
      C: "Snapshotting level-by-level compresses raw strings into JPEG images that load instantly in RAM.",
      D: "Restoring from snapshots deletes all historical frequency counts stored in the snapshot file."
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! Serializing memory pointers directly to disk creates complex binary offsets that break across reloads. Omitting top 10 cached lists keeps snapshot files small, clean, and sequential. Once the BFS tree topology is restored in RAM, a single bottom-up pass recalculates parent top 10 lists efficiently.",
      B: "Option B is incorrect. Binary files serialize arbitrary arrays and structures without issue.",
      C: "Option C is incorrect. Snapshots store structured tree metadata (character + child count), not image files.",
      D: "Option D is incorrect. Terminal frequency counts are preserved in the snapshot file to allow accurate list recalculation."
    }
  },
  {
    id: 8,
    topic: "Client-Side Latency Reduction & Load Suppression",
    question: "A web client experiences high backend network overhead while a user types rapidly. Which combination of client-side techniques minimizes unnecessary server request volume without sacrificing autocompletion responsiveness?",
    options: {
      A: "Forcing the client to wait until the user presses the 'Enter' key before requesting suggestions.",
      B: "Keystroke debouncing (e.g. 50 ms typing pause), canceling stale in-flight fetch requests via AbortController, requiring a minimum prefix length (2+ characters), and local client-side LRU caching.",
      C: "Disabling HTTP keep-alive connections so every keystroke creates a fresh TCP connection.",
      D: "Downloading the entire 25 GB Trie dataset to client browser storage upon initial page load."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Waiting for the 'Enter' key defeats the entire real-time purpose of typeahead autocomplete.",
      B: "Option B is correct! Client-side optimizations suppress redundant load before it touches the network: debouncing avoids firing requests mid-keystroke, AbortController cancels outdated responses, minimum prefix lengths prevent single-letter lookups, and browser caching serves backspace queries locally.",
      C: "Option C is incorrect. Disabling keep-alive forces expensive TCP/TLS handshakes on every keystroke, driving up latency.",
      D: "Option D is incorrect. Transferring 25 GB to browser memory is impossible and violates mobile network constraints."
    }
  },
  {
    id: 9,
    topic: "Offline Machine Learning Integration",
    question: "A product manager asks to integrate a deep neural network / LLM to generate intelligent, personalized autocompletion recommendations on every keystroke. Why is executing real-time AI model inference on the request path infeasible, and where does AI actually fit into the system architecture?",
    options: {
      A: "AI models can only process numerical inputs and cannot understand prefix strings.",
      B: "Real-time AI inference latency (200-800 ms) exceeds the total response budget; instead, AI models run in the offline batch update pipeline to compute multi-factor contextual scores for top 10 lists pre-stored in Trie nodes.",
      C: "AI inference requires storing all user passwords in public database tables.",
      D: "Offline AI pipelines cannot run on servers that host Redis caching instances."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Modern Natural Language Processing (NLP) models excel at string processing.",
      B: "Option B is correct! Deep learning inference consumes 200–800 ms, violating our strict 200 ms total response budget (< 20 ms server path). Instead, complex ML models run offline during MapReduce batch generation to rank top 10 lists using multi-variable features (trends, location, user affinity). The results are serialized directly into Trie nodes for sub-millisecond online serving.",
      C: "Option C is incorrect. Machine learning inference operates on search logs and user intent signals, completely independent of password tables.",
      D: "Option D is incorrect. Offline pipeline execution is completely decoupled from online caching tiers."
    }
  }
];

let userAnswers = {};

function renderQuiz() {
  const container = document.getElementById('quiz-container');
  if (!container) return;

  container.innerHTML = quizQuestions.map((q, idx) => {
    return `
      <div class="quiz-card" id="quiz-card-${q.id}">
        <div class="quiz-card-header">
          <span class="quiz-topic">${q.topic}</span>
          <span class="quiz-qnum">Question ${idx + 1} of ${quizQuestions.length}</span>
        </div>
        <div class="quiz-question">${q.question}</div>
        <div class="quiz-options">
          ${Object.keys(q.options).map(optKey => `
            <button class="quiz-opt" id="opt-${q.id}-${optKey}" onclick="handleOptionClick(${q.id}, '${optKey}')">
              <span class="opt-letter">${optKey}</span>
              <span class="opt-text">${q.options[optKey]}</span>
            </button>
          `).join('')}
        </div>
        <div class="quiz-explanation" id="exp-${q.id}"></div>
      </div>
    `;
  }).join('');

  updateDashboard();
}

function handleOptionClick(questionId, selectedOpt) {
  const q = quizQuestions.find(item => item.id === questionId);
  if (!q) return;

  userAnswers[questionId] = selectedOpt;
  const isRight = (selectedOpt === q.correct);

  Object.keys(q.options).forEach(optKey => {
    const btn = document.getElementById(`opt-${questionId}-${optKey}`);
    if (!btn) return;

    btn.classList.remove('opt-correct', 'opt-incorrect');

    if (optKey === q.correct) {
      btn.classList.add('opt-correct');
    } else if (optKey === selectedOpt && !isRight) {
      btn.classList.add('opt-incorrect');
    }
  });

  const expDiv = document.getElementById(`exp-${questionId}`);
  if (expDiv) {
    expDiv.style.display = 'block';
    expDiv.className = `quiz-explanation ${isRight ? 'exp-right' : 'exp-wrong'}`;

    if (isRight) {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-check-circle" style="color: #16a34a;"></i> Correct Answer!
        </div>
        <div class="exp-body">
          ${q.explanations[selectedOpt]}
        </div>
      `;
    } else {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-times-circle" style="color: #dc2626;"></i> Incorrect Choice (Selected: ${selectedOpt})
        </div>
        <div class="exp-body">
          <p style="margin-bottom: 8px;"><strong>Why Option ${selectedOpt} is incorrect:</strong> ${q.explanations[selectedOpt]}</p>
          <p style="margin-top: 8px; margin-bottom: 0;"><strong>Correct Answer (${q.correct}):</strong> ${q.explanations[q.correct]}</p>
        </div>
      `;
    }
  }

  updateDashboard();
}

function updateDashboard() {
  const answeredCount = Object.keys(userAnswers).length;
  let correctCount = 0;

  Object.keys(userAnswers).forEach(qId => {
    const q = quizQuestions.find(item => item.id == qId);
    if (q && userAnswers[qId] === q.correct) {
      correctCount++;
    }
  });

  const total = quizQuestions.length;
  const accuracy = answeredCount > 0 ? Math.round((correctCount / answeredCount) * 100) : 0;
  const progressPercent = Math.round((answeredCount / total) * 100);

  document.getElementById('quiz-score').innerText = `${correctCount} / ${total}`;
  document.getElementById('quiz-progress').innerText = `${answeredCount} / ${total} Answered`;
  document.getElementById('quiz-accuracy').innerText = `${accuracy}%`;
  document.getElementById('quiz-progress-bar').style.width = `${progressPercent}%`;
}

function resetQuiz() {
  userAnswers = {};
  renderQuiz();
}

document.addEventListener('DOMContentLoaded', renderQuiz);
</script>
