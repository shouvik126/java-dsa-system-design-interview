---
layout: default
title: Quiz - Designing an API Rate Limiter
permalink: /System-Design/4-system-design-problems/4.11-quiz-designing-an-api-rate-limiter/
prev_title: "Designing an API Rate Limiter"
prev_url: "/System-Design/4-system-design-problems/4.11-designing-an-api-rate-limiter/"
---

# Quiz: Designing an API Rate Limiter

Test your architectural knowledge of API Rate Limiter system design with **9 scenario-driven interview questions** covering distributed hard vs. soft throttling, fixed window edge burst flaws, sliding window timestamp ZSET memory footprints, Redis Hash sub-counter optimizations, token bucket state efficiency, atomic Lua execution against race conditions, fail-open vs. fail-closed strategies under failure, and offline AI anomaly detection.

---

<!-- Quiz Dashboard -->
<div class="quiz-dashboard">
  <div class="dash-stat">
    <span class="dash-label">Score</span>
    <span class="dash-value" id="quiz-score">0 / 9</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Progress</span>
    <span class="dash-value" id="quiz-progress">0 / 9 Answered</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Accuracy</span>
    <span class="dash-value" id="quiz-accuracy">0%</span>
  </div>
  <button class="quiz-reset-btn" onclick="resetQuiz()">
    <i class="fas fa-redo-alt"></i> Reset Quiz
  </button>
</div>

<div class="quiz-progress-bar-container">
  <div class="quiz-progress-bar" id="quiz-progress-bar" style="width: 0%;"></div>
</div>

<div id="quiz-container"></div>

<style>
/* Quiz Styling - Compatible with Light & Dark Themes */
.quiz-dashboard {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-secondary, #f8fafc);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 16px 24px;
  margin-bottom: 16px;
}

html.dark-theme .quiz-dashboard {
  background: #1e293b;
  border-color: #334155;
}

.dash-stat {
  display: flex;
  flex-direction: column;
}

.dash-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted, #64748b);
  font-weight: 600;
}

.dash-value {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-color, #0f172a);
}

html.dark-theme .dash-value {
  color: #f8fafc;
}

.quiz-reset-btn {
  background: #3b82f6;
  color: #ffffff;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, transform 0.1s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.quiz-reset-btn:hover {
  background: #2563eb;
  transform: translateY(-1px);
}

.quiz-progress-bar-container {
  width: 100%;
  height: 8px;
  background: var(--border-color, #e2e8f0);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 32px;
}

html.dark-theme .quiz-progress-bar-container {
  background: #334155;
}

.quiz-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #3b82f6, #10b981);
  transition: width 0.3s ease;
}

.quiz-card {
  background: var(--bg-color, #ffffff);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 28px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  transition: border-color 0.2s ease;
}

html.dark-theme .quiz-card {
  background: #0f172a;
  border-color: #1e293b;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

.quiz-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.quiz-topic {
  font-size: 0.82rem;
  font-weight: 600;
  color: #2563eb;
  background: rgba(37, 99, 235, 0.1);
  padding: 4px 10px;
  border-radius: 20px;
}

html.dark-theme .quiz-topic {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.15);
}

.quiz-qnum {
  font-size: 0.85rem;
  color: var(--text-muted, #64748b);
  font-weight: 500;
}

.quiz-question {
  font-size: 1.1rem;
  font-weight: 600;
  margin-top: 6px;
  margin-bottom: 20px;
  color: var(--text-color, #0f172a);
  line-height: 1.5;
}

html.dark-theme .quiz-question {
  color: #f1f5f9;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.quiz-opt {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  width: 100%;
  text-align: left;
  background: var(--bg-secondary, #f8fafc);
  border: 1.5px solid var(--border-color, #e2e8f0);
  border-radius: 10px;
  padding: 14px 16px;
  font-size: 0.95rem;
  color: var(--text-color, #334155);
  cursor: pointer;
  transition: all 0.2s ease;
}

html.dark-theme .quiz-opt {
  background: #1e293b;
  border-color: #334155;
  color: #cbd5e1;
}

.quiz-opt:hover:not(:disabled) {
  border-color: #3b82f6;
  background: rgba(59, 130, 246, 0.05);
}

html.dark-theme .quiz-opt:hover:not(:disabled) {
  border-color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
}

.opt-letter {
  font-weight: 700;
  background: var(--border-color, #cbd5e1);
  color: #0f172a;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  flex-shrink: 0;
  font-size: 0.85rem;
}

html.dark-theme .opt-letter {
  background: #475569;
  color: #f8fafc;
}

.opt-text {
  line-height: 1.45;
  flex-grow: 1;
}

/* Correct Option State */
.quiz-opt.opt-correct {
  background: #dcfce7 !important;
  border-color: #16a34a !important;
  color: #14532d !important;
}

.quiz-opt.opt-correct .opt-letter {
  background: #16a34a !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-correct {
  background: rgba(22, 163, 74, 0.2) !important;
  border-color: #22c55e !important;
  color: #bbf7d0 !important;
}

/* Incorrect Option State */
.quiz-opt.opt-incorrect {
  background: #fef2f2 !important;
  border-color: #dc2626 !important;
  color: #7f1d1d !important;
}

.quiz-opt.opt-incorrect .opt-letter {
  background: #dc2626 !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-incorrect {
  background: rgba(220, 38, 38, 0.2) !important;
  border-color: #ef4444 !important;
  color: #fecaca !important;
}

/* Explanation Box */
.quiz-explanation {
  margin-top: 18px;
  padding: 16px;
  border-radius: 10px;
  font-size: 0.93rem;
  line-height: 1.55;
  display: none;
}

.quiz-explanation.exp-right {
  background: #f0fdf4;
  border: 1px solid #86efac;
  color: #166534;
}

html.dark-theme .quiz-explanation.exp-right {
  background: rgba(22, 163, 74, 0.15);
  border-color: #22c55e;
  color: #bbf7d0;
}

.quiz-explanation.exp-wrong {
  background: #fef2f2;
  border: 1px solid #fca5a5;
  color: #991b1b;
}

html.dark-theme .quiz-explanation.exp-wrong {
  background: rgba(220, 38, 38, 0.15);
  border-color: #ef4444;
  color: #fecaca;
}

.exp-header {
  font-weight: 700;
  font-size: 1rem;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.exp-body {
  margin-top: 6px;
}
</style>

<script>
const quizQuestions = [
  {
    id: 1,
    topic: "Throttling Placement & Resource Protection",
    question: "Why does the rate limiter evaluate incoming requests at the API Gateway / Web Server layer (in front of application servers) and return HTTP 429 Too Many Requests?",
    options: {
      A: "To allow application servers to process request bodies before deciding whether to log them.",
      B: "To drop excessive traffic immediately at the perimeter so rejected requests consume zero downstream CPU, RAM, or database resources.",
      C: "Because HTTP status 429 can only be rendered by edge web proxies.",
      D: "To automatically encrypt HTTP request payloads before reaching microservices."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Processing request bodies on application servers defeats the purpose of rate limiting, which is protecting downstream compute resources.",
      B: "Option B is correct! Evaluating rate limit rules at the API Gateway layer ensures rejected requests short-circuit immediately with HTTP 429, shielding downstream application servers and databases from handling unwanted load.",
      C: "Option C is incorrect. Any HTTP web server or gateway can generate standard 429 status codes.",
      D: "Option D is incorrect. Payload encryption is managed by TLS/SSL termination, independent of rate limiting."
    }
  },
  {
    id: 2,
    topic: "Fixed Window Edge Spike Flaw",
    question: "A fixed window rate limiter enforces a rule of '3 requests per minute'. A client sends 3 requests at 00:00:59 and 3 more requests at 00:01:01. Why does the fixed window counter fail to protect the system here?",
    options: {
      A: "Because minute boundaries are calculated using local server clock offsets instead of UTC.",
      B: "Because the counter resets at clock-minute boundaries (00:01:00), allowing 6 requests to pass within a 2-second burst across the window edge—double the configured limit.",
      C: "Because fixed window counters require 12 GB of RAM per user.",
      D: "Because HTTP pipelining merges adjacent requests into a single socket payload."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Clock synchronization (NTP) is important, but boundary resets are the fundamental flaw here.",
      B: "Option B is correct! Fixed windows align to clock boundaries. If a user bursts at the tail end of one minute and the beginning of the next, 6 requests pass within 2 seconds. Each window individually sees 3 requests, but the rolling 1-minute interval experiences double the allowed throughput.",
      C: "Option C is incorrect. Fixed window counters are lightweight, requiring only ~12 bytes per user (~32 MB for 1M users).",
      D: "Option D is incorrect. Network layer pipelining does not bypass application-level rate limit logic."
    }
  },
  {
    id: 3,
    topic: "Distributed Race Conditions & Atomicity",
    question: "In a distributed rate limiter cluster, two gateway nodes process concurrent requests for a user with current Count = 2 (limit is 3). Both nodes read Count = 2 simultaneously and increment to 3. How is this concurrency race condition natively resolved in Redis?",
    options: {
      A: "By executing atomic evaluation logic inside Redis using INCR commands with key expiry or custom Lua scripts, eliminating read-then-write race conditions without explicit distributed locks.",
      B: "By adding a 500 ms thread sleep delay before every counter read operation.",
      C: "By restarting Redis master instances whenever concurrent writes exceed 1,000 QPS.",
      D: "By converting integer counter fields into floating-point strings."
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! Non-atomic 'read-then-write' operations across distributed servers cause race conditions. Running evaluation logic inside Redis via atomic INCR commands or Lua scripts ensures check-and-update steps execute single-threaded atomically inside Redis without lock overhead.",
      B: "Option B is incorrect. Adding thread sleep delays increases latency significantly without guaranteeing race condition safety.",
      C: "Option C is incorrect. Restarting Redis instances causes cache loss and outage spikes.",
      D: "Option D is incorrect. Changing data types does not provide atomic isolation guarantees."
    }
  },
  {
    id: 4,
    topic: "Sliding Window Memory Footprint",
    question: "Why does a raw sliding window algorithm tracking individual request timestamps in a Redis Sorted Set (ZSET) consume ~12 GB of memory for 1M active users (at 500 req/hr), compared to ~32 MB for fixed window counters?",
    options: {
      A: "Because ZSET entries store raw HTML request payloads alongside each key.",
      B: "Because ZSET stores 500 individual timestamp entries per user, each carrying 64-bit prev/next pointers (~20 bytes overhead per element) plus hash table record overhead.",
      C: "Because Redis duplicates keys across all CPU cores when consistent hashing is enabled.",
      D: "Because epoch timestamps require 128-bit floating point representations."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. ZSETs store timestamps and scores, not HTML payloads.",
      B: "Option B is correct! A fixed window stores a single counter per user (12 bytes + overhead = 32 MB for 1M users). A sliding window ZSET stores every single request timestamp (500 entries × 24 bytes = ~12 KB/user), expanding total memory to ~12 GB for 1M users.",
      C: "Option C is incorrect. Consistent hashing partitions keys across nodes rather than duplicating them.",
      D: "Option D is incorrect. Epoch timestamps are standard 32-bit or 64-bit integers."
    }
  },
  {
    id: 5,
    topic: "Sliding Window with Sub-Counters",
    question: "How does the 'Sliding Window with Counters' algorithm cut memory consumption from ~12 GB to ~1.6 GB (~86% savings) for an hourly limit of 500 requests?",
    options: {
      A: "By compressing 64-bit UserIDs into 8-bit integers.",
      B: "By storing timestamps on client local storage instead of backend RAM.",
      C: "By dividing the 1-hour window into 60 1-minute sub-buckets inside a Redis Hash, tracking sub-counters rather than individual request timestamps.",
      D: "By executing gzip compression algorithms on Redis keys during every read request."
    },
    correct: "C",
    explanations: {
      A: "Option A is incorrect. Truncating UserIDs causes hash collisions across users.",
      B: "Option B is incorrect. Client storage cannot be trusted for rate limit enforcement.",
      C: "Option C is correct! Instead of keeping 500 timestamps per user, the system tracks at most 60 minute sub-counters in a Redis Hash. Summing these sub-counters provides sub-window precision while reducing memory footprint to ~1.6 KB per user (~1.6 GB for 1M users).",
      D: "Option D is incorrect. On-the-fly key decompression introduces high CPU overhead on the read path."
    }
  },
  {
    id: 6,
    topic: "Token Bucket Efficiency & Burstiness",
    question: "Why is the Token Bucket algorithm widely chosen for public-facing APIs over plain sliding windows?",
    options: {
      A: "It enforces strict zero-burst traffic policies at all times.",
      B: "It requires only ~12 bytes per user (Tokens count + LastRefillTimestamp) like fixed windows, while allowing natural client burstiness up to capacity without window-edge flaws.",
      C: "It automatically converts HTTP 429 responses into HTTP 200 OK responses.",
      D: "It eliminates the need for distributed Redis memory stores."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Leaky Bucket enforces smooth zero-burst traffic; Token Bucket explicitly permits bursts up to bucket capacity.",
      B: "Option B is correct! Token Bucket maintains minimal per-user state (~12 bytes: current token count and last refill timestamp). It accommodates legitimate bursty traffic up to bucket capacity while capping sustained rates to the refill rate without window-edge vulnerabilities.",
      C: "Option C is incorrect. Over-limit requests are rejected with 429 Too Many Requests.",
      D: "Option D is incorrect. State must still be persisted in a shared store like Redis."
    }
  },
  {
    id: 7,
    topic: "IP vs User ID Limiting Strategy",
    question: "Why is rate limiting strictly by IP address problematic for public APIs, and how does a Hybrid strategy solve it?",
    options: {
      A: "IP limiting breaks web browsers; hybrid limiting replaces IP addresses with MAC addresses.",
      B: "IP limiting throttles all users behind shared NAT gateways/proxies and is vulnerable to IPv6 subnet rotation; hybrid limiting applies IP limits to anonymous traffic and User ID limits to authenticated sessions.",
      C: "User ID limiting is incompatible with OAuth tokens; hybrid limiting relies solely on session cookies.",
      D: "IP limiting increases database write latency by 500 ms; hybrid limiting bypasses memory caching."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. MAC addresses are stripped at the local router and never reach public API gateways.",
      B: "Option B is correct! Shared NAT IPs (offices, carrier networks) cause false-positive throttling for innocent users, while IPv6 subnets enable attackers to rotate IPs endlessly. Hybrid strategies rate-limit anonymous endpoints by IP address (or IPv6 subnet) and authenticated requests by User ID / API Token.",
      C: "Option C is incorrect. User ID rate limiting works seamlessly with OAuth/JWT tokens.",
      D: "Option D is incorrect. Rate limiting operates in memory (Redis), introducing sub-millisecond latency."
    }
  },
  {
    id: 8,
    topic: "Fail-Open vs Fail-Closed Resilience",
    question: "During a major network partition, the central Redis counter cluster becomes unreachable from API Gateway nodes. Why do public API rate limiters typically default to a Fail-Open strategy?",
    options: {
      A: "Failing closed drops all legitimate client requests with HTTP 500/429 errors during Redis hiccups, causing complete API downtime, whereas failing open prioritizes core availability.",
      B: "Redis automatically restarts itself when configured to fail open.",
      C: "Failing open deletes corrupted database tables to free up memory.",
      D: "HTTP specifications forbid returning 429 status codes when backend databases fail."
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! For public APIs, rate limiting is a defense mechanism, not core business logic. If Redis fails, failing open allows requests through, keeping the main product online. Failing closed turns a rate-limiter outage into a complete site-wide outage.",
      B: "Option B is incorrect. Fail-open is gateway-level fallback logic, not a Redis self-healing feature.",
      C: "Option C is incorrect. Database deletion is never a fallback strategy.",
      D: "Option D is incorrect. HTTP specifications do not mandate fallback behavior."
    }
  },
  {
    id: 9,
    topic: "Offline AI Anomaly Detection",
    question: "Why are AI/ML models deployed offline to analyze access logs rather than evaluating incoming requests synchronously on the live enforcement path?",
    options: {
      A: "Because AI models cannot parse standard JSON request bodies.",
      B: "Synchronous AI evaluation adds prohibitive inference latency to every API call and lacks deterministic explainability (e.g. Retry-After headers), whereas offline AI detects complex attack patterns and safely updates rules.",
      C: "Because Redis Lua scripts cannot execute Python machine learning code.",
      D: "Because offline AI reduces cloud storage costs by 99%."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Machine learning models parse JSON payloads easily.",
      B: "Option B is correct! Synchronous ML model calls add tens of milliseconds of latency to every API request and cannot provide precise, explainable rate-limit headers (e.g. Retry-After, rule hit). Offline AI analyzes access logs asynchronously to detect botnets and recommend rule updates while online enforcement remains deterministic and fast.",
      C: "Option C is incorrect. While true technically, the core design reason is latency and explainability.",
      D: "Option D is incorrect. Storage savings are not the driver behind offline ML deployment."
    }
  }
];

let userAnswers = {};

function renderQuiz() {
  const container = document.getElementById('quiz-container');
  if (!container) return;

  container.innerHTML = quizQuestions.map((q, idx) => {
    return `
      <div class="quiz-card" id="quiz-card-${q.id}">
        <div class="quiz-card-header">
          <span class="quiz-topic">${q.topic}</span>
          <span class="quiz-qnum">Question ${idx + 1} of ${quizQuestions.length}</span>
        </div>
        <div class="quiz-question">${q.question}</div>
        <div class="quiz-options">
          ${Object.keys(q.options).map(optKey => `
            <button class="quiz-opt" id="opt-${q.id}-${optKey}" onclick="handleOptionClick(${q.id}, '${optKey}')">
              <span class="opt-letter">${optKey}</span>
              <span class="opt-text">${q.options[optKey]}</span>
            </button>
          `).join('')}
        </div>
        <div class="quiz-explanation" id="exp-${q.id}"></div>
      </div>
    `;
  }).join('');

  updateDashboard();
}

function handleOptionClick(questionId, selectedOpt) {
  const q = quizQuestions.find(item => item.id === questionId);
  if (!q) return;

  userAnswers[questionId] = selectedOpt;
  const isRight = (selectedOpt === q.correct);

  Object.keys(q.options).forEach(optKey => {
    const btn = document.getElementById(`opt-${questionId}-${optKey}`);
    if (!btn) return;

    btn.classList.remove('opt-correct', 'opt-incorrect');

    if (optKey === q.correct) {
      btn.classList.add('opt-correct');
    } else if (optKey === selectedOpt && !isRight) {
      btn.classList.add('opt-incorrect');
    }
  });

  const expDiv = document.getElementById(`exp-${questionId}`);
  if (expDiv) {
    expDiv.style.display = 'block';
    expDiv.className = `quiz-explanation ${isRight ? 'exp-right' : 'exp-wrong'}`;

    if (isRight) {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-check-circle" style="color: #16a34a;"></i> Correct Answer!
        </div>
        <div class="exp-body">
          ${q.explanations[selectedOpt]}
        </div>
      `;
    } else {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-times-circle" style="color: #dc2626;"></i> Incorrect Choice (Selected: ${selectedOpt})
        </div>
        <div class="exp-body">
          <p style="margin-bottom: 8px;"><strong>Why Option ${selectedOpt} is incorrect:</strong> ${q.explanations[selectedOpt]}</p>
          <p style="margin-top: 8px; margin-bottom: 0;"><strong>Correct Answer (${q.correct}):</strong> ${q.explanations[q.correct]}</p>
        </div>
      `;
    }
  }

  updateDashboard();
}

function updateDashboard() {
  const answeredCount = Object.keys(userAnswers).length;
  let correctCount = 0;

  Object.keys(userAnswers).forEach(qId => {
    const q = quizQuestions.find(item => item.id == qId);
    if (q && userAnswers[qId] === q.correct) {
      correctCount++;
    }
  });

  const total = quizQuestions.length;
  const accuracy = answeredCount > 0 ? Math.round((correctCount / answeredCount) * 100) : 0;
  const progressPercent = Math.round((answeredCount / total) * 100);

  document.getElementById('quiz-score').innerText = `${correctCount} / ${total}`;
  document.getElementById('quiz-progress').innerText = `${answeredCount} / ${total} Answered`;
  document.getElementById('quiz-accuracy').innerText = `${accuracy}%`;
  document.getElementById('quiz-progress-bar').style.width = `${progressPercent}%`;
}

function resetQuiz() {
  userAnswers = {};
  renderQuiz();
}

document.addEventListener('DOMContentLoaded', renderQuiz);
</script>
