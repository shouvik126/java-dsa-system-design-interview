---
layout: default
title: Quiz - Designing an API Rate Limiter
permalink: /System-Design/4-system-design-problems/4.11-quiz-designing-an-api-rate-limiter/
prev_title: "Designing an API Rate Limiter"
prev_url: "/System-Design/4-system-design-problems/4.11-designing-an-api-rate-limiter/"
---

# Quiz: Designing an API Rate Limiter

Test your architectural knowledge of API Rate Limiter system design with **9 scenario-driven interview questions** covering 10M QPS scale estimations, Fixed Window boundary bursts, atomic Redis Lua scripts, Sliding Window ZSET memory costs (~12 GB vs 32 MB), Sliding Window with Counters weighted formula, Token Bucket lazy refill mechanics, Gateway L1 cache shielding, IP vs User-ID hybrid security, and Fail-Open vs Fail-Closed AI offline rules enforcement.

---

<!-- Quiz Dashboard -->
<div class="quiz-dashboard">
  <div class="dash-stat">
    <span class="dash-label">Score</span>
    <span class="dash-value" id="quiz-score">0 / 9</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Progress</span>
    <span class="dash-value" id="quiz-progress">0 / 9 Answered</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Accuracy</span>
    <span class="dash-value" id="quiz-accuracy">0%</span>
  </div>
  <button class="quiz-reset-btn" onclick="resetQuiz()">
    <i class="fas fa-redo-alt"></i> Reset Quiz
  </button>
</div>

<div class="quiz-progress-bar-container">
  <div class="quiz-progress-bar" id="quiz-progress-bar" style="width: 0%;"></div>
</div>

<div id="quiz-container"></div>

<style>
/* Quiz Styling - Compatible with Light & Dark Themes */
.quiz-dashboard {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-secondary, #f8fafc);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 16px 24px;
  margin-bottom: 16px;
}

html.dark-theme .quiz-dashboard {
  background: #1e293b;
  border-color: #334155;
}

.dash-stat {
  display: flex;
  flex-direction: column;
}

.dash-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted, #64748b);
  font-weight: 600;
}

.dash-value {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-color, #0f172a);
}

html.dark-theme .dash-value {
  color: #f8fafc;
}

.quiz-reset-btn {
  background: #3b82f6;
  color: #ffffff;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, transform 0.1s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.quiz-reset-btn:hover {
  background: #2563eb;
  transform: translateY(-1px);
}

.quiz-progress-bar-container {
  width: 100%;
  height: 8px;
  background: var(--border-color, #e2e8f0);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 32px;
}

html.dark-theme .quiz-progress-bar-container {
  background: #334155;
}

.quiz-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #3b82f6, #10b981);
  transition: width 0.3s ease;
}

.quiz-card {
  background: var(--bg-color, #ffffff);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 28px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  transition: border-color 0.2s ease;
}

html.dark-theme .quiz-card {
  background: #0f172a;
  border-color: #1e293b;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

.quiz-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.quiz-topic {
  font-size: 0.82rem;
  font-weight: 600;
  color: #2563eb;
  background: rgba(37, 99, 235, 0.1);
  padding: 4px 10px;
  border-radius: 20px;
}

html.dark-theme .quiz-topic {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.15);
}

.quiz-qnum {
  font-size: 0.85rem;
  color: var(--text-muted, #64748b);
  font-weight: 500;
}

.quiz-question {
  font-size: 1.1rem;
  font-weight: 600;
  margin-top: 6px;
  margin-bottom: 20px;
  color: var(--text-color, #0f172a);
  line-height: 1.5;
}

html.dark-theme .quiz-question {
  color: #f1f5f9;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.quiz-opt {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  width: 100%;
  text-align: left;
  background: var(--bg-secondary, #f8fafc);
  border: 1.5px solid var(--border-color, #e2e8f0);
  border-radius: 10px;
  padding: 14px 16px;
  font-size: 0.95rem;
  color: var(--text-color, #334155);
  cursor: pointer;
  transition: all 0.2s ease;
}

html.dark-theme .quiz-opt {
  background: #1e293b;
  border-color: #334155;
  color: #cbd5e1;
}

.quiz-opt:hover:not(:disabled) {
  border-color: #3b82f6;
  background: rgba(59, 130, 246, 0.05);
}

html.dark-theme .quiz-opt:hover:not(:disabled) {
  border-color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
}

.opt-letter {
  font-weight: 700;
  background: var(--border-color, #cbd5e1);
  color: #0f172a;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  flex-shrink: 0;
  font-size: 0.85rem;
}

html.dark-theme .opt-letter {
  background: #475569;
  color: #f8fafc;
}

.opt-text {
  line-height: 1.45;
  flex-grow: 1;
}

/* Correct Option State */
.quiz-opt.opt-correct {
  background: #dcfce7 !important;
  border-color: #16a34a !important;
  color: #14532d !important;
}

.quiz-opt.opt-correct .opt-letter {
  background: #16a34a !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-correct {
  background: rgba(22, 163, 74, 0.2) !important;
  border-color: #22c55e !important;
  color: #bbf7d0 !important;
}

/* Incorrect Option State */
.quiz-opt.opt-incorrect {
  background: #fef2f2 !important;
  border-color: #dc2626 !important;
  color: #7f1d1d !important;
}

.quiz-opt.opt-incorrect .opt-letter {
  background: #dc2626 !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-incorrect {
  background: rgba(220, 38, 38, 0.2) !important;
  border-color: #ef4444 !important;
  color: #fecaca !important;
}

/* Explanation Box */
.quiz-explanation {
  margin-top: 18px;
  padding: 16px;
  border-radius: 10px;
  font-size: 0.93rem;
  line-height: 1.55;
  display: none;
}

.quiz-explanation.exp-right {
  background: #f0fdf4;
  border: 1px solid #86efac;
  color: #166534;
}

html.dark-theme .quiz-explanation.exp-right {
  background: rgba(22, 163, 74, 0.15);
  border-color: #22c55e;
  color: #bbf7d0;
}

.quiz-explanation.exp-wrong {
  background: #fef2f2;
  border: 1px solid #fca5a5;
  color: #991b1b;
}

html.dark-theme .quiz-explanation.exp-wrong {
  background: rgba(220, 38, 38, 0.15);
  border-color: #ef4444;
  color: #fecaca;
}

.exp-header {
  font-weight: 700;
  font-size: 1rem;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.exp-body {
  margin-top: 6px;
}
</style>

<script>
const quizQuestions = [
  {
    id: 1,
    topic: "Fixed Window Counter & Boundary Spikes",
    question: "In a Fixed Window Rate Limiter configured for a limit of 100 requests per minute (aligned to minute boundaries), an attacker sends 100 requests at 12:00:59 and another 100 requests at 12:01:01. Both windows process all 100 requests successfully. What is the fundamental flaw of this algorithm, and how many requests were processed across that 2-second boundary?",
    options: {
      A: "The algorithm failed because minute counters must be encrypted; 100 requests were processed.",
      B: "The fixed window resets at clock boundaries, allowing 200 total requests to pass across a 2-second interval surrounding the minute edge, effectively doubling the intended maximum request limit.",
      C: "The algorithm allowed only 100 requests because Redis automatically drops duplicate requests sent within 2 seconds.",
      D: "The fixed window failed to record HTTP 429 status codes, processing 0 requests."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Counter resetting at clock boundaries is a structural logic flaw, not an encryption issue.",
      B: "Option B is correct! Because the counter resets at the minute boundary (`12:01:00`), 100 requests pass at `12:00:59` (Window 1) and another 100 pass at `12:01:01` (Window 2). Across the 2-second window surrounding the minute edge, 200 requests were processed—double the quota of 100 req/min!",
      C: "Option C is incorrect. Fixed window counters evaluate windows independently; they do not drop valid requests arriving in a fresh window.",
      D: "Option D is incorrect. Both batches of 100 requests were allowed within their respective fixed windows."
    }
  },
  {
    id: 2,
    topic: "Distributed Race Conditions & Redis Lua Scripts",
    question: "When multiple API Gateway servers concurrently check and update a shared rate limit counter in Redis for the same user, a 'read-then-write' race condition can allow excess requests. Why are distributed locks (such as Redis Redlock) avoided in favor of atomic Redis Lua scripts to solve this problem?",
    options: {
      A: "Because Redis Lua scripts disable all network firewalls on application servers.",
      B: "Because distributed locks introduce unacceptable network round-trips and locking latency on the critical request path, whereas Redis Lua scripts execute atomically and single-threaded inside Redis in sub-milliseconds without locks.",
      C: "Because Redis Redlock cannot store integer values greater than 10.",
      D: "Because Memcached automatically converts Redis locks into SQL queries."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Lua scripts run within the Redis engine; they do not touch host network firewalls.",
      B: "Option B is correct! Distributed locks require acquiring and releasing locks across network nodes, adding several milliseconds of latency to every single API request. Because Redis executes Lua scripts atomically in a single thread, checking and incrementing counters inside a Lua script eliminates race conditions with zero locking overhead.",
      C: "Option C is incorrect. Redlock is a distributed locking consensus algorithm, not a restricted integer data type.",
      D: "Option D is incorrect. Memcached and Redis are independent in-memory key-value stores with no SQL conversion relationship."
    }
  },
  {
    id: 3,
    topic: "Sliding Window Memory & Scale Estimation",
    question: "A system architect evaluates a pure Sliding Window algorithm using Redis Sorted Sets (ZSET) to store Unix timestamps for 1 Million active users, with a quota limit of 500 requests per hour. Why does this exact algorithm require ~12 GB of RAM compared to ~32 MB for a Fixed Window Counter?",
    options: {
      A: "Because Redis Sorted Sets double the IP packet payload size of every HTTP response.",
      B: "Because pure Sliding Window stores individual timestamp logs (~24 bytes per entry for timestamp + ZSET pointers) for all 500 allowed requests per user (~12 KB/user), whereas Fixed Window stores only a single 12-byte integer record per user (~32 B/user).",
      C: "Because 1 Million active users require 12 GB of GPU VRAM for video rendering.",
      D: "Because Fixed Window counters compress timestamps using lossy JPEG compression."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. In-memory data structures affect server RAM storage, not outgoing HTTP response packet header sizes.",
      B: "Option B is correct! Fixed Window stores only `Count` and `StartTime` (~32 bytes per user with hash overhead = 32 MB for 1M users). Pure Sliding Window maintains every request timestamp in a Sorted Set (`8B key + 500 entries * 24B ZSET overhead ≈ 12 KB` per user), requiring ~12 GB of RAM for 1M users (a 375x increase).",
      C: "Option C is incorrect. Rate limit counters reside in system server RAM, not GPU VRAM.",
      D: "Option D is incorrect. Numerical integers and timestamps are stored as primitive scalar values, not JPEG images."
    }
  },
  {
    id: 4,
    topic: "Sliding Window with Counters & Weighted Formula",
    question: "Under a Sliding Window with Counters algorithm (1-hour window limit of 100 requests), a user made 80 requests in the previous 1-minute bucket. Currently, 18 seconds have elapsed in the current 1-minute bucket (30% elapsed), and 10 requests have been logged so far. Using the weighted counter formula, what is the estimated request count, and is the current request allowed?",
    options: {
      A: "Estimated Count = 90; Request is throttled (HTTP 429).",
      B: "Estimated Count = 66; Request is allowed, as 66 is well below the quota limit of 100.",
      C: "Estimated Count = 80; Request is throttled because previous bucket was 80.",
      D: "Estimated Count = 18; Request is allowed because elapsed time is under 30 seconds."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. 90 results from adding 80 + 10 without applying the 70% weight decay to the previous bucket.",
      B: "Option B is correct! The formula is `Current_Count + Previous_Count * (1 - Elapsed_Ratio)`. Here, `10 + 80 * (1 - 18/60) = 10 + 80 * 0.70 = 10 + 56 = 66`. Since 66 < 100, the request is allowed!",
      C: "Option C is incorrect. The previous bucket count is weighted dynamically by remaining window percentage (70%), not taken raw.",
      D: "Option D is incorrect. 18 is the elapsed seconds, not the weighted count calculation."
    }
  },
  {
    id: 5,
    topic: "Token Bucket vs. Leaky Bucket Algorithms",
    question: "Why is the Token Bucket algorithm preferred over the Leaky Bucket algorithm for public-facing developer APIs (like Stripe, AWS, or GitHub), despite both being memory-efficient?",
    options: {
      A: "Because Leaky Bucket requires storing data in relational SQL tables, while Token Bucket runs in browser cookies.",
      B: "Because Token Bucket permits clients to execute bursty traffic up to bucket capacity C while enforcing average refill rate r, whereas Leaky Bucket forces requests through a rigid FIFO queue at a constant output rate, delaying or dropping legitimate bursty API calls.",
      C: "Because Token Bucket automatically encrypts all credit card transactions.",
      D: "Because Leaky Bucket cannot handle more than 1 request per hour."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Both algorithms use in-memory key-value data structures in backend caches.",
      B: "Option B is correct! Real-world client application traffic is naturally bursty (e.g. loading a dashboard page triggers 5 parallel API calls). Token Bucket permits these immediate bursts up to capacity C. Leaky Bucket smooths out traffic into a rigid constant flow rate, causing unnecessary queuing latency for bursty web clients.",
      C: "Option C is incorrect. Token Bucket manages request throughput quotas, not payload encryption.",
      D: "Option D is incorrect. Leaky Bucket can process any configured leak rate, but smooths bursts indiscriminately."
    }
  },
  {
    id: 6,
    topic: "Token Bucket Lazy Refill Mechanics",
    question: "How does a production-grade Token Bucket rate limiter avoid running millions of background timer threads to constantly add tokens to user buckets every second?",
    options: {
      A: "It delegates token updates to client-side JavaScript execution in the user's browser.",
      B: "It uses Lazy Refill (on-demand calculation): when a request arrives, it dynamically calculates `tokens_to_add = (now - last_refill_time) * refill_rate` and updates the state atomically in a single Redis write.",
      C: "It shuts down the API Gateway whenever token counts drop below 50%.",
      D: "It forces users to manually solve a CAPTCHA to generate tokens."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Client-side state can be easily spoofed or bypassed by malicious attackers.",
      B: "Option B is correct! Active background threads for millions of users would burn massive CPU cycles. Lazy Refill calculates token accumulation lazily *only when a request hits the gateway*: `new_tokens = min(capacity, current_tokens + elapsed_seconds * refill_rate)`. This requires only ~16 bytes of state (`tokens`, `last_refill_time`) per user!",
      C: "Option C is incorrect. Gateways must remain online to serve traffic within quota.",
      D: "Option D is incorrect. CAPTCHAs are anti-bot challenges, not background token calculation optimizations."
    }
  },
  {
    id: 7,
    topic: "Distributed Scaling & Local Gateway Caching",
    question: "In a system processing 10 Million QPS across a cluster of Redis shards, how does combining Consistent Hashing with an in-memory L1 LRU cache at the API Gateway protect the Redis cluster during a massive Denial-of-Service (DoS) attack?",
    options: {
      A: "Consistent Hashing encrypts incoming IP addresses, while L1 cache deletes all Redis database partitions.",
      B: "Consistent Hashing evenly distributes entity keys across Redis shards, while the Gateway L1 LRU memory caches 'throttled' decisions so subsequent blocked requests from abusive callers are dropped instantly in Gateway RAM without emitting network calls to Redis.",
      C: "L1 LRU cache forces all blocked users to re-authenticate via OAuth 2.0.",
      D: "Consistent Hashing routes all 10 Million QPS to a single primary database node."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. L1 caching protects backend datastores; it does not delete database partitions.",
      B: "Option B is correct! Consistent Hashing partitions keys uniformly across Redis nodes. When an abusive entity crosses its limit, the API Gateway remembers the `throttled` decision in local L1 node RAM. For the remainder of the window, blocked requests are dropped in Gateway RAM in sub-microseconds, shielding Redis network sockets from flood exhaustion!",
      C: "Option C is incorrect. Rate limiting returns HTTP 429 status, not authentication redirect flows.",
      D: "Option D is incorrect. Consistent Hashing spreads load evenly across shards, specifically preventing single-node bottlenecks."
    }
  },
  {
    id: 8,
    topic: "Entity Identification & Security Vulnerabilities",
    question: "An engineer proposes rate limiting the `/login` endpoint strictly by User ID (email address) to prevent password guessing attacks. What severe security vulnerability does this introduce, and how does a hybrid approach resolve it?",
    options: {
      A: "It causes memory leaks in client web browsers; the hybrid approach disables password hashing.",
      B: "Limiting `/login` by User ID allows an attacker to intentionally submit wrong passwords for a victim's email address to trigger rate limits and lock the legitimate account owner out (Account Lockout DoS). A hybrid approach applies IP/subnet limits to pre-auth endpoints and User-ID limits to post-auth endpoints.",
      C: "User ID rate limiting causes Redis to delete all user profile photos.",
      D: "The hybrid approach converts all HTTP POST requests into GET requests."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Password hashing is a core cryptographic safeguard, unrelated to rate limiter entity selection.",
      B: "Option B is correct! If `/login` rate limits per email, an attacker can flood incorrect logins for target accounts to cause intentional Denial-of-Service against victims. The hybrid strategy uses IP (and IPv6 subnet) rate limiting for pre-authentication routes (`/login`, `/register`), and User ID / API Key rate limiting for authenticated routes (`/dashboard`).",
      C: "Option C is incorrect. Entity key selection dictates counter lookup formatting, not database blob deletion.",
      D: "Option D is incorrect. HTTP methods are governed by REST protocol design, not rate limiting entity selection."
    }
  },
  {
    id: 9,
    topic: "Fail-Open vs. Fail-Closed & AI Architectural Role",
    question: "How should a rate limiter system handle a total Redis cluster outage for a public web API versus a payment processing endpoint, and what is AI's role in this architecture?",
    options: {
      A: "Both endpoints must Fail-Closed; AI models execute inline HTTP header validation on every API request.",
      B: "Public web APIs should Fail-Open to preserve service availability, while payment processing endpoints should Fail-Closed to prevent financial abuse. AI operates offline asynchronously analyzing access logs to dynamically update rate limit rules ('AI Advises, Rules Enforce').",
      C: "Public web APIs should Fail-Closed, while payment endpoints Fail-Open; AI replaces Redis with neural network weights.",
      D: "Both endpoints Fail-Open; AI models automatically grant unlimited API quota to all anonymous IP addresses."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Placing AI model inference inline on every request adds 20-100+ ms of unacceptable latency.",
      B: "Option B is correct! In a cache failure, public web APIs Fail-Open (allow traffic) to avoid total user downtime, while sensitive payment routes Fail-Closed (block traffic) to prevent fraud. AI operates asynchronously offline over log streams (Kafka) to detect complex botnet patterns and update configuration rules ('AI Advises, Rules Enforce').",
      C: "Option C is incorrect. Allowing un-rate-limited payment processing during outages invites catastrophic financial abuse.",
      D: "Option D is incorrect. AI anomaly detection tightens security against abusive bots, rather than granting anonymous floods unlimited access."
    }
  }
];

let userAnswers = {};

function renderQuiz() {
  const container = document.getElementById('quiz-container');
  if (!container) return;

  container.innerHTML = quizQuestions.map((q, idx) => {
    return `
      <div class="quiz-card" id="quiz-card-${q.id}">
        <div class="quiz-card-header">
          <span class="quiz-topic">${q.topic}</span>
          <span class="quiz-qnum">Question ${idx + 1} of ${quizQuestions.length}</span>
        </div>
        <div class="quiz-question">${q.question}</div>
        <div class="quiz-options">
          ${Object.keys(q.options).map(optKey => `
            <button class="quiz-opt" id="opt-${q.id}-${optKey}" onclick="handleOptionClick(${q.id}, '${optKey}')">
              <span class="opt-letter">${optKey}</span>
              <span class="opt-text">${q.options[optKey]}</span>
            </button>
          `).join('')}
        </div>
        <div class="quiz-explanation" id="exp-${q.id}"></div>
      </div>
    `;
  }).join('');

  updateDashboard();
}

function handleOptionClick(questionId, selectedOpt) {
  const q = quizQuestions.find(item => item.id === questionId);
  if (!q) return;

  userAnswers[questionId] = selectedOpt;
  const isRight = (selectedOpt === q.correct);

  Object.keys(q.options).forEach(optKey => {
    const btn = document.getElementById(`opt-${questionId}-${optKey}`);
    if (!btn) return;

    btn.classList.remove('opt-correct', 'opt-incorrect');

    if (optKey === q.correct) {
      btn.classList.add('opt-correct');
    } else if (optKey === selectedOpt && !isRight) {
      btn.classList.add('opt-incorrect');
    }
  });

  const expDiv = document.getElementById(`exp-${questionId}`);
  if (expDiv) {
    expDiv.style.display = 'block';
    expDiv.className = `quiz-explanation ${isRight ? 'exp-right' : 'exp-wrong'}`;

    if (isRight) {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-check-circle" style="color: #16a34a;"></i> Correct Answer!
        </div>
        <div class="exp-body">
          ${q.explanations[selectedOpt]}
        </div>
      `;
    } else {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-times-circle" style="color: #dc2626;"></i> Incorrect Choice (Selected: ${selectedOpt})
        </div>
        <div class="exp-body">
          <p style="margin-bottom: 8px;"><strong>Why Option ${selectedOpt} is incorrect:</strong> ${q.explanations[selectedOpt]}</p>
          <p style="margin-top: 8px; margin-bottom: 0;"><strong>Correct Answer (${q.correct}):</strong> ${q.explanations[q.correct]}</p>
        </div>
      `;
    }
  }

  updateDashboard();
}

function updateDashboard() {
  const answeredCount = Object.keys(userAnswers).length;
  let correctCount = 0;

  Object.keys(userAnswers).forEach(qId => {
    const q = quizQuestions.find(item => item.id == qId);
    if (q && userAnswers[qId] === q.correct) {
      correctCount++;
    }
  });

  const total = quizQuestions.length;
  const accuracy = answeredCount > 0 ? Math.round((correctCount / answeredCount) * 100) : 0;
  const progressPercent = Math.round((answeredCount / total) * 100);

  document.getElementById('quiz-score').innerText = `${correctCount} / ${total}`;
  document.getElementById('quiz-progress').innerText = `${answeredCount} / ${total} Answered`;
  document.getElementById('quiz-accuracy').innerText = `${accuracy}%`;
  document.getElementById('quiz-progress-bar').style.width = `${progressPercent}%`;
}

function resetQuiz() {
  userAnswers = {};
  renderQuiz();
}

document.addEventListener('DOMContentLoaded', renderQuiz);
</script>
