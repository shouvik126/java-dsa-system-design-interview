---
layout: default
title: Quiz - Designing Twitter
permalink: /System-Design/4-system-design-problems/4.8-quiz-designing-twitter/
prev_title: "Designing Twitter"
prev_url: "/System-Design/4-system-design-problems/4.8-designing-twitter/"
next_title: "Designing YouTube or Netflix"
next_url: "/System-Design/4-system-design-problems/4.9-designing-youtube-or-netflix/"
---

# Quiz: Designing Twitter

Test your architectural knowledge of Twitter system design with **9 scenario-driven interview questions** covering 280:1 read-heavy asymmetry, 64-bit Snowflake TweetIDs, TweetID vs UserID sharding, 3-day in-memory caching, hybrid push/pull timeline fan-out, media decoupling, and two-stage AI timeline ranking.

---

<!-- Quiz Dashboard -->
<div class="quiz-dashboard">
  <div class="dash-stat">
    <span class="dash-label">Score</span>
    <span class="dash-value" id="quiz-score">0 / 9</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Progress</span>
    <span class="dash-value" id="quiz-progress">0 / 9 Answered</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Accuracy</span>
    <span class="dash-value" id="quiz-accuracy">0%</span>
  </div>
  <button class="quiz-reset-btn" onclick="resetQuiz()">
    <i class="fas fa-redo-alt"></i> Reset Quiz
  </button>
</div>

<div class="quiz-progress-bar-container">
  <div class="quiz-progress-bar" id="quiz-progress-bar" style="width: 0%;"></div>
</div>

<div id="quiz-container"></div>

<style>
/* Quiz Styling - Compatible with Light & Dark Themes */
.quiz-dashboard {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-secondary, #f8fafc);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 16px 24px;
  margin-bottom: 16px;
}

html.dark-theme .quiz-dashboard {
  background: #1e293b;
  border-color: #334155;
}

.dash-stat {
  display: flex;
  flex-direction: column;
}

.dash-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted, #64748b);
  font-weight: 600;
}

.dash-value {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-color, #0f172a);
}

html.dark-theme .dash-value {
  color: #f8fafc;
}

.quiz-reset-btn {
  background: #3b82f6;
  color: #ffffff;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, transform 0.1s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.quiz-reset-btn:hover {
  background: #2563eb;
  transform: translateY(-1px);
}

.quiz-progress-bar-container {
  width: 100%;
  height: 8px;
  background: var(--border-color, #e2e8f0);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 32px;
}

html.dark-theme .quiz-progress-bar-container {
  background: #334155;
}

.quiz-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #3b82f6, #10b981);
  transition: width 0.3s ease;
}

.quiz-card {
  background: var(--bg-color, #ffffff);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 28px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  transition: border-color 0.2s ease;
}

html.dark-theme .quiz-card {
  background: #0f172a;
  border-color: #1e293b;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

.quiz-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.quiz-topic {
  font-size: 0.82rem;
  font-weight: 600;
  color: #2563eb;
  background: rgba(37, 99, 235, 0.1);
  padding: 4px 10px;
  border-radius: 20px;
}

html.dark-theme .quiz-topic {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.15);
}

.quiz-qnum {
  font-size: 0.85rem;
  color: var(--text-muted, #64748b);
  font-weight: 500;
}

.quiz-question {
  font-size: 1.1rem;
  font-weight: 600;
  margin-top: 6px;
  margin-bottom: 20px;
  color: var(--text-color, #0f172a);
  line-height: 1.5;
}

html.dark-theme .quiz-question {
  color: #f1f5f9;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.quiz-opt {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  width: 100%;
  text-align: left;
  background: var(--bg-secondary, #f8fafc);
  border: 1.5px solid var(--border-color, #e2e8f0);
  border-radius: 10px;
  padding: 14px 16px;
  font-size: 0.95rem;
  color: var(--text-color, #334155);
  cursor: pointer;
  transition: all 0.2s ease;
}

html.dark-theme .quiz-opt {
  background: #1e293b;
  border-color: #334155;
  color: #cbd5e1;
}

.quiz-opt:hover:not(:disabled) {
  border-color: #3b82f6;
  background: rgba(59, 130, 246, 0.05);
}

html.dark-theme .quiz-opt:hover:not(:disabled) {
  border-color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
}

.opt-letter {
  font-weight: 700;
  background: var(--border-color, #cbd5e1);
  color: #0f172a;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  flex-shrink: 0;
  font-size: 0.85rem;
}

html.dark-theme .opt-letter {
  background: #475569;
  color: #f8fafc;
}

.opt-text {
  line-height: 1.45;
  flex-grow: 1;
}

/* Correct Option State */
.quiz-opt.opt-correct {
  background: #dcfce7 !important;
  border-color: #16a34a !important;
  color: #14532d !important;
}

.quiz-opt.opt-correct .opt-letter {
  background: #16a34a !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-correct {
  background: rgba(22, 163, 74, 0.2) !important;
  border-color: #22c55e !important;
  color: #bbf7d0 !important;
}

/* Incorrect Option State */
.quiz-opt.opt-incorrect {
  background: #fef2f2 !important;
  border-color: #dc2626 !important;
  color: #7f1d1d !important;
}

.quiz-opt.opt-incorrect .opt-letter {
  background: #dc2626 !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-incorrect {
  background: rgba(220, 38, 38, 0.2) !important;
  border-color: #ef4444 !important;
  color: #fecaca !important;
}

/* Explanation Box */
.quiz-explanation {
  margin-top: 18px;
  padding: 16px;
  border-radius: 10px;
  font-size: 0.93rem;
  line-height: 1.55;
  display: none;
}

.quiz-explanation.exp-right {
  background: #f0fdf4;
  border: 1px solid #86efac;
  color: #166534;
}

html.dark-theme .quiz-explanation.exp-right {
  background: rgba(22, 163, 74, 0.15);
  border-color: #22c55e;
  color: #bbf7d0;
}

.quiz-explanation.exp-wrong {
  background: #fef2f2;
  border: 1px solid #fca5a5;
  color: #991b1b;
}

html.dark-theme .quiz-explanation.exp-wrong {
  background: rgba(220, 38, 38, 0.15);
  border-color: #ef4444;
  color: #fecaca;
}

.exp-header {
  font-weight: 700;
  font-size: 1rem;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.exp-body {
  margin-top: 6px;
}
</style>

<script>
const quizQuestions = [
  {
    id: 1,
    topic: "Read-Heavy Asymmetry & Caching Strategy",
    question: "Twitter handles ~325,000 read QPS against ~1,157 write QPS (a 280:1 read-to-write ratio). Applying the 80/20 Pareto principle and time-window caching, why is an in-memory cache holding the last 3 days of tweets (~90 GB RAM) sufficient to serve over 80-90% of all timeline read queries?",
    options: {
      A: "Because 90 GB of RAM automatically compresses all historical tweets using lossy video compression.",
      B: "Because over 80% of active users view tweets published within the last 3 days, and 30 GB/day of daily text metadata requires only 90 GB across the cluster, keeping the vast majority of active read requests in memory.",
      C: "Because caching tweets for 3 days forces all secondary database shards to turn off disk logging.",
      D: "Because Memcached cannot hold data for longer than 72 hours due to HTTP header limitations."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Text metadata is stored uncompressed or with lossless memory structures, not lossy video compression.",
      B: "Option B is correct! Applying Pareto's 80/20 rule, recent content generates the overwhelming majority of views. Since daily text and metadata grow by ~30 GB/day, a 3-day window totals ~90 GB, which easily fits in a replicated RAM cache tier, offloading >80-90% of reads from underlying database partitions.",
      C: "Option C is incorrect. In-memory caching sits in front of database shards; it does not disable database disk persistence.",
      D: "Option D is incorrect. In-memory stores like Memcached/Redis support arbitrary TTL key expirations independent of HTTP headers."
    }
  },
  {
    id: 2,
    topic: "Database Partitioning (UserID vs. TweetID)",
    question: "When sharding Twitter's database holding billions of tweets across multiple nodes, why is sharding by TweetID (`hash(TweetID) % total_shards`) preferred over sharding by UserID, despite the scatter-gather latency cost during timeline generation?",
    options: {
      A: "Sharding by UserID causes all incoming tweets from every user to write to a single primary database node.",
      B: "Sharding by UserID causes the 'Hot User' problem where high-profile celebrity accounts concentrate massive read/write traffic onto a single shard, whereas TweetID sharding uniformly distributes traffic across all partitions.",
      C: "Sharding by TweetID eliminates the need for load balancers across application servers.",
      D: "Sharding by UserID prevents databases from executing SQL join statements."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Hashing `UserID` distributes users across shards, but puts all data for any *one* user on one shard.",
      B: "Option B is correct! Under UserID sharding, a celebrity account (e.g. 50M followers) creates extreme traffic hotspots on whichever shard holds their profile. TweetID sharding distributes posts uniformly across all shards. The resulting scatter-gather read latency is absorbed by placing a 3-day in-memory cache layer in front of the database shards.",
      C: "Option C is incorrect. Database partitioning is completely independent of front-end web application load balancing.",
      D: "Option D is incorrect. Joins depend on database engine capabilities, not solely on partition key selection."
    }
  },
  {
    id: 3,
    topic: "Distributed Key Generation & Index Optimization",
    question: "How does embedding a 41-bit millisecond timestamp directly inside Twitter's 64-bit Snowflake `TweetID` optimize write throughput on database shards compared to traditional auto-incrementing IDs?",
    options: {
      A: "It enables primary key range scans to return chronologically ordered records directly, eliminating the overhead of maintaining a secondary B-Tree index on `CreationTime` during inserts.",
      B: "It reduces the size of every tweet text payload from 140 characters down to 8 bytes.",
      C: "It forces database replicas to synchronously block until all 1,024 generator nodes acknowledge the write.",
      D: "It encrypts the TweetID using 256-bit AES encryption."
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! Maintaining a secondary B-Tree index on a separate `CreationTime` column requires updating index trees on disk during every single write, causing write-amplification. Because 64-bit Snowflake `TweetID`s are natively time-ordered, the primary key itself orders tweets by time, eliminating secondary index maintenance.",
      B: "Option B is incorrect. The TweetID is an 8-byte primary key identifier, not a compressor for the tweet text body.",
      C: "Option C is incorrect. Snowflake generators assign machine IDs to generate keys independently without inter-node locks or synchronous coordination.",
      D: "Option D is incorrect. Snowflake IDs are structured numeric bitfields (timestamp + worker ID + sequence), not encrypted ciphertext."
    }
  },
  {
    id: 4,
    topic: "Timeline Generation (Push vs. Pull Model)",
    question: "Why does a pure Push (fan-out on write) architecture fail for celebrity accounts, and how does Twitter's Hybrid timeline model resolve this bottleneck?",
    options: {
      A: "Celebrity accounts cannot send HTTP POST requests; the hybrid model converts celebrity tweets into SMS messages.",
      B: "When a celebrity with 50 million followers posts a tweet, a pure push model requires executing 50 million write operations into follower inbox caches (write amplification); the hybrid model uses push for standard users and pull (merge on read) for high-follower accounts.",
      C: "The pure push model forces clients to download video attachments before rendering text.",
      D: "The hybrid model stores celebrity tweets exclusively in browser local storage."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. System models deal with fan-out delivery mechanics, not SMS fallback for POST APIs.",
      B: "Option B is correct! Delivering one post to 50M follower cache inboxes causes massive write amplification and queue latency. The hybrid model fans out tweets for standard accounts (< 10k followers) upon posting (push), but leaves celebrity tweets in author stores to be merged on-demand when followers refresh their feeds (pull).",
      C: "Option C is incorrect. Fan-out logic manages Tweet IDs in feed timelines, decoupled from binary asset downloading.",
      D: "Option D is incorrect. Feeds are pre-computed in server-side memory caches (Redis/Memcached), not client local storage."
    }
  },
  {
    id: 5,
    topic: "REST API Design & Media Asset Decoupling",
    question: "Why does Twitter's post creation endpoint `tweet(api_dev_key, tweet_data, media_ids)` accept pre-assigned `media_ids` rather than uploading binary photo/video files directly inside the post request?",
    options: {
      A: "Because REST APIs do not support transferring strings longer than 140 bytes.",
      B: "Decoupling binary uploads keeps the post API payload small, prevents heavy file transfers from holding open web worker connections, and allows independent retry logic if a media upload fails.",
      C: "Because media files stored in database rows automatically corrupt primary key indexes.",
      D: "Because HTTP load balancers can only route JSON payloads that contain no images."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. HTTP POST endpoints handle multi-megabyte payloads easily.",
      B: "Option B is correct! Uploading large media files (up to 2 MB videos or 200 KB photos) directly inside the post request would tie up stateless application server threads. Pre-uploading media to blob storage returns lightweight `media_ids`, keeping the post creation call fast, atomic, and resilient.",
      C: "Option C is incorrect. Binary media is kept in object storage (S3/GCS/CDN) for performance and cost reasons, not index corruption.",
      D: "Option D is incorrect. Load balancers route arbitrary binary and multipart HTTP payloads."
    }
  },
  {
    id: 6,
    topic: "In-Memory Cache Structure & Eviction Policy",
    question: "How is a user's recent tweet feed organized inside Twitter's in-memory cache layer, and which eviction policy is applied when memory bounds are reached?",
    options: {
      A: "Organized as a B+ Tree indexed by UserID; evicted using First-In-First-Out (FIFO).",
      B: "Organized as a Hash Table mapping `OwnerID` to a Doubly Linked List of recent Tweet objects; evicted using Least Recently Used (LRU) to discard tail items while keeping active content in memory.",
      C: "Organized as an unindexed flat text file; evicted using Random Early Detection (RED).",
      D: "Organized as a relational table; evicted using round-robin row deletion."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Doubly linked lists are used over B+ trees for $O(1)$ push and tail eviction performance in memory caches.",
      B: "Option B is correct! A Hash Table with a Doubly Linked List allows $O(1)$ prepending of new tweets at the head and $O(1)$ removal at the tail. LRU eviction ensures that active, frequently viewed tweets stay in memory while stale posts drop out when the 90 GB limit is reached.",
      C: "Option C is incorrect. In-memory caching relies on key-value data structures, not unindexed flat text files.",
      D: "Option D is incorrect. Relational tables live in database storage tiers, not in-memory cache replacement structures."
    }
  },
  {
    id: 7,
    topic: "CAP Theorem & Consistency Trade-Offs",
    question: "Twitter's system requirements mandate Eventual Consistency over Immediate Strong Consistency. What is the practical architectural benefit of this choice during timeline delivery?",
    options: {
      A: "It allows database partitions to bypass hardware RAID disk redundancy.",
      B: "It relaxes immediate cross-node synchronization, enabling reads to be served instantly from memory caches under a < 200 ms SLA even if a new tweet takes a few seconds to propagate across all follower feeds.",
      C: "It guarantees that no user will ever experience cache misses during breaking news events.",
      D: "It automatically encrypts all database backups with zero CPU overhead."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Eventual consistency is a software replication and state agreement model, unrelated to hardware RAID.",
      B: "Option B is correct! Under the CAP theorem, choosing High Availability and low latency (< 200 ms) means accepting eventual consistency. Users prefer immediate timeline rendering from local cache replicas over waiting for synchronous multi-region database locks to guarantee instant global visibility.",
      C: "Option C is incorrect. Cache misses still occur if content expires or is evicted under high load.",
      D: "Option D is incorrect. Encryption is a security feature, not a CAP consistency model trade-off."
    }
  },
  {
    id: 8,
    topic: "Database Replication & High Availability",
    question: "How does each database shard in Twitter maintain high availability and absorb 325,000 read QPS without overwhelming the Primary database node?",
    options: {
      A: "By using Primary-Replica (Master-Slave) replication where write operations target the Primary node and replicate asynchronously to multiple Replica nodes that handle all timeline read traffic.",
      B: "By requiring every client web browser to host a database replica inside WebAssembly.",
      C: "By converting all read queries into DNS lookup requests.",
      D: "By locking the Primary node whenever a read query is executed."
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! To scale read throughput, writes hit the Primary node while read traffic is distributed across multiple Read Replicas. If a Primary node fails, an automated consensus coordinator (e.g. ZooKeeper) promotes a healthy Replica to Primary.",
      B: "Option B is incorrect. Database replicas live on server clusters in datacenters, not in client browser WebAssembly sandboxes.",
      C: "Option C is incorrect. DNS resolves domain IP addresses, not database query results.",
      D: "Option D is incorrect. Locking the Primary node on reads would destroy read availability and severely increase latency."
    }
  },
  {
    id: 9,
    topic: "Two-Stage Architecture for AI-Ranked Timelines",
    question: "When transitioning from a reverse-chronological timeline to an AI-ranked feed, how does the system incorporate Machine Learning ranking models without overloading backend database infrastructure?",
    options: {
      A: "The AI model executes full-table SQL scans over the entire 50-terabyte historical database for every page scroll.",
      B: "The system uses a Two-Stage Architecture: Stage 1 (Candidate Retrieval) uses fast systems caching/sharding to pull ~500 recent tweets, and Stage 2 (ML Ranking) reorders only that candidate pool based on predicted engagement scores.",
      C: "The AI model runs directly inside client mobile CPUs before sending requests to the server.",
      D: "The system replaces database shards with raw machine learning neural network weights."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Running full-table scans over tens of terabytes per request would cause instant system collapse.",
      B: "Option B is correct! To rank feeds efficiently, systems engineering decouples candidate retrieval from ranking. Stage 1 pulls ~500 candidate tweets from followed accounts in milliseconds using existing caches. Stage 2 passes this small candidate set to ML models (e.g. neural nets/GBDTs) to score and sort posts by predicted engagement.",
      C: "Option C is incorrect. Ranking models consume significant server-side features (engagement history, user graph signals) and run in the backend tier.",
      D: "Option D is incorrect. Neural networks perform ranking predictions; they do not replace persistent storage database engines."
    }
  }
];

let userAnswers = {};

function renderQuiz() {
  const container = document.getElementById('quiz-container');
  if (!container) return;

  container.innerHTML = quizQuestions.map((q, idx) => {
    return `
      <div class="quiz-card" id="quiz-card-${q.id}">
        <div class="quiz-card-header">
          <span class="quiz-topic">${q.topic}</span>
          <span class="quiz-qnum">Question ${idx + 1} of ${quizQuestions.length}</span>
        </div>
        <div class="quiz-question">${q.question}</div>
        <div class="quiz-options">
          ${Object.keys(q.options).map(optKey => `
            <button class="quiz-opt" id="opt-${q.id}-${optKey}" onclick="handleOptionClick(${q.id}, '${optKey}')">
              <span class="opt-letter">${optKey}</span>
              <span class="opt-text">${q.options[optKey]}</span>
            </button>
          `).join('')}
        </div>
        <div class="quiz-explanation" id="exp-${q.id}"></div>
      </div>
    `;
  }).join('');

  updateDashboard();
}

function handleOptionClick(questionId, selectedOpt) {
  const q = quizQuestions.find(item => item.id === questionId);
  if (!q) return;

  userAnswers[questionId] = selectedOpt;
  const isRight = (selectedOpt === q.correct);

  Object.keys(q.options).forEach(optKey => {
    const btn = document.getElementById(`opt-${questionId}-${optKey}`);
    if (!btn) return;

    btn.classList.remove('opt-correct', 'opt-incorrect');

    if (optKey === q.correct) {
      btn.classList.add('opt-correct');
    } else if (optKey === selectedOpt && !isRight) {
      btn.classList.add('opt-incorrect');
    }
  });

  const expDiv = document.getElementById(`exp-${questionId}`);
  if (expDiv) {
    expDiv.style.display = 'block';
    expDiv.className = `quiz-explanation ${isRight ? 'exp-right' : 'exp-wrong'}`;

    if (isRight) {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-check-circle" style="color: #16a34a;"></i> Correct Answer!
        </div>
        <div class="exp-body">
          ${q.explanations[selectedOpt]}
        </div>
      `;
    } else {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-times-circle" style="color: #dc2626;"></i> Incorrect Choice (Selected: ${selectedOpt})
        </div>
        <div class="exp-body">
          <p style="margin-bottom: 8px;"><strong>Why Option ${selectedOpt} is incorrect:</strong> ${q.explanations[selectedOpt]}</p>
          <p style="margin-top: 8px; margin-bottom: 0;"><strong>Correct Answer (${q.correct}):</strong> ${q.explanations[q.correct]}</p>
        </div>
      `;
    }
  }

  updateDashboard();
}

function updateDashboard() {
  const answeredCount = Object.keys(userAnswers).length;
  let correctCount = 0;

  Object.keys(userAnswers).forEach(qId => {
    const q = quizQuestions.find(item => item.id == qId);
    if (q && userAnswers[qId] === q.correct) {
      correctCount++;
    }
  });

  const total = quizQuestions.length;
  const accuracy = answeredCount > 0 ? Math.round((correctCount / answeredCount) * 100) : 0;
  const progressPercent = Math.round((answeredCount / total) * 100);

  document.getElementById('quiz-score').innerText = `${correctCount} / ${total}`;
  document.getElementById('quiz-progress').innerText = `${answeredCount} / ${total} Answered`;
  document.getElementById('quiz-accuracy').innerText = `${accuracy}%`;
  document.getElementById('quiz-progress-bar').style.width = `${progressPercent}%`;
}

function resetQuiz() {
  userAnswers = {};
  renderQuiz();
}

document.addEventListener('DOMContentLoaded', renderQuiz);
</script>
