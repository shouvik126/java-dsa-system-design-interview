---
layout: default
title: Quiz - Designing Twitter
permalink: /System-Design/4-system-design-problems/4.8-quiz-designing-twitter/
prev_title: "Designing Twitter"
prev_url: "/System-Design/4-system-design-problems/4.8-designing-twitter/"
next_title: "Designing Youtube or Netflix"
next_url: "/System-Design/4-system-design-problems/4.9-designing-youtube-or-netflix/"
---

# Quiz: Designing Twitter

Test your architectural knowledge of Twitter system design with **9 scenario-driven interview questions** covering read-heavy workloads, eventual consistency trade-offs, TweetID sharding, 64-bit Snowflake timestamp encoding, 3-day RAM caching with doubly linked lists, hybrid push/pull fan-out models, and two-phase AI feed candidate retrieval & ranking.

---

<!-- Quiz Dashboard -->
<div class="quiz-dashboard">
  <div class="dash-stat">
    <span class="dash-label">Score</span>
    <span class="dash-value" id="quiz-score">0 / 9</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Progress</span>
    <span class="dash-value" id="quiz-progress">0 / 9 Answered</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Accuracy</span>
    <span class="dash-value" id="quiz-accuracy">0%</span>
  </div>
  <button class="quiz-reset-btn" onclick="resetQuiz()">
    <i class="fas fa-redo-alt"></i> Reset Quiz
  </button>
</div>

<div class="quiz-progress-bar-container">
  <div class="quiz-progress-bar" id="quiz-progress-bar" style="width: 0%;"></div>
</div>

<div id="quiz-container"></div>

<style>
/* Quiz Styling - Compatible with Light & Dark Themes */
.quiz-dashboard {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-secondary, #f8fafc);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 16px 24px;
  margin-bottom: 16px;
}

html.dark-theme .quiz-dashboard {
  background: #1e293b;
  border-color: #334155;
}

.dash-stat {
  display: flex;
  flex-direction: column;
}

.dash-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted, #64748b);
  font-weight: 600;
}

.dash-value {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-color, #0f172a);
}

html.dark-theme .dash-value {
  color: #f8fafc;
}

.quiz-reset-btn {
  background: #3b82f6;
  color: #ffffff;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, transform 0.1s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.quiz-reset-btn:hover {
  background: #2563eb;
  transform: translateY(-1px);
}

.quiz-progress-bar-container {
  width: 100%;
  height: 8px;
  background: var(--border-color, #e2e8f0);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 32px;
}

html.dark-theme .quiz-progress-bar-container {
  background: #334155;
}

.quiz-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #3b82f6, #10b981);
  transition: width 0.3s ease;
}

.quiz-card {
  background: var(--bg-color, #ffffff);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 28px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  transition: border-color 0.2s ease;
}

html.dark-theme .quiz-card {
  background: #0f172a;
  border-color: #1e293b;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

.quiz-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.quiz-topic {
  font-size: 0.82rem;
  font-weight: 600;
  color: #2563eb;
  background: rgba(37, 99, 235, 0.1);
  padding: 4px 10px;
  border-radius: 20px;
}

html.dark-theme .quiz-topic {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.15);
}

.quiz-qnum {
  font-size: 0.85rem;
  color: var(--text-muted, #64748b);
  font-weight: 500;
}

.quiz-question {
  font-size: 1.1rem;
  font-weight: 600;
  margin-top: 6px;
  margin-bottom: 20px;
  color: var(--text-color, #0f172a);
  line-height: 1.5;
}

html.dark-theme .quiz-question {
  color: #f1f5f9;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.quiz-opt {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  width: 100%;
  text-align: left;
  background: var(--bg-secondary, #f8fafc);
  border: 1.5px solid var(--border-color, #e2e8f0);
  border-radius: 10px;
  padding: 14px 16px;
  font-size: 0.95rem;
  color: var(--text-color, #334155);
  cursor: pointer;
  transition: all 0.2s ease;
}

html.dark-theme .quiz-opt {
  background: #1e293b;
  border-color: #334155;
  color: #cbd5e1;
}

.quiz-opt:hover:not(:disabled) {
  border-color: #3b82f6;
  background: rgba(59, 130, 246, 0.05);
}

html.dark-theme .quiz-opt:hover:not(:disabled) {
  border-color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
}

.opt-letter {
  font-weight: 700;
  background: var(--border-color, #cbd5e1);
  color: #0f172a;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  flex-shrink: 0;
  font-size: 0.85rem;
}

html.dark-theme .opt-letter {
  background: #475569;
  color: #f8fafc;
}

.opt-text {
  line-height: 1.45;
  flex-grow: 1;
}

/* Correct Option State */
.quiz-opt.opt-correct {
  background: #dcfce7 !important;
  border-color: #16a34a !important;
  color: #14532d !important;
}

.quiz-opt.opt-correct .opt-letter {
  background: #16a34a !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-correct {
  background: rgba(22, 163, 74, 0.2) !important;
  border-color: #22c55e !important;
  color: #4ade80 !important;
}

/* Incorrect Option State */
.quiz-opt.opt-incorrect {
  background: #fee2e2 !important;
  border-color: #dc2626 !important;
  color: #7f1d1d !important;
}

.quiz-opt.opt-incorrect .opt-letter {
  background: #dc2626 !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-incorrect {
  background: rgba(220, 38, 38, 0.2) !important;
  border-color: #ef4444 !important;
  color: #fca5a5 !important;
}

/* Explanation Box */
.quiz-explanation {
  margin-top: 16px;
  padding: 16px;
  border-radius: 10px;
  font-size: 0.93rem;
  line-height: 1.5;
  display: none;
}

.quiz-explanation.exp-right {
  background: rgba(22, 163, 74, 0.08);
  border: 1px solid rgba(22, 163, 74, 0.3);
  color: #14532d;
}

html.dark-theme .quiz-explanation.exp-right {
  background: rgba(34, 197, 94, 0.12);
  border-color: rgba(34, 197, 94, 0.3);
  color: #86efac;
}

.quiz-explanation.exp-wrong {
  background: rgba(220, 38, 38, 0.08);
  border: 1px solid rgba(220, 38, 38, 0.3);
  color: #7f1d1d;
}

html.dark-theme .quiz-explanation.exp-wrong {
  background: rgba(239, 68, 68, 0.12);
  border-color: rgba(239, 68, 68, 0.3);
  color: #fca5a5;
}

.exp-header {
  font-weight: 700;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}
</style>

<script>
const quizQuestions = [
  {
    id: 1,
    topic: "System Workload & Read/Write Ratios",
    question: "Twitter processes ~1,150 new tweets/sec (writes) versus ~325,000 tweet-views/sec (reads). What is the primary architectural implication of this ~280:1 read-to-write ratio?",
    options: {
      A: "The write path must be prioritized with multi-region database locking to guarantee zero write latency.",
      B: "Architectural resources and caching mechanisms must target the read path, since reads outnumber writes by nearly two orders of magnitude.",
      C: "Relational database tables must be replaced entirely with flat unindexed file storage.",
      D: "Read operations must be throttled to 1,000 requests per minute per user."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Multi-region database locking severely degrades write throughput and is unnecessary for read-dominated workloads.",
      B: "Option B is correct! A 280:1 read-to-write ratio means Twitter is heavily read-dominated. Every expensive architectural decision (e.g. multi-tier caching, timeline pre-generation, read replicas) belongs on the read path.",
      C: "Option C is incorrect. Structured relational/NoSQL metadata databases are essential for key-value lookups, indexing, and user follow mappings.",
      D: "Option D is incorrect. Throttling reads degrades user experience and violates availability constraints."
    }
  },
  {
    id: 2,
    topic: "Consistency vs. Availability Trade-Offs",
    question: "Twitter relaxes timeline consistency to eventual consistency in return for high availability. Why is this trade-off acceptable in a Twitter-scale system design?",
    options: {
      A: "Because tweets contain binary media files that cannot be serialized across network connections.",
      B: "Because a user seeing a newly published tweet a few seconds late is an acceptable compromise for guaranteeing instant timeline rendering (<200ms) during node failures.",
      C: "Because strong consistency causes database hardware corruption during read spikes.",
      D: "Because Twitter's underlying network protocol uses UDP instead of TCP."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Media files are stored separately in distributed object storage (S3/Blob) and linked via media IDs.",
      B: "Option B is correct! High availability (CAP theorem) guarantees that timeline reads return instantly even when database shards or cache replicas experience partition delays. A temporary delay of a few seconds in seeing a new tweet is completely harmless to user experience.",
      C: "Option C is incorrect. Strong consistency causes lock contention and latency spikes, not hardware corruption.",
      D: "Option D is incorrect. Application APIs run over HTTP/TCP, not UDP."
    }
  },
  {
    id: 3,
    topic: "API Design & Media Upload Decoupling",
    question: "Why does the `tweet()` API endpoint accept an array of `media_ids` (pre-uploaded media IDs) rather than allowing binary photos and videos to be uploaded directly within the `tweet()` request payload?",
    options: {
      A: "Because HTTP POST requests cannot transport multi-part binary data.",
      B: "To keep the tweet creation payload lightweight and allow media upload retries independently without risking duplicate tweet creation.",
      C: "Because media IDs automatically compress photo sizes by 99%.",
      D: "To bypass load balancer security inspection rules."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. HTTP POST supports binary uploads via multipart/form-data.",
      B: "Option B is correct! Decoupling media upload from tweet creation keeps `tweet()` payload tiny (text & metadata). If a 20 MB video upload fails midway, the client can retry the media upload without creating duplicate tweets or holding application server connections open.",
      C: "Option C is incorrect. Media IDs are unique database/object identifiers, not compression algorithms.",
      D: "Option D is incorrect. Load balancers inspect all HTTP headers and payloads regardless of media separation."
    }
  },
  {
    id: 4,
    topic: "Database Sharding Strategies",
    question: "An engineer proposes sharding Twitter's database by `UserID`. Why is sharding by `TweetID` preferred for building timeline features?",
    options: {
      A: "UserID sharding causes key collisions across auto-increment primary keys.",
      B: "UserID sharding creates severe hot spots for celebrity accounts and unbalances storage, whereas TweetID sharding distributes tweet data evenly across all database partitions.",
      C: "TweetID sharding eliminates the need for database caching servers.",
      D: "UserID sharding prevents users from uploading video attachments."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Primary key generation is managed independently via key generators or Snowflake IDs.",
      B: "Option B is correct! Under UserID sharding, a celebrity posting thousands of tweets or receiving millions of queries creates a massive hot-spot partition. Sharding by TweetID spreads tweets uniformly across all database partitions, eliminating single-partition overload (at the cost of scatter-gather reads).",
      C: "Option C is incorrect. In-memory caching (Memcached/Redis) is still required to reduce scatter-gather database latency.",
      D: "Option D is incorrect. Sharding strategies affect database metadata partitioning, not media upload capabilities."
    }
  },
  {
    id: 5,
    topic: "64-Bit ID Generation (Twitter Snowflake)",
    question: "Twitter's Snowflake ID generator encodes Epoch Timestamp (41 bits), Machine ID (10 bits), and Sequence Number (12 bits) into a 64-bit `TweetID`. What major index optimization does this enable?",
    options: {
      A: "It allows tweets to be stored without any primary keys.",
      B: "Primary key lookup and sorting by `TweetID` naturally sorts tweets by creation time, eliminating the need for a secondary index on creation time.",
      C: "It converts database tables into in-memory Redis hashes automatically.",
      D: "It restricts key generation to a single master database node."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. `TweetID` itself serves as the 64-bit primary key.",
      B: "Option B is correct! Because the high-order bits of `TweetID` contain the timestamp, sorting by `TweetID` is identical to sorting chronologically. This removes the secondary index on creation time, significantly reducing write latency and database storage overhead.",
      C: "Option C is incorrect. Key formatting does not convert database engines into Redis hashes.",
      D: "Option D is incorrect. Snowflake machine IDs allow up to 1,024 independent worker machines to generate unique IDs concurrently without central coordination."
    }
  },
  {
    id: 6,
    topic: "Multi-Tier In-Memory Caching",
    question: "Twitter caches tweets from the past 3 days in memory (~90 GB metadata). How does this cache satisfy the 200ms timeline generation budget?",
    options: {
      A: "By executing full SQL database table scans in background threads.",
      B: "According to the 80-20 rule, ~80% of reads target recent tweets (past 3 days); caching them in RAM allows most timeline requests to be served instantly from memory without querying database shards.",
      C: "By storing raw video binary files directly inside Memcached RAM.",
      D: "By automatically purging all account follow relationships every 3 days."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. SQL full table scans are extremely slow and violate the 200ms latency budget.",
      B: "Option B is correct! User behavior demonstrates that ~80% of timeline reads focus on tweets from the last 3 days (~90 GB). Holding this 3-day working set in replicated RAM caches means most timeline queries never hit database partitions.",
      C: "Option C is incorrect. Raw media files (photos/videos) live in object storage (S3) and CDN caches, not Memcached RAM.",
      D: "Option D is incorrect. Follow relationships are persistent graph data and must never be purged."
    }
  },
  {
    id: 7,
    topic: "Hybrid Push/Pull Architecture",
    question: "When a user with 30 million followers posts a tweet, pushing the tweet to 30 million follower feed caches causes severe write amplification (Fan-Out on Write). How does a Hybrid Timeline architecture solve this problem?",
    options: {
      A: "By restricting celebrity accounts to 1 tweet per month.",
      B: "By using Push (fan-out on write) for normal users (<5,000 followers) and Pull (fan-out on read) for high-follower celebrity accounts, merging celebrity tweets into follower feeds at read time.",
      C: "By requiring followers to pay a subscription fee to view celebrity tweets.",
      D: "By replacing Redis cache clusters with relational SQL views."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Throttling active engagement harms platform activity.",
      B: "Option B is correct! Pushing a single tweet to 30M follower caches causes 30 million cache write spikes. The Hybrid model uses Push for low-follower accounts (fast read, manageable write) and Pull for celebrity accounts (celebrity tweets are fetched on-demand when followers open their timeline).",
      C: "Option C is incorrect. Subscription fees are business decisions, not architectural solutions for write amplification.",
      D: "Option D is incorrect. SQL views increase read latency and do not solve fan-out write amplification."
    }
  },
  {
    id: 8,
    topic: "Cache Data Structure Design",
    question: "Within the timeline cache, a user's recent tweets are stored as a **Doubly Linked List** mapped by `OwnerID` in a Hash Table. Why is a doubly linked list the ideal data structure for this cache?",
    options: {
      A: "It allows O(1) random index lookups for any historical tweet from 10 years ago.",
      B: "It enables cheap O(1) inserts at the head for new incoming tweets and cheap O(1) removals at the tail when purging old tweets during cache eviction.",
      C: "It automatically encrypts tweet text using AES-256 bit keys.",
      D: "It compresses text strings into floating-point numbers."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Linked lists require O(N) traversal for random middle index accesses.",
      B: "Option B is correct! A doubly linked list allows new tweets to be prepended to the head in O(1) time, while old tweets past the 3-day retention boundary can be trimmed from the tail in O(1) time.",
      C: "Option C is incorrect. Encryption is handled at storage/transport layers, not by linked list pointers.",
      D: "Option D is incorrect. Linked lists store pointers to memory nodes, not text-to-float compression algorithms."
    }
  },
  {
    id: 9,
    topic: "AI & Machine Learning Integration",
    question: "When incorporating an AI/ML ranking model into Twitter's timeline generation, how is system latency kept low while evaluating relevant tweets?",
    options: {
      A: "The ML model evaluates every tweet stored in the entire global database (~ billions of tweets) on every feed refresh.",
      B: "Candidate Retrieval (First Phase) fetches ~500 recent tweets from followed accounts, and the ML Ranking Model (Second Phase) re-orders only that small candidate set before rendering.",
      C: "The ML model runs directly inside the client web browser using WebAssembly.",
      D: "Timeline ranking is delegated to third-party search engines via external API webhooks."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Evaluating billions of tweets per request would take minutes and crash infrastructure.",
      B: "Option B is correct! Two-phase architecture decouples candidate retrieval from ranking. Phase 1 quickly retrieves ~500 candidate tweets from cache/DB partitions. Phase 2 feeds only those ~500 candidates into the ML ranking model, maintaining sub-200ms latency.",
      C: "Option C is incorrect. ML feed models rely on proprietary feature stores and server-side compute clusters.",
      D: "Option D is incorrect. External webhooks introduce unpredictable network latency and privacy issues."
    }
  }
];

let userAnswers = {};

function renderQuiz() {
  const container = document.getElementById('quiz-container');
  if (!container) return;

  container.innerHTML = quizQuestions.map((q, idx) => {
    return `
      <div class="quiz-card" id="qcard-${q.id}">
        <div class="quiz-card-header">
          <span class="quiz-topic">${q.topic}</span>
          <span class="quiz-qnum">Question ${idx + 1} of ${quizQuestions.length}</span>
        </div>
        <div class="quiz-question">${q.question}</div>
        <div class="quiz-options">
          ${Object.keys(q.options).map(optKey => `
            <button class="quiz-opt" id="opt-${q.id}-${optKey}" onclick="handleOptionClick(${q.id}, '${optKey}')">
              <span class="opt-letter">${optKey}</span>
              <span class="opt-text">${q.options[optKey]}</span>
            </button>
          `).join('')}
        </div>
        <div class="quiz-explanation" id="exp-${q.id}"></div>
      </div>
    `;
  }).join('');

  updateDashboard();
}

function handleOptionClick(questionId, selectedOpt) {
  const q = quizQuestions.find(item => item.id === questionId);
  if (!q) return;

  userAnswers[questionId] = selectedOpt;
  const isRight = (selectedOpt === q.correct);

  Object.keys(q.options).forEach(optKey => {
    const btn = document.getElementById(`opt-${questionId}-${optKey}`);
    if (!btn) return;

    btn.classList.remove('opt-correct', 'opt-incorrect');

    if (optKey === q.correct) {
      btn.classList.add('opt-correct');
    } else if (optKey === selectedOpt && !isRight) {
      btn.classList.add('opt-incorrect');
    }
  });

  const expDiv = document.getElementById(`exp-${questionId}`);
  if (expDiv) {
    expDiv.style.display = 'block';
    expDiv.className = `quiz-explanation ${isRight ? 'exp-right' : 'exp-wrong'}`;

    if (isRight) {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-check-circle" style="color: #16a34a;"></i> Correct Answer!
        </div>
        <div class="exp-body">
          ${q.explanations[selectedOpt]}
        </div>
      `;
    } else {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-times-circle" style="color: #dc2626;"></i> Incorrect Choice (Selected: ${selectedOpt})
        </div>
        <div class="exp-body">
          <p style="margin-bottom: 8px;"><strong>Why Option ${selectedOpt} is incorrect:</strong> ${q.explanations[selectedOpt]}</p>
          <p style="margin-top: 8px; margin-bottom: 0;"><strong>Correct Answer (${q.correct}):</strong> ${q.explanations[q.correct]}</p>
        </div>
      `;
    }
  }

  updateDashboard();
}

function updateDashboard() {
  const answeredCount = Object.keys(userAnswers).length;
  let correctCount = 0;

  Object.keys(userAnswers).forEach(qId => {
    const q = quizQuestions.find(item => item.id == qId);
    if (q && userAnswers[qId] === q.correct) {
      correctCount++;
    }
  });

  const total = quizQuestions.length;
  const accuracy = answeredCount > 0 ? Math.round((correctCount / answeredCount) * 100) : 0;
  const progressPercent = Math.round((answeredCount / total) * 100);

  document.getElementById('quiz-score').innerText = `${correctCount} / ${total}`;
  document.getElementById('quiz-progress').innerText = `${answeredCount} / ${total} Answered`;
  document.getElementById('quiz-accuracy').innerText = `${accuracy}%`;
  document.getElementById('quiz-progress-bar').style.width = `${progressPercent}%`;
}

function resetQuiz() {
  userAnswers = {};
  renderQuiz();
}

document.addEventListener('DOMContentLoaded', renderQuiz);
</script>
