---
layout: default
title: Quiz - Designing Pastebin
permalink: /System-Design/4-system-design-problems/4.4-quiz-designing-pastebin/
prev_title: "Designing Pastebin"
prev_url: "/System-Design/4-system-design-problems/4.4-designing-pastebin/"
---

# Quiz: Designing Pastebin

Test your understanding of Pastebin architecture with **9 scenario-driven interview questions** covering payload size limits, capacity estimation math, Base64 keyspace math, database schema decoupling, Amazon S3 object storage, KGS pre-generation, and API rate-limiting.

---

<!-- Quiz Dashboard -->
<div class="quiz-dashboard">
  <div class="dash-stat">
    <span class="dash-label">Score</span>
    <span class="dash-value" id="quiz-score">0 / 9</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Progress</span>
    <span class="dash-value" id="quiz-progress">0 / 9 Answered</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Accuracy</span>
    <span class="dash-value" id="quiz-accuracy">0%</span>
  </div>
  <button class="quiz-reset-btn" onclick="resetQuiz()">
    <i class="fas fa-redo-alt"></i> Reset Quiz
  </button>
</div>

<div class="quiz-progress-bar-container">
  <div class="quiz-progress-bar" id="quiz-progress-bar" style="width: 0%;"></div>
</div>

<div id="quiz-container"></div>

<style>
/* Quiz Styling - Compatible with Light & Dark Themes */
.quiz-dashboard {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-secondary, #f8fafc);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 16px 24px;
  margin-bottom: 16px;
}

html.dark-theme .quiz-dashboard {
  background: #1e293b;
  border-color: #334155;
}

.dash-stat {
  display: flex;
  flex-direction: column;
}

.dash-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted, #64748b);
  font-weight: 600;
}

.dash-value {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-color, #0f172a);
}

html.dark-theme .dash-value {
  color: #f8fafc;
}

.quiz-reset-btn {
  background: #3b82f6;
  color: #ffffff;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, transform 0.1s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.quiz-reset-btn:hover {
  background: #2563eb;
  transform: translateY(-1px);
}

.quiz-progress-bar-container {
  width: 100%;
  height: 8px;
  background: var(--border-color, #e2e8f0);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 32px;
}

html.dark-theme .quiz-progress-bar-container {
  background: #334155;
}

.quiz-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #3b82f6, #10b981);
  transition: width 0.3s ease;
}

.quiz-card {
  background: var(--bg-color, #ffffff);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 28px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  transition: border-color 0.2s ease;
}

html.dark-theme .quiz-card {
  background: #0f172a;
  border-color: #1e293b;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

.quiz-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.quiz-topic {
  font-size: 0.82rem;
  font-weight: 600;
  color: #2563eb;
  background: rgba(37, 99, 235, 0.1);
  padding: 4px 10px;
  border-radius: 20px;
}

html.dark-theme .quiz-topic {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.15);
}

.quiz-qnum {
  font-size: 0.85rem;
  color: var(--text-muted, #64748b);
  font-weight: 500;
}

.quiz-question {
  font-size: 1.1rem;
  font-weight: 600;
  margin-top: 6px;
  margin-bottom: 20px;
  color: var(--text-color, #0f172a);
  line-height: 1.5;
}

html.dark-theme .quiz-question {
  color: #f1f5f9;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.quiz-opt {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  width: 100%;
  text-align: left;
  background: var(--bg-secondary, #f8fafc);
  border: 1.5px solid var(--border-color, #e2e8f0);
  border-radius: 10px;
  padding: 14px 16px;
  font-size: 0.95rem;
  color: var(--text-color, #334155);
  cursor: pointer;
  transition: all 0.2s ease;
}

html.dark-theme .quiz-opt {
  background: #1e293b;
  border-color: #334155;
  color: #cbd5e1;
}

.quiz-opt:hover:not(:disabled) {
  border-color: #3b82f6;
  background: rgba(59, 130, 246, 0.05);
}

html.dark-theme .quiz-opt:hover:not(:disabled) {
  border-color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
}

.opt-letter {
  font-weight: 700;
  background: var(--border-color, #cbd5e1);
  color: #0f172a;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  flex-shrink: 0;
  font-size: 0.85rem;
}

html.dark-theme .opt-letter {
  background: #475569;
  color: #f8fafc;
}

.opt-text {
  line-height: 1.45;
  flex-grow: 1;
}

/* Correct Option State */
.quiz-opt.opt-correct {
  background: #dcfce7 !important;
  border-color: #16a34a !important;
  color: #14532d !important;
}

.quiz-opt.opt-correct .opt-letter {
  background: #16a34a !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-correct {
  background: rgba(22, 163, 74, 0.2) !important;
  border-color: #22c55e !important;
  color: #bbf7d0 !important;
}

/* Incorrect Option State */
.quiz-opt.opt-incorrect {
  background: #fef2f2 !important;
  border-color: #dc2626 !important;
  color: #7f1d1d !important;
}

.quiz-opt.opt-incorrect .opt-letter {
  background: #dc2626 !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-incorrect {
  background: rgba(220, 38, 38, 0.2) !important;
  border-color: #ef4444 !important;
  color: #fecaca !important;
}

/* Explanation Box */
.quiz-explanation {
  margin-top: 18px;
  padding: 16px;
  border-radius: 10px;
  font-size: 0.93rem;
  line-height: 1.55;
  display: none;
}

.quiz-explanation.exp-right {
  background: #f0fdf4;
  border: 1px solid #86efac;
  color: #166534;
}

html.dark-theme .quiz-explanation.exp-right {
  background: rgba(22, 163, 74, 0.15);
  border-color: #22c55e;
  color: #bbf7d0;
}

.quiz-explanation.exp-wrong {
  background: #fef2f2;
  border: 1px solid #fca5a5;
  color: #991b1b;
}

html.dark-theme .quiz-explanation.exp-wrong {
  background: rgba(220, 38, 38, 0.15);
  border-color: #ef4444;
  color: #fecaca;
}

.exp-header {
  font-weight: 700;
  font-size: 1rem;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.exp-body {
  margin-top: 6px;
}
</style>

<script>
const quizQuestions = [
  {
    id: 1,
    topic: "Design Constraints & Abuse Protection",
    question: "Unlike URL shorteners that store tiny text strings, Pastebin allows users to upload full text files (logs, code, configs). Why does Pastebin strictly cap single upload payloads at 10 MB?",
    options: {
      A: "Because 10 MB is the maximum file size supported by TCP/IP network packets.",
      B: "To prevent denial-of-service (DoS) attacks and buffer pool exhaustion where malicious clients upload multi-gigabyte payloads to saturate ingress bandwidth and disk storage.",
      C: "Because Base64 encoding cannot convert text larger than 10 MB.",
      D: "Because Amazon S3 rejects file objects larger than 10 MB."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. TCP packets handle data streams of arbitrary size via fragmentation.",
      B: "Option B is correct! Without a strict single upload limit (10 MB), abusive users could upload massive files (e.g. 5 GB video files or memory dumps), consuming web server RAM, filling storage arrays, and causing denial-of-service outages.",
      C: "Option C is incorrect. Base64 encoding operates on binary buffers of any size.",
      D: "Option D is incorrect. Amazon S3 supports individual object sizes up to 5 TB."
    }
  },
  {
    id: 2,
    topic: "Scale & Bandwidth Estimation",
    question: "A Pastebin system handles 1 million new pastes per day and 5 million read requests per day (5:1 read ratio). If average paste size is 10 KB, what are the estimated average Write QPS and Read Egress Data Rate?",
    options: {
      A: "Write QPS: ~12 pastes/sec | Egress Rate: ~0.6 MB/sec",
      B: "Write QPS: ~120 pastes/sec | Egress Rate: ~6.0 MB/sec",
      C: "Write QPS: ~500 pastes/sec | Egress Rate: ~50 MB/sec",
      D: "Write QPS: ~5 pastes/sec | Egress Rate: ~0.1 MB/sec"
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! Write QPS = 1M / 86,400 sec ≈ 11.5 (~12 writes/sec). Read QPS = 5M / 86,400 sec ≈ 58 reads/sec. Egress Data Rate = 58 reads/sec × 10 KB = 580 KB/sec ≈ 0.6 MB/sec.",
      B: "Option B is incorrect. 120 Write QPS would equal 10 million new pastes per day.",
      C: "Option C is incorrect. 500 Write QPS would equal 43 million new pastes per day.",
      D: "Option D is incorrect. 5 Write QPS underestimates the 1 million daily uploads."
    }
  },
  {
    id: 3,
    topic: "70% Target Capacity Storage Model",
    question: "Over 10 years, a Pastebin system generates 3.65 billion pastes averaging 10 KB each, requiring 36.5 TB of raw disk storage. Applying a 70% target capacity model (never allowing storage arrays to exceed 70% capacity), what total storage capacity must be provisioned?",
    options: {
      A: "36.5 TB",
      B: "45.0 TB",
      C: "51.4 TB",
      D: "73.0 TB"
    },
    correct: "C",
    explanations: {
      A: "Option A is incorrect. 36.5 TB is the raw data volume; running storage at 100% capacity risks disk failure and performance degradation.",
      B: "Option B is incorrect. 45 TB leaves storage at ~81% capacity.",
      C: "Option C is correct! 36.5 TB / 0.70 = 52.1 TB ≈ 51.4 TB allocated capacity. Maintaining a 30% headroom buffer prevents disk fill outages and maintains IOPS throughput.",
      D: "Option D is incorrect. 73 TB represents a 50% capacity model."
    }
  },
  {
    id: 4,
    topic: "Keyspace Encoding & Base64 Calculation",
    question: "Why is a 6-character Base64 key (`[A-Z, a-z, 0-9, ., -]`) sufficient for a 10-year Pastebin system storing 3.65 billion records?",
    options: {
      A: "Base64 strings automatically compress text payloads by 50%.",
      B: "A 6-character Base64 key yields 64^6 = 68.7 billion unique keys; storing 3.65 billion pastes occupies <5.4% of the keyspace, preventing collision bottlenecks.",
      C: "Base64 keys can only be decoded by relational databases.",
      D: "6 characters is the maximum string length permitted by DNS servers."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Base64 is an encoding format; it slightly increases data size (~33%), but keys are short fixed strings.",
      B: "Option B is correct! 64^6 = 68,719,476,736 (~68.7 billion keys). Storing 3.65B records utilizes only 5.3% of the total keyspace, leaving 94.7% free and ensuring minimal collision risks.",
      C: "Option C is incorrect. Base64 keys are standard text strings compatible with all databases.",
      D: "Option D is incorrect. DNS hostnames support labels up to 63 characters."
    }
  },
  {
    id: 5,
    topic: "Memory Cache Sizing (80/20 Rule)",
    question: "With 5 million daily read requests and an average paste size of 10 KB, how much RAM is required for a Redis cache storing the top 20% hot pastes under the Pareto 80/20 principle?",
    options: {
      A: "1 GB RAM",
      B: "10 GB RAM",
      C: "100 GB RAM",
      D: "50 GB RAM"
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. 1 GB caches only 2% of daily reads.",
      B: "Option B is correct! 20% of 5 million reads = 1 million hot pastes. 1M pastes × 10 KB = 10,000,000 KB = 10 GB RAM.",
      C: "Option C is incorrect. 100 GB RAM would cache 200% of daily read volume.",
      D: "Option D is incorrect. 50 GB RAM would cache 100% of all 5M daily reads."
    }
  },
  {
    id: 6,
    topic: "Database Schema & Content Decoupling",
    question: "In the `Pastes` database schema, the `ContentKey` column stores an S3 object reference URI (e.g. `s3://bucket/pastes/aB3x9Z`) rather than the raw 10 MB text payload. Why is this decoupling critical?",
    options: {
      A: "Because SQL databases cannot store characters longer than 255 bytes.",
      B: "Storing multi-megabyte text payloads directly inside database table rows causes buffer pool bloat, degrades B-Tree index scan efficiency, and slows down metadata queries.",
      C: "Because Amazon S3 requires database tables to hold object pointers for billing.",
      D: "Because NoSQL databases do not support string data types."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Databases support BLOB/TEXT types up to gigabytes, but doing so degrades query performance.",
      B: "Option B is correct! Storing large text blobs directly in database rows bloats memory page buffers. When the database reads table pages, large blobs reduce row density per page, degrading cache hit ratios and index scan speeds. Storing a 512-byte `ContentKey` URI keeps table rows tiny and fast.",
      C: "Option C is incorrect. Amazon S3 operates independently of database schema definitions.",
      D: "Option D is incorrect. NoSQL key-value stores natively support string data types."
    }
  },
  {
    id: 7,
    topic: "Decoupled Storage Architecture",
    question: "Why does the Pastebin architecture separate the storage layer into a NoSQL Metadata DB (e.g., Cassandra) and an Object Store (e.g., Amazon S3)?",
    options: {
      A: "NoSQL databases cannot persist data to physical disks.",
      B: "Metadata (<1 KB) requires fast O(1) key lookups by URLHash, while raw text payloads (up to 10 MB) require cheap, infinitely scalable blob storage; decoupling allows each tier to scale independently.",
      C: "Amazon S3 cannot process HTTP GET requests.",
      D: "Decoupling storage is only necessary for video streaming platforms."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. NoSQL stores like Cassandra commit writes to persistent disk commit logs and SSTables.",
      B: "Option B is correct! Metadata queries (creation date, expiration, user ID) demand fast database indexing ($O(1)$ key lookups). Text payloads require high storage capacity. Storing text in S3 ($99.999999999\%$ durability) provides cost-effective blob storage while keeping the database lightweight.",
      C: "Option C is incorrect. Amazon S3 natively serves HTTP GET requests for stored objects.",
      D: "Option D is incorrect. Decoupling metadata from blob content is a standard pattern for any file or text-heavy architecture."
    }
  },
  {
    id: 8,
    topic: "Key Generation Service (KGS) Mechanics",
    question: "How does a background Key Generation Service (KGS) prevent database collision checks and retry loops when a user posts a new paste?",
    options: {
      A: "By calculating SHA-256 hashes synchronously on the web server.",
      B: "By pre-computing unique 6-character Base64 keys offline and buffering key blocks in application server memory; when a write request arrives, the server assigns a key without querying the DB.",
      C: "By locking the entire database during every paste upload.",
      D: "By delegating key generation to client-side browser extensions."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Synchronous hashing requires live database collision checks, creating latency bottlenecks.",
      B: "Option B is correct! KGS generates unique 6-character Base64 keys in background worker processes and stores them in a key database. App servers pre-fetch bulk key blocks into RAM. When a write request arrives, the server immediately pops a key from memory—moving uniqueness verification entirely off the live request path.",
      C: "Option C is incorrect. Locking the database per write destroys concurrency and throughput.",
      D: "Option D is incorrect. Client-side browser key generation cannot guarantee uniqueness across independent users."
    }
  },
  {
    id: 9,
    topic: "System APIs & Developer Key Rate Limiting",
    question: "The `POST /api/v1/pastes` creation endpoint requires an `api_dev_key` parameter. What is the primary operational purpose of requiring developer API keys?",
    options: {
      A: "To encrypt the paste text before writing to Amazon S3.",
      B: "To identify registered user accounts at the API Gateway to enforce rate-limiting thresholds and daily upload quotas, preventing automated scripts from abusing storage.",
      C: "To compress raw text data into ZIP archives.",
      D: "To automatically translate code snippets into different programming languages."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Data encryption at rest is managed via KMS or S3 server-side encryption, not developer API keys.",
      B: "Option B is correct! Developer API keys (`api_dev_key`) allow the API Gateway to track usage per account. If an abusive script sends thousands of requests, rate limiters (e.g. Token Bucket) drop excessive requests (`HTTP 429 Too Many Requests`) before they hit app servers or object storage.",
      C: "Option C is incorrect. Data compression is handled by application server gzip streams.",
      D: "Option D is incorrect. API keys represent identity credentials, not language translation services."
    }
  }
];

let userAnswers = {};

function renderQuiz() {
  const container = document.getElementById('quiz-container');
  if (!container) return;

  container.innerHTML = quizQuestions.map((q, idx) => {
    return `
      <div class="quiz-card" id="quiz-card-${q.id}">
        <div class="quiz-card-header">
          <span class="quiz-topic">${q.topic}</span>
          <span class="quiz-qnum">Question ${idx + 1} of ${quizQuestions.length}</span>
        </div>
        <div class="quiz-question">${q.question}</div>
        <div class="quiz-options">
          ${Object.keys(q.options).map(optKey => `
            <button class="quiz-opt" id="opt-${q.id}-${optKey}" onclick="handleOptionClick(${q.id}, '${optKey}')">
              <span class="opt-letter">${optKey}</span>
              <span class="opt-text">${q.options[optKey]}</span>
            </button>
          `).join('')}
        </div>
        <div class="quiz-explanation" id="exp-${q.id}"></div>
      </div>
    `;
  }).join('');

  updateDashboard();
}

function handleOptionClick(questionId, selectedOpt) {
  const q = quizQuestions.find(item => item.id === questionId);
  if (!q) return;

  userAnswers[questionId] = selectedOpt;
  const isRight = (selectedOpt === q.correct);

  Object.keys(q.options).forEach(optKey => {
    const btn = document.getElementById(`opt-${questionId}-${optKey}`);
    if (!btn) return;

    btn.classList.remove('opt-correct', 'opt-incorrect');

    if (optKey === q.correct) {
      btn.classList.add('opt-correct');
    } else if (optKey === selectedOpt && !isRight) {
      btn.classList.add('opt-incorrect');
    }
  });

  const expDiv = document.getElementById(`exp-${questionId}`);
  if (expDiv) {
    expDiv.style.display = 'block';
    expDiv.className = `quiz-explanation ${isRight ? 'exp-right' : 'exp-wrong'}`;

    if (isRight) {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-check-circle" style="color: #16a34a;"></i> Correct Answer!
        </div>
        <div class="exp-body">
          ${q.explanations[selectedOpt]}
        </div>
      `;
    } else {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-times-circle" style="color: #dc2626;"></i> Incorrect Choice (Selected: ${selectedOpt})
        </div>
        <div class="exp-body">
          <p style="margin-bottom: 8px;"><strong>Why Option ${selectedOpt} is incorrect:</strong> ${q.explanations[selectedOpt]}</p>
          <p style="margin-top: 8px; margin-bottom: 0;"><strong>Correct Answer (${q.correct}):</strong> ${q.explanations[q.correct]}</p>
        </div>
      `;
    }
  }

  updateDashboard();
}

function updateDashboard() {
  const answeredCount = Object.keys(userAnswers).length;
  let correctCount = 0;

  Object.keys(userAnswers).forEach(qId => {
    const q = quizQuestions.find(item => item.id == qId);
    if (q && userAnswers[qId] === q.correct) {
      correctCount++;
    }
  });

  const total = quizQuestions.length;
  const accuracy = answeredCount > 0 ? Math.round((correctCount / answeredCount) * 100) : 0;
  const progressPercent = Math.round((answeredCount / total) * 100);

  document.getElementById('quiz-score').innerText = `${correctCount} / ${total}`;
  document.getElementById('quiz-progress').innerText = `${answeredCount} / ${total} Answered`;
  document.getElementById('quiz-accuracy').innerText = `${accuracy}%`;
  document.getElementById('quiz-progress-bar').style.width = `${progressPercent}%`;
}

function resetQuiz() {
  userAnswers = {};
  renderQuiz();
}

document.addEventListener('DOMContentLoaded', renderQuiz);
</script>
