---
layout: default
title: Quiz - Designing Facebook Messenger
permalink: /System-Design/4-system-design-problems/4.7-quiz-designing-facebook-messenger/
prev_title: "Designing Facebook Messenger"
prev_url: "/System-Design/4-system-design-problems/4.7-designing-facebook-messenger/"
next_title: "Designing Twitter"
next_url: "/System-Design/4-system-design-problems/4.8-designing-twitter/"
---

# Quiz: Designing Facebook Messenger

Test your architectural knowledge of Facebook Messenger system design with **9 scenario-driven interview questions** covering real-time push architecture, HBase wide-column storage, per-client sequence numbers, UserID sharding, stateful load balancing, status broadcast optimizations, group chat routing, and push notifications.

---

<!-- Quiz Dashboard -->
<div class="quiz-dashboard">
  <div class="dash-stat">
    <span class="dash-label">Score</span>
    <span class="dash-value" id="quiz-score">0 / 9</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Progress</span>
    <span class="dash-value" id="quiz-progress">0 / 9 Answered</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Accuracy</span>
    <span class="dash-value" id="quiz-accuracy">0%</span>
  </div>
  <button class="quiz-reset-btn" onclick="resetQuiz()">
    <i class="fas fa-redo-alt"></i> Reset Quiz
  </button>
</div>

<div class="quiz-progress-bar-container">
  <div class="quiz-progress-bar" id="quiz-progress-bar" style="width: 0%;"></div>
</div>

<div id="quiz-container"></div>

<style>
/* Quiz Styling - Compatible with Light & Dark Themes */
.quiz-dashboard {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-secondary, #f8fafc);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 16px 24px;
  margin-bottom: 16px;
}

html.dark-theme .quiz-dashboard {
  background: #1e293b;
  border-color: #334155;
}

.dash-stat {
  display: flex;
  flex-direction: column;
}

.dash-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted, #64748b);
  font-weight: 600;
}

.dash-value {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-color, #0f172a);
}

html.dark-theme .dash-value {
  color: #f8fafc;
}

.quiz-reset-btn {
  background: #3b82f6;
  color: #ffffff;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, transform 0.1s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.quiz-reset-btn:hover {
  background: #2563eb;
  transform: translateY(-1px);
}

.quiz-progress-bar-container {
  width: 100%;
  height: 8px;
  background: var(--border-color, #e2e8f0);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 32px;
}

html.dark-theme .quiz-progress-bar-container {
  background: #334155;
}

.quiz-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #3b82f6, #10b981);
  transition: width 0.3s ease;
}

.quiz-card {
  background: var(--bg-color, #ffffff);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 28px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  transition: border-color 0.2s ease;
}

html.dark-theme .quiz-card {
  background: #0f172a;
  border-color: #1e293b;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

.quiz-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.quiz-topic {
  font-size: 0.82rem;
  font-weight: 600;
  color: #2563eb;
  background: rgba(37, 99, 235, 0.1);
  padding: 4px 10px;
  border-radius: 20px;
}

html.dark-theme .quiz-topic {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.15);
}

.quiz-qnum {
  font-size: 0.85rem;
  color: var(--text-muted, #64748b);
  font-weight: 500;
}

.quiz-question {
  font-size: 1.1rem;
  font-weight: 600;
  margin-top: 6px;
  margin-bottom: 20px;
  color: var(--text-color, #0f172a);
  line-height: 1.5;
}

html.dark-theme .quiz-question {
  color: #f1f5f9;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.quiz-opt {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  width: 100%;
  text-align: left;
  background: var(--bg-secondary, #f8fafc);
  border: 1.5px solid var(--border-color, #e2e8f0);
  border-radius: 10px;
  padding: 14px 16px;
  font-size: 0.95rem;
  color: var(--text-color, #334155);
  cursor: pointer;
  transition: all 0.2s ease;
}

html.dark-theme .quiz-opt {
  background: #1e293b;
  border-color: #334155;
  color: #cbd5e1;
}

.quiz-opt:hover:not(:disabled) {
  border-color: #3b82f6;
  background: rgba(59, 130, 246, 0.05);
}

html.dark-theme .quiz-opt:hover:not(:disabled) {
  border-color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
}

.opt-letter {
  font-weight: 700;
  background: var(--border-color, #cbd5e1);
  color: #0f172a;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  flex-shrink: 0;
  font-size: 0.85rem;
}

html.dark-theme .opt-letter {
  background: #475569;
  color: #f8fafc;
}

.opt-text {
  line-height: 1.45;
  flex-grow: 1;
}

/* Correct Option State */
.quiz-opt.opt-correct {
  background: #dcfce7 !important;
  border-color: #16a34a !important;
  color: #14532d !important;
}

.quiz-opt.opt-correct .opt-letter {
  background: #16a34a !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-correct {
  background: rgba(22, 163, 74, 0.2) !important;
  border-color: #22c55e !important;
  color: #bbf7d0 !important;
}

/* Incorrect Option State */
.quiz-opt.opt-incorrect {
  background: #fef2f2 !important;
  border-color: #dc2626 !important;
  color: #7f1d1d !important;
}

.quiz-opt.opt-incorrect .opt-letter {
  background: #dc2626 !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-incorrect {
  background: rgba(220, 38, 38, 0.2) !important;
  border-color: #ef4444 !important;
  color: #fecaca !important;
}

/* Explanation Box */
.quiz-explanation {
  margin-top: 18px;
  padding: 16px;
  border-radius: 10px;
  font-size: 0.93rem;
  line-height: 1.55;
  display: none;
}

.quiz-explanation.exp-right {
  background: #f0fdf4;
  border: 1px solid #86efac;
  color: #166534;
}

html.dark-theme .quiz-explanation.exp-right {
  background: rgba(22, 163, 74, 0.15);
  border-color: #22c55e;
  color: #bbf7d0;
}

.quiz-explanation.exp-wrong {
  background: #fef2f2;
  border: 1px solid #fca5a5;
  color: #991b1b;
}

html.dark-theme .quiz-explanation.exp-wrong {
  background: rgba(220, 38, 38, 0.15);
  border-color: #ef4444;
  color: #fecaca;
}

.exp-header {
  font-weight: 700;
  font-size: 1rem;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.exp-body {
  margin-top: 6px;
}
</style>

<script>
const quizQuestions = [
  {
    id: 1,
    topic: "Consistency vs. Availability Trade-off",
    question: "When evaluating the CAP theorem for real-time instant messaging like Facebook Messenger, why does the architecture explicitly favor High Consistency over High Availability when a trade-off is required?",
    options: {
      A: "Because network bandwidth becomes 10x cheaper under CP systems.",
      B: "Because seeing missing messages, out-of-order replies, or divergent chat histories across a user's devices severely degrades user trust, making temporary history fetch delays acceptable during failover.",
      C: "Because High Availability requires using hardware load balancers, which cannot handle WebSocket protocols.",
      D: "Because NoSQL databases like HBase do not support multi-region replication."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. System trade-offs are driven by user experience contracts and consistency models, not network hardware bandwidth costs.",
      B: "Option B is correct! If consistency is sacrificed, a user might see different chat histories on their phone versus laptop, or receive replies out of order. Users prefer waiting briefly for chat history during a database failover over seeing inconsistent or corrupted message histories.",
      C: "Option C is incorrect. Load balancers natively handle TCP, HTTP, and WebSocket protocols.",
      D: "Option D is incorrect. HBase supports cross-datacenter replication, but consistency remains the primary design requirement."
    }
  },
  {
    id: 2,
    topic: "Message Delivery Architecture (Push vs. Pull)",
    question: "Facebook Messenger handles 20 billion messages daily across 500 million daily active users. Why is a server-initiated Push model (WebSockets / HTTP Long Polling) chosen over a client-initiated Pull model (short HTTP polling)?",
    options: {
      A: "Because short HTTP polling requires clients to encrypt every request with RSA 4096-bit keys.",
      B: "Because periodic pull polling forces 500M clients to constantly send HTTP requests where most checks return empty data, wasting massive network bandwidth and server resources while introducing delivery latency.",
      C: "Because WebSockets automatically store all chat history in the client's local storage without database writes.",
      D: "Because HTTP pull models cannot transmit text strings exceeding 50 bytes."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Encryption choice (TLS/RSA/AES) is independent of push vs pull transport protocols.",
      B: "Option B is correct! Under a pull model, to achieve near-instant delivery, millions of clients must poll every 1-2 seconds. Since users spend most of their time idle, >95% of requests return no new messages—wasting CPU and bandwidth. A persistent push connection delivers incoming messages instantly with zero wasted polling calls.",
      C: "Option C is incorrect. WebSockets are transport connections; messages must still be persisted to backend storage (HBase).",
      D: "Option D is incorrect. HTTP GET/POST requests can transfer multi-megabyte payloads."
    }
  },
  {
    id: 3,
    topic: "Message Ordering & Sequence Numbers",
    question: "Why are server-generated timestamps (T1, T2) insufficient for establishing a globally consistent message order between two users, and how do per-client sequence numbers resolve this issue?",
    options: {
      A: "Server timestamps use 32-bit integers which overflow every 68 years.",
      B: "Due to clock skew across distributed servers and concurrent bi-directional message sends, server timestamps can produce conflicting order views; per-client sequence numbers guarantee each user sees a deterministic, consistent order across all their devices.",
      C: "Server timestamps require synchronous atomic locks across all database shards.",
      D: "Sequence numbers compress 100-byte text messages into 4-byte hash codes."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. 64-bit epoch timestamps (milliseconds) are standard and do not overflow.",
      B: "Option B is correct! When User 1 sends message M1 and User 2 sends message M2 concurrently, network transit delays and slight clock variations mean server arrival order might differ per user view. A sequence number incremented per conversation/client forces a consistent order across every device connected to that account.",
      C: "Option C is incorrect. Sequence numbers are maintained per-client/conversation, avoiding global distributed locking.",
      D: "Option D is incorrect. Sequence numbers are monotonic counters used for ordering, not lossy text compression."
    }
  },
  {
    id: 4,
    topic: "Database Engine Selection (HBase vs. RDBMS)",
    question: "Messenger receives 20 billion message writes per day (~2 TB/day). Why is a wide-column database like Apache HBase (built on HDFS) significantly better suited for this workload than a traditional relational database (MySQL)?",
    options: {
      A: "HBase buffers writes in a memory buffer (MemStore) and flushes them sequentially in bulk to disk, converting billions of tiny random writes into fast sequential disk I/O.",
      B: "MySQL does not support primary keys or index lookups when table size exceeds 10 GB.",
      C: "HBase automatically translates English chat messages into 50 languages in real time.",
      D: "Relational databases cannot perform range scans on sequential keys."
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! Performing 20 billion random row inserts per day on a B-Tree indexed RDBMS causes heavy disk seek contention and high write latency. HBase buffers incoming writes in memory (MemStore) and writes sequentially to SSTables/HDFS, handling massive append-heavy write rates effortlessly.",
      B: "Option B is incorrect. MySQL supports primary keys on multi-terabyte tables, but random write throughput is the bottleneck.",
      C: "Option C is incorrect. Language translation is an application-level ML service, not a storage engine feature.",
      D: "Option D is incorrect. Relational databases support range scans, but struggle with high-throughput random write loads."
    }
  },
  {
    id: 5,
    topic: "Database Sharding Strategy",
    question: "When partitioning 3.6 PB of message history across 1,000 database shards, why is partitioning by `UserID` (`hash(UserID) % 1000`) preferred over partitioning by `MessageID`?",
    options: {
      A: "Partitioning by `MessageID` causes all incoming messages to hit Shard 0.",
      B: "Partitioning by `UserID` places all messages of a user on a single shard, allowing history range fetches to execute against 1 shard rather than scattering queries across 1,000 shards.",
      C: "Partitioning by `UserID` eliminates the need for database backups.",
      D: "Partitioning by `MessageID` prevents clients from establishing WebSocket connections."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Hashing `MessageID` would distribute rows evenly across all shards, not just Shard 0.",
      B: "Option B is correct! The primary query pattern in a chat app is fetching recent messages for a user's conversation. If partitioned by `UserID`, a single shard query returns the user's history. If partitioned by `MessageID`, fetching 20 messages would require querying all 1,000 shards (scatter-gather), causing severe latency.",
      C: "Option C is incorrect. Backups are required regardless of the sharding key strategy.",
      D: "Option D is incorrect. WebSockets operate at the transport layer, completely decoupled from database sharding."
    }
  },
  {
    id: 6,
    topic: "Stateful Connection Management & Load Balancing",
    question: "Assuming 500 million concurrent connected users and 50,000 connections per chat server, the system requires ~10,000 stateful chat servers. How does the front load balancer route a message from User-A to User-B?",
    options: {
      A: "The load balancer broadcasts the message to all 10,000 chat servers simultaneously.",
      B: "The load balancer uses a mapping table (e.g. in-memory lookup or hash ring) that maps User-B's `UserID` to the specific chat server maintaining User-B's active connection.",
      C: "The load balancer drops the connection and forces User-B to poll an Amazon S3 bucket.",
      D: "The load balancer converts the TCP payload into an email sent to User-B's inbox."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Broadcasting every message to 10,000 servers would consume immense CPU and network bandwidth.",
      B: "Option B is correct! Because chat servers are stateful (holding live TCP/WebSocket sockets for connected users), a load balancer or connection service maintains an active mapping of `UserID -> ServerID` to route messages directly to the target user's server.",
      C: "Option C is incorrect. S3 is cold object storage and cannot serve real-time push connections.",
      D: "Option D is incorrect. Instant messaging uses active socket connections, not email protocols."
    }
  },
  {
    id: 7,
    topic: "Online Status Traffic Optimization",
    question: "If 500 million active users continuously update their online/offline presence, naive status broadcasting would generate trillions of events. Which set of techniques is used to control this traffic?",
    options: {
      A: "Broadcasting status updates only during nighttime hours when server load is low.",
      B: "Pulling status lazily for visible friends in the viewport, delaying broadcast on login to filter rapid reconnects, and fetching status on-demand when opening a chat.",
      C: "Disabling online status features for all users who have more than 10 friends.",
      D: "Storing online status in relational database tables with disk logging enabled."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Real-time presence indicators must reflect current state during active usage hours.",
      B: "Option B is correct! To avoid presence broadcast storms, clients only pull status for friends currently visible on screen (viewport), buffer/delay login presence broadcasts by a few seconds to absorb connection flutters, and query status on-demand when starting a chat.",
      C: "Option C is incorrect. Arbitrarily disabling features for active social users destroys core utility.",
      D: "Option D is incorrect. Real-time presence lives in memory (RAM key-value stores / chat server memory), never on disk."
    }
  },
  {
    id: 8,
    topic: "Group Chat Routing & Wide-Column Schema",
    question: "How does group chat message routing work in a wide-column store, and why is storing member lists inside the `GroupChat` row considered optimal?",
    options: {
      A: "Group chats require creating a separate physical database shard for every group member.",
      B: "The Wide-Column model shapes rows around query patterns; storing member lists in the `GroupChat` row allows the chat server to fetch all recipients in a single row read and route messages to their respective connection servers.",
      C: "Storing member lists in one row violates 3NF normalization rules and causes severe SQL compilation crashes.",
      D: "Group chat messages bypass chat servers and travel directly peer-to-peer between client mobile phones."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Creating a physical database shard per user member is unscalable and unnecessary.",
      B: "Option B is correct! Wide-column stores prioritize query-driven schema design over strict relational normalization. Since the primary access path for a group message is 'find all members of Group X', keeping member IDs inside Group X's row allows 1 fast read to retrieve all target recipient IDs.",
      C: "Option C is incorrect. Wide-column stores intentionally denormalize data to eliminate costly multi-table joins.",
      D: "Option D is incorrect. Messenger relies on centralized chat servers for delivery guarantees, history logging, and offline push."
    }
  },
  {
    id: 9,
    topic: "Offline Delivery & Push Notification Flow",
    question: "When User-A sends a message to User-B, but User-B has no active TCP/WebSocket connection to any chat server (offline state), what steps does the system take to notify User-B?",
    options: {
      A: "The chat server drops the message and returns an unrecoverable error to User-A.",
      B: "The chat server persists the message to HBase, looks up User-B's registered device tokens in the `Device` table, and passes a notification payload to third-party push services (APNs/FCM).",
      C: "The chat server repeatedly pings User-B's mobile IP address using ICMP packets until the phone turns on.",
      D: "The system forces User-A to re-type the message as an SMS text message."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Messages sent to offline users are stored permanently so they can be read upon reconnecting.",
      B: "Option B is correct! When the chat server detects that the receiver is offline, it safely saves the message in HBase, queries the `Device` table for User-B's APNs (Apple) or FCM (Google) device tokens, and triggers a Notification Server request to deliver an OS push alert.",
      C: "Option C is incorrect. Mobile OS power management blocks incoming ICMP pings when the phone or app is asleep.",
      D: "Option D is incorrect. Application logic handles offline delivery via background push notifications seamlessly."
    }
  }
];

let userAnswers = {};

function renderQuiz() {
  const container = document.getElementById('quiz-container');
  if (!container) return;

  container.innerHTML = quizQuestions.map((q, idx) => {
    return `
      <div class="quiz-card" id="quiz-card-${q.id}">
        <div class="quiz-card-header">
          <span class="quiz-topic">${q.topic}</span>
          <span class="quiz-qnum">Question ${idx + 1} of ${quizQuestions.length}</span>
        </div>
        <div class="quiz-question">${q.question}</div>
        <div class="quiz-options">
          ${Object.keys(q.options).map(optKey => `
            <button class="quiz-opt" id="opt-${q.id}-${optKey}" onclick="handleOptionClick(${q.id}, '${optKey}')">
              <span class="opt-letter">${optKey}</span>
              <span class="opt-text">${q.options[optKey]}</span>
            </button>
          `).join('')}
        </div>
        <div class="quiz-explanation" id="exp-${q.id}"></div>
      </div>
    `;
  }).join('');

  updateDashboard();
}

function handleOptionClick(questionId, selectedOpt) {
  const q = quizQuestions.find(item => item.id === questionId);
  if (!q) return;

  userAnswers[questionId] = selectedOpt;
  const isRight = (selectedOpt === q.correct);

  Object.keys(q.options).forEach(optKey => {
    const btn = document.getElementById(`opt-${questionId}-${optKey}`);
    if (!btn) return;

    btn.classList.remove('opt-correct', 'opt-incorrect');

    if (optKey === q.correct) {
      btn.classList.add('opt-correct');
    } else if (optKey === selectedOpt && !isRight) {
      btn.classList.add('opt-incorrect');
    }
  });

  const expDiv = document.getElementById(`exp-${questionId}`);
  if (expDiv) {
    expDiv.style.display = 'block';
    expDiv.className = `quiz-explanation ${isRight ? 'exp-right' : 'exp-wrong'}`;

    if (isRight) {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-check-circle" style="color: #16a34a;"></i> Correct Answer!
        </div>
        <div class="exp-body">
          ${q.explanations[selectedOpt]}
        </div>
      `;
    } else {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-times-circle" style="color: #dc2626;"></i> Incorrect Choice (Selected: ${selectedOpt})
        </div>
        <div class="exp-body">
          <p style="margin-bottom: 8px;"><strong>Why Option ${selectedOpt} is incorrect:</strong> ${q.explanations[selectedOpt]}</p>
          <p style="margin-top: 8px; margin-bottom: 0;"><strong>Correct Answer (${q.correct}):</strong> ${q.explanations[q.correct]}</p>
        </div>
      `;
    }
  }

  updateDashboard();
}

function updateDashboard() {
  const answeredCount = Object.keys(userAnswers).length;
  let correctCount = 0;

  Object.keys(userAnswers).forEach(qId => {
    const q = quizQuestions.find(item => item.id == qId);
    if (q && userAnswers[qId] === q.correct) {
      correctCount++;
    }
  });

  const total = quizQuestions.length;
  const accuracy = answeredCount > 0 ? Math.round((correctCount / answeredCount) * 100) : 0;
  const progressPercent = Math.round((answeredCount / total) * 100);

  document.getElementById('quiz-score').innerText = `${correctCount} / ${total}`;
  document.getElementById('quiz-progress').innerText = `${answeredCount} / ${total} Answered`;
  document.getElementById('quiz-accuracy').innerText = `${accuracy}%`;
  document.getElementById('quiz-progress-bar').style.width = `${progressPercent}%`;
}

function resetQuiz() {
  userAnswers = {};
  renderQuiz();
}

document.addEventListener('DOMContentLoaded', renderQuiz);
</script>
