---
layout: default
title: Quiz - Designing a URL Shortening Service
permalink: /System-Design/4-system-design-problems/4.3-quiz-designing-url-shortner/
prev_title: "Designing a URL Shortening Service like TinyURL"
prev_url: "/System-Design/4-system-design-problems/4.3-designing-a-url-shortening-service-like-tinyurl/"
---

# Quiz: Designing a URL Shortening Service

Test your knowledge of URL shortener architecture with **17 scenario-driven interview questions** covering requirements, scale estimation, KGS key generation, HTTP status codes, caching, database partitioning, and fault isolation.

---

<!-- Quiz Dashboard -->
<div class="quiz-dashboard">
  <div class="dash-stat">
    <span class="dash-label">Score</span>
    <span class="dash-value" id="quiz-score">0 / 17</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Progress</span>
    <span class="dash-value" id="quiz-progress">0 / 17 Answered</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Accuracy</span>
    <span class="dash-value" id="quiz-accuracy">0%</span>
  </div>
  <button class="quiz-reset-btn" onclick="resetQuiz()">
    <i class="fas fa-redo-alt"></i> Reset Quiz
  </button>
</div>

<div class="quiz-progress-bar-container">
  <div class="quiz-progress-bar" id="quiz-progress-bar" style="width: 0%;"></div>
</div>

<div id="quiz-container"></div>

<style>
/* Quiz Styling - Compatible with Light & Dark Themes */
.quiz-dashboard {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-secondary, #f8fafc);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 16px 24px;
  margin-bottom: 16px;
}

html.dark-theme .quiz-dashboard {
  background: #1e293b;
  border-color: #334155;
}

.dash-stat {
  display: flex;
  flex-direction: column;
}

.dash-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted, #64748b);
  font-weight: 600;
}

.dash-value {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-color, #0f172a);
}

html.dark-theme .dash-value {
  color: #f8fafc;
}

.quiz-reset-btn {
  background: #3b82f6;
  color: #ffffff;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, transform 0.1s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.quiz-reset-btn:hover {
  background: #2563eb;
  transform: translateY(-1px);
}

.quiz-progress-bar-container {
  width: 100%;
  height: 8px;
  background: var(--border-color, #e2e8f0);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 32px;
}

html.dark-theme .quiz-progress-bar-container {
  background: #334155;
}

.quiz-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #3b82f6, #10b981);
  transition: width 0.3s ease;
}

.quiz-card {
  background: var(--bg-color, #ffffff);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 28px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  transition: border-color 0.2s ease;
}

html.dark-theme .quiz-card {
  background: #0f172a;
  border-color: #1e293b;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

.quiz-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.quiz-topic {
  font-size: 0.82rem;
  font-weight: 600;
  color: #2563eb;
  background: rgba(37, 99, 235, 0.1);
  padding: 4px 10px;
  border-radius: 20px;
}

html.dark-theme .quiz-topic {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.15);
}

.quiz-qnum {
  font-size: 0.85rem;
  color: var(--text-muted, #64748b);
  font-weight: 500;
}

.quiz-question {
  font-size: 1.1rem;
  font-weight: 600;
  margin-top: 6px;
  margin-bottom: 20px;
  color: var(--text-color, #0f172a);
  line-height: 1.5;
}

html.dark-theme .quiz-question {
  color: #f1f5f9;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.quiz-opt {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  width: 100%;
  text-align: left;
  background: var(--bg-secondary, #f8fafc);
  border: 1.5px solid var(--border-color, #e2e8f0);
  border-radius: 10px;
  padding: 14px 16px;
  font-size: 0.95rem;
  color: var(--text-color, #334155);
  cursor: pointer;
  transition: all 0.2s ease;
}

html.dark-theme .quiz-opt {
  background: #1e293b;
  border-color: #334155;
  color: #cbd5e1;
}

.quiz-opt:hover:not(:disabled) {
  border-color: #3b82f6;
  background: rgba(59, 130, 246, 0.05);
}

html.dark-theme .quiz-opt:hover:not(:disabled) {
  border-color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
}

.opt-letter {
  font-weight: 700;
  background: var(--border-color, #cbd5e1);
  color: #0f172a;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  flex-shrink: 0;
  font-size: 0.85rem;
}

html.dark-theme .opt-letter {
  background: #475569;
  color: #f8fafc;
}

.opt-text {
  line-height: 1.45;
  flex-grow: 1;
}

/* Correct Option State */
.quiz-opt.opt-correct {
  background: #dcfce7 !important;
  border-color: #16a34a !important;
  color: #14532d !important;
}

.quiz-opt.opt-correct .opt-letter {
  background: #16a34a !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-correct {
  background: rgba(22, 163, 74, 0.2) !important;
  border-color: #22c55e !important;
  color: #bbf7d0 !important;
}

/* Incorrect Option State */
.quiz-opt.opt-incorrect {
  background: #fef2f2 !important;
  border-color: #dc2626 !important;
  color: #7f1d1d !important;
}

.quiz-opt.opt-incorrect .opt-letter {
  background: #dc2626 !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-incorrect {
  background: rgba(220, 38, 38, 0.2) !important;
  border-color: #ef4444 !important;
  color: #fecaca !important;
}

/* Explanation Box */
.quiz-explanation {
  margin-top: 18px;
  padding: 16px;
  border-radius: 10px;
  font-size: 0.93rem;
  line-height: 1.55;
  display: none;
}

.quiz-explanation.exp-right {
  background: #f0fdf4;
  border: 1px solid #86efac;
  color: #166534;
}

html.dark-theme .quiz-explanation.exp-right {
  background: rgba(22, 163, 74, 0.15);
  border-color: #22c55e;
  color: #bbf7d0;
}

.quiz-explanation.exp-wrong {
  background: #fef2f2;
  border: 1px solid #fca5a5;
  color: #991b1b;
}

html.dark-theme .quiz-explanation.exp-wrong {
  background: rgba(220, 38, 38, 0.15);
  border-color: #ef4444;
  color: #fecaca;
}

.exp-header {
  font-weight: 700;
  font-size: 1rem;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.exp-body {
  margin-top: 6px;
}
</style>

<script>
const quizQuestions = [
  {
    id: 1,
    topic: "System Requirements & Impact",
    question: "During a system design interview, a candidate suggests that a URL shortener service can operate at 99% availability since 'it is just a utility service.' Why is this considered a severe misunderstanding of non-functional requirements?",
    options: {
      A: "Because 99% availability prevents user authentication tokens from working properly.",
      B: "Because when a URL shortener fails, every short link ever distributed across emails, social media, printed materials, and external sites breaks globally, causing widespread external outages.",
      C: "Because a 99% SLA increases database storage costs tenfold.",
      D: "Because URL shorteners only store temporary data that cannot be recovered."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. URL shortening availability is about link resolution, not user authentication token handling.",
      B: "Option B is correct! Outages in a URL shortener break external links across third-party websites, marketing campaigns, and emails worldwide, making high availability (99.99%+) a non-negotiable requirement.",
      C: "Option C is incorrect. SLA percentage defines uptime targets, not direct storage cost multipliers.",
      D: "Option D is incorrect. Stored URL mappings are persistent primary data, not unrecoverable temporary data."
    }
  },
  {
    id: 2,
    topic: "Scale Estimation (Traffic QPS)",
    question: "A URL shortening system generates 500 million new short links per month with a 100:1 read-to-write ratio. Assuming 100,000 seconds per day, what are the expected average Write QPS and Read QPS?",
    options: {
      A: "Write QPS: ~20 writes/sec | Read QPS: ~2,000 reads/sec",
      B: "Write QPS: ~200 writes/sec | Read QPS: ~20,000 reads/sec",
      C: "Write QPS: ~2,000 writes/sec | Read QPS: ~200,000 reads/sec",
      D: "Write QPS: ~500 writes/sec | Read QPS: ~50,000 reads/sec"
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. 500M / 30 days = 16.6M links/day. 16.6M / 100,000 sec = ~170 writes/sec (rounded to ~200 writes/sec), not 20.",
      B: "Option B is correct! 500M / 30 days = 16.6M links/day. 16.6M / 100,000 sec ≈ 170 (rounded to 200 Write QPS). Multiplying by 100 gives 20,000 Read QPS.",
      C: "Option C is incorrect. 2,000 Write QPS would equal 5 billion new links per month.",
      D: "Option D is incorrect. 500 Write QPS would equal 1.5 billion new links per month."
    }
  },
  {
    id: 3,
    topic: "Cache Memory Sizing",
    question: "The redirect service processes 2 billion daily reads. Applying the 80/20 Pareto principle to cache the top 20% of popular links in memory, what is the estimated cache RAM required if each payload is 500 bytes?",
    options: {
      A: "20 GB RAM",
      B: "200 GB RAM",
      C: "2 TB RAM",
      D: "500 GB RAM"
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. 20 GB represents caching only 2% of daily reads.",
      B: "Option B is correct! 20% of 2 billion reads = 400 million items. 400M × 500 bytes = 200,000,000,000 bytes = 200 GB RAM.",
      C: "Option C is incorrect. 2 TB would cache 100% of 4 billion daily reads.",
      D: "Option D is incorrect. Math calculation: 0.2 × 2B × 500B = 200 GB, not 500 GB."
    }
  },
  {
    id: 4,
    topic: "Data Model & Storage Design",
    question: "A developer proposes adding a `click_count` column directly to the primary `urls` table row and executing `UPDATE urls SET click_count = click_count + 1 WHERE short_key = ?` on every redirect. Why is this a major architectural anti-pattern?",
    options: {
      A: "Because SQL databases do not support integer increment operations.",
      B: "Because it converts 20,000 Read QPS into 20,000 synchronous database write operations, creating severe write hotspots on viral links and degrading redirect latency.",
      C: "Because click counts can only be recorded using client-side JavaScript.",
      D: "Because key-value stores reject update commands."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. All major database engines support atomic integer increment operations.",
      B: "Option B is correct! Updating a database row on every redirect converts cheap read traffic into expensive synchronous database writes. Viral short links would saturate row locks and cripple latency. Click analytics must be handled asynchronously off-path.",
      C: "Option C is incorrect. Server-side redirect handlers emit click telemetry before returning HTTP 302 headers.",
      D: "Option D is incorrect. Key-value stores support atomic increments, but doing so synchronously on every redirect breaks read scalability."
    }
  },
  {
    id: 5,
    topic: "Read vs. Write Path Asymmetry",
    question: "In a URL shortener system handling 200 Write QPS and 20,000 Read QPS (100:1 ratio), where should caching layers and read-replicas be deployed?",
    options: {
      A: "Exclusively on the Write Path, because key generation requires caching.",
      B: "Exclusively on the Read Path (Redirect Service), because reads outnumber writes 100-to-1; the write path needs key correctness rather than caching.",
      C: "Equally distributed across both paths with identical replica counts.",
      D: "Neither path needs caching if SSD drives are utilized."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Caching write operations does not optimize a read-heavy 100:1 system.",
      B: "Option B is correct! Asymmetric traffic demands targeted optimization. The Read Path handles 99% of total QPS, so Redis caches and read replicas belong on the read path. The write path requires pre-generated unique keys, not read caching.",
      C: "Option C is incorrect. Equal scaling wastes resources on the low-volume write path (200 QPS).",
      D: "Option D is incorrect. In-memory RAM caching (sub-millisecond) is significantly faster than SSD disk lookups under 20,000 QPS."
    }
  },
  {
    id: 6,
    topic: "Key Length & Guessability Math",
    question: "Why is a 6-character Base62 key ($62^6 \\approx 56.8$ billion) insufficient when storing 30 billion URLs, whereas a 7-character Base62 key ($62^7 \\approx 3.5$ trillion) satisfies security requirements?",
    options: {
      A: "Because 6-character strings cannot be indexed by database engines.",
      B: "Because 30 billion stored URLs occupy >52% of a 6-character keyspace, allowing random guessing to find valid links on every 2nd try; 7 characters reduces keyspace occupancy to <0.9%.",
      C: "Because Base62 requires odd-numbered string lengths.",
      D: "Because 6-character keys cause CPU memory alignment faults."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Database engines index strings of any length (e.g. VARCHAR(6)).",
      B: "Option B is correct! Storing 30B records in a 56.8B keyspace fills 52.8% of all possible keys. An attacker guessing random 6-character strings hits a real link ~53% of the time. Adding a 7th character expands the keyspace 62-fold to 3.5T, dropping occupancy to ~0.85% (1 hit per 117 random tries), satisfying non-guessability.",
      C: "Option C is incorrect. Base62 character encoding supports arbitrary string lengths.",
      D: "Option D is incorrect. String character length has no correlation with hardware memory alignment faults."
    }
  },
  {
    id: 7,
    topic: "Base62 vs. Base64 URL Encoding",
    question: "Why do URL shortening architectures prefer Base62 (`[A-Z, a-z, 0-9]`) over standard Base64 encoding?",
    options: {
      A: "Base64 uses 128 bits per character, making keys twice as long.",
      B: "Standard Base64 contains `+` and `/`; `/` acts as a URL path segment delimiter and `+` is often space-decoded by HTTP proxies, forcing percent-encoding which lengthens URLs.",
      C: "Base62 supports encryption whereas Base64 is unencrypted.",
      D: "Base64 characters cannot be stored in NoSQL databases."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Base64 encodes 6 bits per character, not 128 bits.",
      B: "Option B is correct! Standard Base64 includes `+` and `/`. In HTTP URLs, `/` splits path segments (e.g., `domain.com/a/b`) and `+` is decoded as a space by form parsers. Percent-encoding them (`%2B`, `%2F`) makes short links longer and harder to read.",
      C: "Option C is incorrect. Neither Base62 nor Base64 are encryption algorithms; both are encoding schemes.",
      D: "Option D is incorrect. All databases support storing Base64 string characters."
    }
  },
  {
    id: 8,
    topic: "URL Hashing vs. Key Generation Service",
    question: "An engineer proposes generating short keys by hashing the long URL (MD5/SHA-256) and taking the first 7 Base62 characters. What is the critical failure mode of this approach under 30 billion stored records?",
    options: {
      A: "MD5 hashing requires hardware GPU acceleration for every write.",
      B: "Truncating a hash to 7 characters (42 bits) causes frequent hash collisions, requiring live database read checks and retry loops on the synchronous request path as the database fills.",
      C: "Hashing long URLs converts HTTP 302 redirects into HTTP 500 errors.",
      D: "SHA-256 produces variable-length strings that violate fixed database schemas."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Hash functions like MD5 run in microseconds on standard CPUs.",
      B: "Option B is correct! Truncating a hash to 42 bits creates hash collisions across 30B records. When a collision occurs, the write handler must query the database, generate a new hash seed, and retry. This retry loop runs synchronously while the user waits, degrading write latency.",
      C: "Option C is incorrect. Hash generation affects short key generation, not redirect response headers.",
      D: "Option D is incorrect. SHA-256 produces a fixed 256-bit (64 hex character) digest."
    }
  },
  {
    id: 9,
    topic: "Key Generation Service (KGS) Architecture",
    question: "How does a dedicated Key Generation Service (KGS) eliminate collision detection latency from the synchronous URL shortening write path?",
    options: {
      A: "By running MD5 hashing directly inside Redis cache memory.",
      B: "By pre-computing random unique 7-character Base62 keys offline and buffering them in a pool; the shortening service simply pops an unassigned key without hashing or live database collision checks.",
      C: "By using client-side JavaScript to generate random keys on the user's browser.",
      D: "By disabling primary key uniqueness constraints on the database."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Moving hash calculation to Redis does not resolve collision retry loops.",
      B: "Option B is correct! KGS generates unique random 7-character Base62 keys in a background process offline. When a write request arrives, the application server fetches a pre-generated key from memory and writes the DB record—moving uniqueness verification entirely off the live request path.",
      C: "Option C is incorrect. Client-side key generation cannot guarantee cross-client uniqueness without server coordination.",
      D: "Option D is incorrect. Disabling primary key constraints leads to database data corruption."
    }
  },
  {
    id: 10,
    topic: "KGS Concurrency & Key Allocation",
    question: "How does KGS prevent two concurrent application server instances from assigning the exact same pre-generated short key to different long URLs?",
    options: {
      A: "By acquiring a global distributed lock on every single write request.",
      B: "By pre-allocating keys to application servers in bulk blocks (e.g., 1,000 keys) and marking the entire block as 'used' in the KGS database before loading them into server RAM.",
      C: "By routing all write requests to a single-threaded monolithic web server.",
      D: "By broadcasting UDP multicast packets to all web servers on every write."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Global distributed locks per write introduce high latency and contention bottlenecks.",
      B: "Option B is correct! KGS assigns contiguous blocks of pre-generated keys (e.g., Server A gets keys 1–1000, Server B gets 1001–2000). KGS marks the block used atomically. Servers buffer keys in local memory and assign them locally with zero lock contention.",
      C: "Option C is incorrect. Single-threaded monolithic web servers cannot scale horizontally across CPU cores.",
      D: "Option D is incorrect. UDP multicast provides no delivery or atomic ordering guarantees."
    }
  },
  {
    id: 11,
    topic: "Custom Short Aliases Handling",
    question: "When a user requests a custom short alias (e.g. `tinyurl.com/my-brand-link`), how does the system handle key uniqueness verification?",
    options: {
      A: "It retrieves a pre-generated key from the KGS pool and overwrites it.",
      B: "It performs an explicit synchronous database lookup for the custom alias; if present, it returns HTTP 409 Conflict; otherwise, it inserts the row.",
      C: "It rejects all custom alias requests because KGS cannot handle text strings.",
      D: "It appends a random 7-character KGS string to the end of the custom alias."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Pre-generated KGS keys are random strings and cannot fulfill custom user text.",
      B: "Option B is correct! Custom aliases cannot be pre-generated by KGS. The system performs a synchronous database check for that specific alias. If taken, it returns HTTP 409 Conflict. Because custom aliases are rare (<1% of writes), this synchronous check does not impact overall system throughput.",
      C: "Option C is incorrect. Custom aliases are a functional requirement supported via fallback synchronous checks.",
      D: "Option D is incorrect. Appending random strings alters the custom link requested by the user."
    }
  },
  {
    id: 12,
    topic: "HTTP Status Codes (302 Found vs. 301 Moved Permanently)",
    question: "Why do commercial URL shorteners return `HTTP 302 Found` (Temporary Redirect) instead of `HTTP 301 Moved Permanently` for destination redirects?",
    options: {
      A: "HTTP 301 redirects are limited to HTTPS URLs.",
      B: "HTTP 301 causes browsers to cache the redirect target permanently locally, bypassing shortener servers on subsequent clicks and destroying click analytics telemetry.",
      C: "HTTP 302 automatically encrypts the payload during transit.",
      D: "HTTP 301 is unsupported by mobile web browsers."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. HTTP 301 works identically for HTTP and HTTPS URLs.",
      B: "Option B is correct! An HTTP 301 response instructs browsers to cache the redirect destination permanently in browser memory. Future clicks on that short link bypass the URL shortener servers entirely, preventing the system from recording click count analytics.",
      C: "Option C is incorrect. HTTP status codes define redirection semantics, not transport-layer encryption.",
      D: "Option D is incorrect. HTTP 301 is universally supported across all mobile browsers."
    }
  },
  {
    id: 13,
    topic: "Caching & Negative Cache Protection",
    question: "A malicious botnet launches a cache-penetration attack, issuing 50,000 requests per second for non-existent or expired short keys. How does the caching layer protect the underlying database?",
    options: {
      A: "By restarting Redis cluster nodes every 60 seconds.",
      B: "By implementing Negative Caching—storing non-existent/expired keys in Redis with a short Time-To-Live (e.g. 2 minutes)—so repeated dead requests hit cache instead of DB.",
      C: "By routing all 404 traffic directly to the Kafka analytics queue.",
      D: "By converting all non-existent keys into HTTP 302 redirects to Google."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Flushing Redis nodes degrades legitimate cache hit rates.",
      B: "Option B is correct! Negative caching stores empty or `NULL` cache entries for missing keys with a brief TTL (e.g., 2 minutes). Subsequent bot queries for the same dead keys hit Redis and return HTTP 404 instantly without touching the backend database.",
      C: "Option C is incorrect. Message queues buffer telemetry; they do not process HTTP 404 client responses.",
      D: "Option D is incorrect. Redirecting invalid keys to external sites breaks standard API contracts."
    }
  },
  {
    id: 14,
    topic: "Database Partitioning & Consistent Hashing",
    question: "Why is Consistent Hashing by `short_key` preferred over Range-Based Partitioning (e.g. partition by 1st letter A–Z) when sharding a 15 TB URL database across 32 nodes?",
    options: {
      A: "Range partitioning requires relational foreign key constraints.",
      B: "Pseudo-random Base62 keys hashed via Consistent Hashing distribute data storage and read QPS uniformly across nodes, whereas Range Partitioning causes severe traffic hotspots.",
      C: "Consistent Hashing eliminates the need for database read replicas.",
      D: "Range partitioning restricts total database storage to 1 GB."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Range partitioning can be implemented in both SQL and NoSQL databases.",
      B: "Option B is correct! Range partitioning (e.g. keys starting with 'a') creates hotspots if user key generation or custom aliases favor specific characters. Consistent Hashing on pseudo-random Base62 keys guarantees uniform row and QPS distribution across all 32 partitions.",
      C: "Option C is incorrect. Read replicas are required for fault tolerance and read scaling regardless of partitioning algorithm.",
      D: "Option D is incorrect. Partitioning strategies affect data distribution, not artificial capacity caps."
    }
  },
  {
    id: 15,
    topic: "Link Expiration & Key Security",
    question: "An architect suggests recycling expired short keys back into the KGS pool to conserve key storage. Why is key recycling strongly discouraged in production URL shorteners?",
    options: {
      A: "Recycling keys increases Redis memory usage by 500%.",
      B: "Reusing a short key causes old printed links, bookmarks, or emails to suddenly point to new external websites, creating severe phishing and security vulnerabilities.",
      C: "Database primary keys cannot be overwritten once deleted.",
      D: "KGS key pools only support append-only array operations."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Key recycling does not increase Redis RAM footprint.",
      B: "Option B is correct! Short links printed on business cards, embedded in old emails, or saved in browser bookmarks persist for years. If an expired key is recycled for a new destination, old users opening the link arrive at an unexpected new site, introducing severe security risks and brand confusion. With 3.5 trillion keys available (7 Base62 chars), recycling is completely unnecessary.",
      C: "Option C is incorrect. Databases allow reusing primary key values after row deletion.",
      D: "Option D is incorrect. KGS data stores support standard CRUD operations."
    }
  },
  {
    id: 16,
    topic: "Asynchronous Analytics Pipeline",
    question: "How does the URL shortener architecture collect rich click telemetry (IP address, country, user agent, referrer) without increasing user redirect latency?",
    options: {
      A: "By performing synchronous HTTP POST requests to an external analytics API before returning HTTP 302.",
      B: "By returning the HTTP 302 response immediately and asynchronously publishing a click event payload to an Apache Kafka message queue for batch processing.",
      C: "By storing click log files on the client's local browser storage.",
      D: "By executing analytical SQL aggregation queries inside the HTTP GET handler thread."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Synchronous external API calls add hundreds of milliseconds of latency to every redirect.",
      B: "Option B is correct! Decoupling analytics onto an asynchronous event queue (Kafka) ensures the redirect handler returns HTTP 302 instantly. Background stream workers (Flink/Spark) consume Kafka events in batches and write aggregated counts to an OLAP database.",
      C: "Option C is incorrect. Client-side local storage cannot aggregate global analytics across users.",
      D: "Option D is incorrect. Running SQL aggregation queries inside redirect threads saturates CPU and database connections."
    }
  },
  {
    id: 17,
    topic: "Fault Isolation & Resilience",
    question: "Suppose both the Key Generation Service (KGS) and the Apache Kafka analytics cluster suffer a simultaneous total outage. What is the operational state of the live URL Redirect Service?",
    options: {
      A: "Redirects fail completely and return HTTP 500 Internal Server Error.",
      B: "Redirects continue functioning normally without degradation because the read path depends exclusively on the Redis cache and URL database; write and analytics failures are completely isolated.",
      C: "The database automatically locks all read partitions until KGS recovers.",
      D: "All active Redis cache entries are evicted immediately."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. The redirect path does not depend on KGS or Kafka to resolve short keys.",
      B: "Option B is correct! This is a core architectural strength of the design: fault isolation. The redirect path reads exclusively from Redis/NoSQL database. KGS outage only blocks NEW link creation; Kafka outage only delays analytics updates. Existing redirects continue working at 100% availability.",
      C: "Option C is incorrect. Database partitions operate independently of key generation services.",
      D: "Option D is incorrect. Cache eviction policies (LRU/TTL) are independent of external service availability."
    }
  }
];

let userAnswers = {};

function renderQuiz() {
  const container = document.getElementById('quiz-container');
  if (!container) return;

  container.innerHTML = quizQuestions.map((q, idx) => {
    return `
      <div class="quiz-card" id="quiz-card-${q.id}">
        <div class="quiz-card-header">
          <span class="quiz-topic">${q.topic}</span>
          <span class="quiz-qnum">Question ${idx + 1} of ${quizQuestions.length}</span>
        </div>
        <div class="quiz-question">${q.question}</div>
        <div class="quiz-options">
          ${Object.keys(q.options).map(optKey => `
            <button class="quiz-opt" id="opt-${q.id}-${optKey}" onclick="handleOptionClick(${q.id}, '${optKey}')">
              <span class="opt-letter">${optKey}</span>
              <span class="opt-text">${q.options[optKey]}</span>
            </button>
          `).join('')}
        </div>
        <div class="quiz-explanation" id="exp-${q.id}"></div>
      </div>
    `;
  }).join('');

  updateDashboard();
}

function handleOptionClick(questionId, selectedOpt) {
  const q = quizQuestions.find(item => item.id === questionId);
  if (!q) return;

  userAnswers[questionId] = selectedOpt;
  const isRight = (selectedOpt === q.correct);

  Object.keys(q.options).forEach(optKey => {
    const btn = document.getElementById(`opt-${questionId}-${optKey}`);
    if (!btn) return;

    btn.classList.remove('opt-correct', 'opt-incorrect');

    if (optKey === q.correct) {
      btn.classList.add('opt-correct');
    } else if (optKey === selectedOpt && !isRight) {
      btn.classList.add('opt-incorrect');
    }
  });

  const expDiv = document.getElementById(`exp-${questionId}`);
  if (expDiv) {
    expDiv.style.display = 'block';
    expDiv.className = `quiz-explanation ${isRight ? 'exp-right' : 'exp-wrong'}`;

    if (isRight) {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-check-circle" style="color: #16a34a;"></i> Correct Answer!
        </div>
        <div class="exp-body">
          ${q.explanations[selectedOpt]}
        </div>
      `;
    } else {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-times-circle" style="color: #dc2626;"></i> Incorrect Choice (Selected: ${selectedOpt})
        </div>
        <div class="exp-body">
          <p style="margin-bottom: 8px;"><strong>Why Option ${selectedOpt} is incorrect:</strong> ${q.explanations[selectedOpt]}</p>
          <p style="margin-top: 8px; margin-bottom: 0;"><strong>Correct Answer (${q.correct}):</strong> ${q.explanations[q.correct]}</p>
        </div>
      `;
    }
  }

  updateDashboard();
}

function updateDashboard() {
  const answeredCount = Object.keys(userAnswers).length;
  let correctCount = 0;

  Object.keys(userAnswers).forEach(qId => {
    const q = quizQuestions.find(item => item.id == qId);
    if (q && userAnswers[qId] === q.correct) {
      correctCount++;
    }
  });

  const total = quizQuestions.length;
  const accuracy = answeredCount > 0 ? Math.round((correctCount / answeredCount) * 100) : 0;
  const progressPercent = Math.round((answeredCount / total) * 100);

  document.getElementById('quiz-score').innerText = `${correctCount} / ${total}`;
  document.getElementById('quiz-progress').innerText = `${answeredCount} / ${total} Answered`;
  document.getElementById('quiz-accuracy').innerText = `${accuracy}%`;
  document.getElementById('quiz-progress-bar').style.width = `${progressPercent}%`;
}

function resetQuiz() {
  userAnswers = {};
  renderQuiz();
}

document.addEventListener('DOMContentLoaded', renderQuiz);
</script>
