---
layout: default
title: Quiz - Designing Dropbox
permalink: /System-Design/4-system-design-problems/4.6-quiz-designing-dropbox/
prev_title: "Designing Dropbox"
prev_url: "/System-Design/4-system-design-problems/4.6-designing-dropbox/"
next_title: "Designing Facebook Messenger"
next_url: "/System-Design/4-system-design-problems/4.7-designing-facebook-messenger/"
---

# Quiz: Designing Dropbox

Test your architectural knowledge of Dropbox system design with **9 scenario-driven interview questions** covering 4MB file chunking, delta sync trade-offs, client component design (Watcher/Indexer/Chunker), HTTP long polling, per-client response queues, in-line vs. post-process deduplication, consistent hash sharding, chunk immutability caching, and AI semantic indexing.

---

<!-- Quiz Dashboard -->
<div class="quiz-dashboard">
  <div class="dash-stat">
    <span class="dash-label">Score</span>
    <span class="dash-value" id="quiz-score">0 / 9</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Progress</span>
    <span class="dash-value" id="quiz-progress">0 / 9 Answered</span>
  </div>
  <div class="dash-stat">
    <span class="dash-label">Accuracy</span>
    <span class="dash-value" id="quiz-accuracy">0%</span>
  </div>
  <button class="quiz-reset-btn" onclick="resetQuiz()">
    <i class="fas fa-redo-alt"></i> Reset Quiz
  </button>
</div>

<div class="quiz-progress-bar-container">
  <div class="quiz-progress-bar" id="quiz-progress-bar" style="width: 0%;"></div>
</div>

<div id="quiz-container"></div>

<style>
/* Quiz Styling - Compatible with Light & Dark Themes */
.quiz-dashboard {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  align-items: center;
  justify-content: space-between;
  background: var(--bg-secondary, #f8fafc);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 16px 24px;
  margin-bottom: 16px;
}

html.dark-theme .quiz-dashboard {
  background: #1e293b;
  border-color: #334155;
}

.dash-stat {
  display: flex;
  flex-direction: column;
}

.dash-label {
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-muted, #64748b);
  font-weight: 600;
}

.dash-value {
  font-size: 1.4rem;
  font-weight: 700;
  color: var(--text-color, #0f172a);
}

html.dark-theme .dash-value {
  color: #f8fafc;
}

.quiz-reset-btn {
  background: #3b82f6;
  color: #ffffff;
  border: none;
  padding: 10px 18px;
  border-radius: 8px;
  font-weight: 600;
  cursor: pointer;
  transition: background 0.2s ease, transform 0.1s ease;
  display: flex;
  align-items: center;
  gap: 8px;
}

.quiz-reset-btn:hover {
  background: #2563eb;
  transform: translateY(-1px);
}

.quiz-progress-bar-container {
  width: 100%;
  height: 8px;
  background: var(--border-color, #e2e8f0);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: 32px;
}

html.dark-theme .quiz-progress-bar-container {
  background: #334155;
}

.quiz-progress-bar {
  height: 100%;
  background: linear-gradient(90deg, #3b82f6, #10b981);
  transition: width 0.3s ease;
}

.quiz-card {
  background: var(--bg-color, #ffffff);
  border: 1px solid var(--border-color, #e2e8f0);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 28px;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
  transition: border-color 0.2s ease;
}

html.dark-theme .quiz-card {
  background: #0f172a;
  border-color: #1e293b;
  box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.3);
}

.quiz-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.quiz-topic {
  font-size: 0.82rem;
  font-weight: 600;
  color: #2563eb;
  background: rgba(37, 99, 235, 0.1);
  padding: 4px 10px;
  border-radius: 20px;
}

html.dark-theme .quiz-topic {
  color: #60a5fa;
  background: rgba(96, 165, 250, 0.15);
}

.quiz-qnum {
  font-size: 0.85rem;
  color: var(--text-muted, #64748b);
  font-weight: 500;
}

.quiz-question {
  font-size: 1.1rem;
  font-weight: 600;
  margin-top: 6px;
  margin-bottom: 20px;
  color: var(--text-color, #0f172a);
  line-height: 1.5;
}

html.dark-theme .quiz-question {
  color: #f1f5f9;
}

.quiz-options {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.quiz-opt {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  width: 100%;
  text-align: left;
  background: var(--bg-secondary, #f8fafc);
  border: 1.5px solid var(--border-color, #e2e8f0);
  border-radius: 10px;
  padding: 14px 16px;
  font-size: 0.95rem;
  color: var(--text-color, #334155);
  cursor: pointer;
  transition: all 0.2s ease;
}

html.dark-theme .quiz-opt {
  background: #1e293b;
  border-color: #334155;
  color: #cbd5e1;
}

.quiz-opt:hover:not(:disabled) {
  border-color: #3b82f6;
  background: rgba(59, 130, 246, 0.05);
}

html.dark-theme .quiz-opt:hover:not(:disabled) {
  border-color: #60a5fa;
  background: rgba(96, 165, 250, 0.1);
}

.opt-letter {
  font-weight: 700;
  background: var(--border-color, #cbd5e1);
  color: #0f172a;
  width: 26px;
  height: 26px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  flex-shrink: 0;
  font-size: 0.85rem;
}

html.dark-theme .opt-letter {
  background: #475569;
  color: #f8fafc;
}

.opt-text {
  line-height: 1.45;
  flex-grow: 1;
}

/* Correct Option State */
.quiz-opt.opt-correct {
  background: #dcfce7 !important;
  border-color: #16a34a !important;
  color: #14532d !important;
}

.quiz-opt.opt-correct .opt-letter {
  background: #16a34a !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-correct {
  background: rgba(22, 163, 74, 0.2) !important;
  border-color: #22c55e !important;
  color: #bbf7d0 !important;
}

/* Incorrect Option State */
.quiz-opt.opt-incorrect {
  background: #fef2f2 !important;
  border-color: #dc2626 !important;
  color: #7f1d1d !important;
}

.quiz-opt.opt-incorrect .opt-letter {
  background: #dc2626 !important;
  color: #ffffff !important;
}

html.dark-theme .quiz-opt.opt-incorrect {
  background: rgba(220, 38, 38, 0.2) !important;
  border-color: #ef4444 !important;
  color: #fecaca !important;
}

/* Explanation Box */
.quiz-explanation {
  margin-top: 18px;
  padding: 16px;
  border-radius: 10px;
  font-size: 0.93rem;
  line-height: 1.55;
  display: none;
}

.quiz-explanation.exp-right {
  background: #f0fdf4;
  border: 1px solid #86efac;
  color: #166534;
}

html.dark-theme .quiz-explanation.exp-right {
  background: rgba(22, 163, 74, 0.15);
  border-color: #22c55e;
  color: #bbf7d0;
}

.quiz-explanation.exp-wrong {
  background: #fef2f2;
  border: 1px solid #fca5a5;
  color: #991b1b;
}

html.dark-theme .quiz-explanation.exp-wrong {
  background: rgba(220, 38, 38, 0.15);
  border-color: #ef4444;
  color: #fecaca;
}

.exp-header {
  font-weight: 700;
  font-size: 1rem;
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}

.exp-body {
  margin-top: 6px;
}
</style>

<script>
const quizQuestions = [
  {
    id: 1,
    topic: "File Chunking & Bandwidth Optimization",
    question: "Why does Dropbox split large files into fixed-size 4MB chunks and hash each chunk (SHA-256) instead of uploading files as single whole binaries?",
    options: {
      A: "To allow SQL metadata databases to store binary image chunks directly in BLOB columns.",
      B: "To minimize network transfer by syncing only modified chunks, lower retry costs during upload failures, and enable data deduplication across files.",
      C: "Because cloud storage providers like Amazon S3 reject object uploads larger than 4MB.",
      D: "To convert offline text documents into searchable PDF files automatically."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Large binary data is never stored in metadata databases; chunks are stored in cloud object storage.",
      B: "Option B is correct! Chunking files into 4MB pieces means a minor edit to a 1GB file requires transferring only the modified 4MB chunks rather than the entire file. If an upload fails mid-way, only the failed chunk is retried, saving bandwidth and time.",
      C: "Option C is incorrect. Amazon S3 natively supports object uploads up to 5TB.",
      D: "Option D is incorrect. Chunking is an architectural data movement mechanism, not document conversion."
    }
  },
  {
    id: 2,
    topic: "Delta Sync Server Trade-off",
    question: "When a client performs a delta sync by uploading only small byte diffs of a modified chunk, what computational trade-off occurs on the server?",
    options: {
      A: "The server must disable SSL/TLS encryption across all client connections.",
      B: "Because stored chunks are immutable, a block server must fetch the original chunk, apply the diff, hash the new chunk, and write it—trading client bandwidth savings for server read, compute, and write overhead.",
      C: "The metadata database is forced to switch from relational MySQL to a graph database.",
      D: "Delta sync requires client devices to maintain a permanent Wi-Fi connection."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Encryption protocols operate independently of file sync payload logic.",
      B: "Option B is correct! Chunks stored in cloud storage are immutable. Applying a diff on the server requires reading the old 4MB chunk, applying the patch in memory, computing the new SHA-256 hash, and writing the new chunk. This shifts work from client network bandwidth to server I/O and compute.",
      C: "Option C is incorrect. Database engine selection is driven by ACID requirements, not delta sync computation.",
      D: "Option D is incorrect. Offline edits are recorded locally regardless of network connection type."
    }
  },
  {
    id: 3,
    topic: "Client Component Architecture",
    question: "Which component inside the Dropbox client application monitors the local workspace folder and notifies the Indexer whenever a file or directory is created, updated, or deleted?",
    options: {
      A: "The Chunker",
      B: "The Internal Metadata Database",
      C: "The Watcher",
      D: "The Request Queue"
    },
    correct: "C",
    explanations: {
      A: "Option A is incorrect. The Chunker splits files into 4MB chunks and reconstructs files from chunks.",
      B: "Option B is incorrect. The Internal Metadata Database stores local file/chunk versions and locations on disk.",
      C: "Option C is correct! The Watcher monitors local workspace directories for OS filesystem events (create/update/delete) and alerts the Indexer. It also listens for change broadcasts from the remote Synchronization Service.",
      D: "Option D is incorrect. The Request Queue lives in the server-side Message Queuing Service, not on the client."
    }
  },
  {
    id: 4,
    topic: "Notification Protocol & Connection Scaling",
    question: "Why does Dropbox implement HTTP Long Polling instead of standard periodic polling for delivering real-time change notifications to clients?",
    options: {
      A: "Standard periodic polling requires keeping 10 million persistent database connections open simultaneously.",
      B: "Periodic polling introduces notification delays and generates massive unnecessary request volume with empty responses, whereas HTTP Long Polling holds open requests until changes occur.",
      C: "HTTP Long Polling removes the requirement for load balancers in front of web servers.",
      D: "Periodic polling disables local offline editing on desktop client devices."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Long polling holds HTTP connections on web/sync servers, not direct database connection pools.",
      B: "Option B is correct! Periodic polling forces clients to check every N seconds, causing delayed updates and sending huge numbers of redundant requests when no files changed. Long polling holds the connection open until a server update occurs or a timeout expires, delivering immediate sync.",
      C: "Option C is incorrect. Load balancers are still essential for managing the ~1M concurrent open long polling connections.",
      D: "Option D is incorrect. Offline editing relies on the local Internal Metadata Database."
    }
  },
  {
    id: 5,
    topic: "Message Queuing Architecture",
    question: "Why does the Message Queuing Service use a single global Request Queue alongside per-client Response Queues for each subscribed device?",
    options: {
      A: "To buffer raw binary file chunks in RAM before persisting them to cloud object storage.",
      B: "The global Request Queue load-balances metadata updates across Synchronization Service instances, while per-client Response Queues ensure every subscribed device asynchronously receives update notifications, even after being offline.",
      C: "To prevent NoSQL metadata tables from running out of primary keys.",
      D: "Because web browsers cannot parse asynchronous JSON messages."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Binary file chunks bypass queues and stream directly between clients and Cloud/Block Storage.",
      B: "Option B is correct! The shared global Request Queue decouples incoming client metadata updates so worker instances can process them at scale. Individual Response Queues ensure that every client device attached to a shared folder receives a dedicated copy of change events, persisting notifications until offline devices reconnect.",
      C: "Option C is incorrect. Primary key exhaustion is handled by 64-bit integer sizing.",
      D: "Option D is incorrect. Web browsers handle asynchronous JSON payloads natively."
    }
  },
  {
    id: 6,
    topic: "Data Deduplication Strategies",
    question: "What is the primary trade-off of using In-Line Deduplication compared to Post-Process Deduplication during chunk uploads?",
    options: {
      A: "In-line deduplication doubles the total raw storage consumed in Amazon S3.",
      B: "In-line deduplication requires a real-time chunk hash lookup on the synchronous upload path, adding latency while the user waits.",
      C: "In-line deduplication permanently deletes older file versions from the database metadata.",
      D: "Post-process deduplication corrupts immutable 4MB chunks."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. In-line deduplication prevents writing duplicate chunks, optimizing storage immediately.",
      B: "Option B is correct! In-line deduplication checks chunk hashes synchronously as data arrives. If the chunk exists, only a metadata reference is added. This optimizes network and storage immediately, but forces the client upload path to wait on the metadata hash lookup.",
      C: "Option C is incorrect. File versioning relies on chunk list metadata history, unaffected by deduplication timing.",
      D: "Option D is incorrect. Post-process deduplication stores chunks first and cleans up duplicate chunks asynchronously in the background."
    }
  },
  {
    id: 7,
    topic: "Metadata Partitioning Strategy",
    question: "When partitioning the metadata database across 100 billion files, why is Hash-based partitioning on `FileID` (with Consistent Hashing) preferred over Range-based partitioning on file paths?",
    options: {
      A: "Range-based partitioning on file path prefixes creates unbalanced server hotspots, whereas hashing `FileID` distributes data uniformly across database shards.",
      B: "Hash-based partitioning allows multi-table SQL joins across separate physical databases without network overhead.",
      C: "Range-based partitioning cannot store SHA-256 chunk hash keys.",
      D: "Hash-based partitioning eliminates the need for database read replicas."
    },
    correct: "A",
    explanations: {
      A: "Option A is correct! Range-based partitioning by first letter or path directory leads to severe hotspots if certain letters (e.g. '/documents' or 'E') contain a disproportionate share of files. Hashing `FileID` uniformly distributes metadata rows across all database partitions.",
      B: "Option B is incorrect. Cross-shard joins remain costly in any distributed partitioning scheme.",
      C: "Option C is incorrect. Data storage types are independent of database partition keys.",
      D: "Option D is incorrect. Read replicas are still needed for high availability and read scalability."
    }
  },
  {
    id: 8,
    topic: "Cache Eviction & Immutability",
    question: "In the Block Server caching layer (Memcached holding ~36,000 4MB chunks per 144GB server), why are cached chunks completely immune to cache staleness under LRU eviction?",
    options: {
      A: "Because Memcached automatically invalidates keys using background database triggers.",
      B: "Because file chunks are content-addressed and immutable; modifying chunk content produces a new SHA-256 hash and a brand-new cache key.",
      C: "Because metadata servers force block servers to flush RAM every hour.",
      D: "Because LRU eviction only applies to user session tokens."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Memcached does not maintain direct database triggers.",
      B: "Option B is correct! Chunks are content-addressed by their SHA-256 hash. Because chunk data never changes in place (any edit yields a different hash), a cached chunk entry can never become stale or inconsistent with storage.",
      C: "Option C is incorrect. Periodic cache flushes waste CPU and ruin cache hit ratios.",
      D: "Option D is incorrect. LRU (Least Recently Used) is an eviction policy applied to hot data blocks when memory fills up."
    }
  },
  {
    id: 9,
    topic: "AI Semantic Search & Derived Data",
    question: "How does the Dropbox architecture integrate AI semantic search (embeddings and OCR) without blocking the primary file synchronization path?",
    options: {
      A: "By calculating vector embeddings synchronously inside the client Chunker before transmitting bytes over HTTP.",
      B: "By having background workers subscribe to message queue change notifications asynchronously, compute embeddings, and write them to a derived search index.",
      C: "By replacing the remote SQL Metadata Database with an in-memory vector database.",
      D: "By storing raw 4MB file binary chunks directly inside the AI search engine."
    },
    correct: "B",
    explanations: {
      A: "Option A is incorrect. Heavy AI embedding compute on client devices would drain battery and slow file sync.",
      B: "Option B is correct! File synchronization triggers change notifications in the message queue. An asynchronous background AI worker consumes these events, generates vector embeddings/OCR text, and populates a search index. Because the index is derived data, it never blocks the sync path and can be completely rebuilt from source files if lost.",
      C: "Option C is incorrect. SQL/NoSQL databases remain the primary source of truth for ACID metadata and file structure.",
      D: "Option D is incorrect. Raw binary files stay in cloud object storage (S3), while search indices store numerical embeddings."
    }
  }
];

let userAnswers = {};

function renderQuiz() {
  const container = document.getElementById('quiz-container');
  if (!container) return;

  container.innerHTML = quizQuestions.map((q, idx) => {
    return `
      <div class="quiz-card" id="quiz-card-${q.id}">
        <div class="quiz-card-header">
          <span class="quiz-topic">${q.topic}</span>
          <span class="quiz-qnum">Question ${idx + 1} of ${quizQuestions.length}</span>
        </div>
        <div class="quiz-question">${q.question}</div>
        <div class="quiz-options">
          ${Object.keys(q.options).map(optKey => `
            <button class="quiz-opt" id="opt-${q.id}-${optKey}" onclick="handleOptionClick(${q.id}, '${optKey}')">
              <span class="opt-letter">${optKey}</span>
              <span class="opt-text">${q.options[optKey]}</span>
            </button>
          `).join('')}
        </div>
        <div class="quiz-explanation" id="exp-${q.id}"></div>
      </div>
    `;
  }).join('');

  updateDashboard();
}

function handleOptionClick(questionId, selectedOpt) {
  const q = quizQuestions.find(item => item.id === questionId);
  if (!q) return;

  userAnswers[questionId] = selectedOpt;
  const isRight = (selectedOpt === q.correct);

  Object.keys(q.options).forEach(optKey => {
    const btn = document.getElementById(`opt-${questionId}-${optKey}`);
    if (!btn) return;

    btn.classList.remove('opt-correct', 'opt-incorrect');

    if (optKey === q.correct) {
      btn.classList.add('opt-correct');
    } else if (optKey === selectedOpt && !isRight) {
      btn.classList.add('opt-incorrect');
    }
  });

  const expDiv = document.getElementById(`exp-${questionId}`);
  if (expDiv) {
    expDiv.style.display = 'block';
    expDiv.className = `quiz-explanation ${isRight ? 'exp-right' : 'exp-wrong'}`;

    if (isRight) {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-check-circle" style="color: #16a34a;"></i> Correct Answer!
        </div>
        <div class="exp-body">
          ${q.explanations[selectedOpt]}
        </div>
      `;
    } else {
      expDiv.innerHTML = `
        <div class="exp-header">
          <i class="fas fa-times-circle" style="color: #dc2626;"></i> Incorrect Choice (Selected: ${selectedOpt})
        </div>
        <div class="exp-body">
          <p style="margin-bottom: 8px;"><strong>Why Option ${selectedOpt} is incorrect:</strong> ${q.explanations[selectedOpt]}</p>
          <p style="margin-top: 8px; margin-bottom: 0;"><strong>Correct Answer (${q.correct}):</strong> ${q.explanations[q.correct]}</p>
        </div>
      `;
    }
  }

  updateDashboard();
}

function updateDashboard() {
  const answeredCount = Object.keys(userAnswers).length;
  let correctCount = 0;

  Object.keys(userAnswers).forEach(qId => {
    const q = quizQuestions.find(item => item.id == qId);
    if (q && userAnswers[qId] === q.correct) {
      correctCount++;
    }
  });

  const total = quizQuestions.length;
  const accuracy = answeredCount > 0 ? Math.round((correctCount / answeredCount) * 100) : 0;
  const progressPercent = Math.round((answeredCount / total) * 100);

  document.getElementById('quiz-score').innerText = `${correctCount} / ${total}`;
  document.getElementById('quiz-progress').innerText = `${answeredCount} / ${total} Answered`;
  document.getElementById('quiz-accuracy').innerText = `${accuracy}%`;
  document.getElementById('quiz-progress-bar').style.width = `${progressPercent}%`;
}

function resetQuiz() {
  userAnswers = {};
  renderQuiz();
}

document.addEventListener('DOMContentLoaded', renderQuiz);
</script>
